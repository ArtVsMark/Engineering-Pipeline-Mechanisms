{
  "schema": "1.2",
  "answers_to": "1.5",
  "_answers_to": "Версия ВЫГРУЗКИ каталога (export/rules.json → contracts.export), по которой построены ответы. Каталог сверяет её со своей и печатает отставание: подъём контракта — повод перечитать ответы, а не только формат (157). Читается из выгрузки, а не по памяти: первая редакция этого файла объявила schema 1.0, когда каталог ждал 1.2, и расхождение всплыло только на прогоне входящих.",
  "project": "ArtVsMark/Engineering-Pipeline-Mechanisms",
  "catalogue": "https://github.com/ArtVsMark/Engineering-Incidents-Playbook",
  "_": "Ответ ЭТОГО проекта по каждому правилу каталога. Статус — active, rejected, not-applicable или unreviewed; у двух отрицательных обязательна причина в поле why, у active — mechanism и where.",
  "_зачем": "Ответ живёт здесь, а не в каталоге, потому что здесь живёт механизм: одно правило в разных проектах держится по-разному.",
  "rules": {
    "001": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ghrest.py — единственный транспорт к площадке, REST стандартной библиотекой; tests/test_ghrest.py держит это гейтом: ни один механизм не импортирует urllib.request и не собирает заголовок с токеном сам. Раньше ответ был «pipeline»: транспорт соблюдался внимательностью, и пять копий обвязки с четырьмя разными разборами отказа — цена этого"
    },
    "002": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/ci.yml — джоб на каждый шаг; tests/test_gates_reject.py — гейты проверены тем, что обязаны отвергнуть"
    },
    "003": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/agent-pr.yml — приставка `agent/**` переключает открытие изменения; scripts/agent_pr.py отказывает ветке без неё (PREFIXES), а tests/test_gates_contract.py::test_branch_prefixes_match_the_workflow не даёт двум спискам разъехаться. Приставка одна: `claude/**` была переходной заглушкой и снята (094)"
    },
    "004": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/automerge.py — конфликтная голова ПРОПУСКАЕТСЯ, очередь идёт дальше и ничего не замораживает: изменение возвращается в контур 1 источником 1 (docs/behaviour.md). Держит tests/test_automerge.py::test_a_conflict_is_stepped_over_not_escalated. Тот же приём у сводного гейта: scripts/ci_complete.py не роняет опрос на красной совещательной, а помечает её и выносит вердикт по обязательным"
    },
    "005": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_version.py — число версии вне CONTRACT_VERSION и вне маркера краснеет"
    },
    "006": {
      "status": "active",
      "mechanism": "document",
      "where": "CLAUDE.md — раздел «Когда окно перезапускают»: срок и признаки, по которым не ждут календаря. Механизма нет и быть не может — счётчика сессии площадка не даёт"
    },
    "007": {
      "status": "not-applicable",
      "why": "механизма, решающего за окно, что чужая работа брошена, у проекта нет: исполнитель один, и «зависло или ждёт разрешения» видит владелец, а не автоматика. Реестра сессий площадка проекту не отдаёт — возраст окна ему не виден вовсе (об этом CLAUDE.md). Правило вступит вместе с очередью (шаг 8), где голова может стоять из-за окна, ждущего человека"
    },
    "008": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_docs_shape.py::test_no_collapsed_blocks — сворачиваемых блоков в документах нет ни одного. Основной читатель здесь окно: оно читает документ текстом и целиком, и `<details>` для него — заголовок без содержимого, причём пропажа выглядит пустым разделом, а не скрытым"
    },
    "009": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — записи проверок берутся с filter=latest и сворачиваются по ИМЕНИ, а не суммируются: повторный прогон кладёт на голову вторую запись того же имени, и счётчик по записям соврал бы. Границу этого свёртывания держит tests/test_gates_complete.py: отменённые записи filter=latest не отсекает, и они разбираются отдельно. То же в scripts/changerefs.py и scripts/squash_body.py — связи и трейлеры собираются без повторов"
    },
    "010": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — пустой список записей на голове считается отказом, а не «зелено»: «прогон не стартовал» и «все прошли» разведены; tests/test_gates_complete.py::test_no_records_on_the_head_is_rejected держит именно пустой вход, а ::test_missing_record_is_rejected — недостачу одного имени"
    },
    "011": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/ci_complete.py — опрос условный: с пределом ожидания и выходом по вердикту, а не бесконечный; прочие прогоны идут от событий, расписаний в проекте нет"
    },
    "012": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md, критические запреты — «НЕ пушить в ветку чужого изменения: её ведёт своё окно». Машинно не проверяется и назван в своде как раз поэтому"
    },
    "013": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_contract.py::test_workflows_do_not_embed_code — ни один шаг прогона не несёт встроенной программы: логика зовётся файлом из scripts/. Цена встроенного кода здесь двойная: оболочка меняет экранирование по дороге, и вдобавок его не касаются ни ruff, ни mypy, ни набор тестов — три гейта разом перестают его видеть"
    },
    "014": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_journal.py — починка, тронувшая механизм, обязана принести проверку: без входа, на котором гейт краснел до правки, убирается ПОВЕДЕНИЕ, а не разбор, и дефект возвращается молча. Держат tests/test_gates_reject.py::test_a_fix_without_a_test_is_rejected и ::test_a_fix_with_a_test_passes; починка прозы проверки не требует — требовать её значило бы заводить пустые проверки ради гейта. Замер 09.09.2026 по сорока последним коммитам общей ветки: починок с кодом 15, все принесли проверку — гейт закрепил соблюдаемое, а не ввёл новое. Половина правила механизмом не держится и названа пробелом в AGENTS.md: САМ замер «гейт краснел до починки» дереву доказать нечем"
    },
    "015": {
      "status": "not-applicable",
      "why": "параллельных исполнителей проект не запускает: окно одно, и оно же правит дерево. Правило вступит вместе с первым подагентом"
    },
    "016": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/report.py — обрезка вывода одна на все механизмы и называет полный объём; tests/test_report.py запрещает срез строки-вывода на месте, в обход общего механизма"
    },
    "017": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ghrest.py — остаток квоты читается из заголовков ответа при каждом запросе, исчерпание отличается от прочих отказов отдельным родом (RateLimited) и называет срок сброса; tests/test_ghrest.py держит разбор отказа"
    },
    "018": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/ci.yml — узлы и цепочка разведены по окружениям: набор в scripts/ и tests/ проверяет механизмы по одному на раннере, а цепочку — площадка, и только она. Поэтому решение принимается по прогону, а не по зелёному набору (139), а с #48 гейты идут ещё и на общей ветке: изменение и main — разные окружения, и зелёное на изменении не значит зелёного на main"
    },
    "019": {
      "status": "active",
      "mechanism": "document",
      "where": "docs/pipeline.md — сверка с соседями построена от ПОВЕРХНОСТЕЙ конвейера: четырнадцать шагов скелета, и по каждому видно, у кого эталон и чего нет ни у кого. Обход по файлам дал бы покрытие копий и пропустил ровно то, чего в файлах нет, — а именно отсутствующие шаги и оказались находкой замера"
    },
    "020": {
      "status": "not-applicable",
      "why": "волн параллельных исполнителей нет — нечего перезапускать дельтой"
    },
    "021": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_docs_shape.py::test_every_document_declares_its_reader — строка «Читатель:» обязана стоять в первых 12 строках каждого документа дерева; исключения названы поимённо (CHANGELOG.md — производное). Адресаты сами: AGENTS.md (агент) · CLAUDE.md (окно) · README.md (посетитель) · docs/pipeline.md и docs/behaviour.md (ведущий конвейер). Гейт перенесён от соседей: у троих из пяти правило уже держалось машиной (162)."
    },
    "022": {
      "status": "active",
      "mechanism": "document",
      "where": "docs/pipeline.md · docs/behaviour.md · docs/release.md — три вопроса, три канона; своды на них ссылаются и не пересказывают"
    },
    "023": {
      "status": "active",
      "mechanism": "document",
      "where": "README.md — витрина: что это, зачем, где граница с каталогом; устройство вынесено в docs/"
    },
    "024": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md — раздел «Открытая работа»: статусы живут в трекере, журнал сделанного — в CHANGELOG.md, в действующих документах их нет"
    },
    "025": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/journal.py — LINK_LINE_RE, форма строки со связью; scripts/build_changelog.py — номер задачи обязан быть ПОСЛЕДНЕЙ строкой фрагмента: там он датировка и там нужен; scripts/check_journal.py требует самого фрагмента у изменения. В объяснительных документах номер стоит только там, где назван пробел с адресом (046) — «чего ещё нет» в AGENTS.md, шаги в docs/roadmap.md, — а не как отметка о том, когда абзац написан"
    },
    "026": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_bindings.py::test_negative_answers_name_the_reason — отклонить правило каталога без причины нельзя: у rejected и not-applicable поле why обязательно, и след отклонения живёт в .rules/bindings.json рядом с ответом. У находок ревью тот же приём: запись снимается строкой «Разобрано: <отпечаток>» в теле изменения (scripts/review_findings.py), то есть отклонение едет вместе с работой и остаётся в истории, а не стирается из тела задачи молча"
    },
    "027": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/review_findings.py — render_body печатает «Пусто», а не оставляет пустой раздел; .rules/proposals.json объявляет пустой список законным состоянием; tests/test_review_findings.py::test_empty_body_says_so_explicitly"
    },
    "028": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md, раздел «Открытая работа» — комплексная задача ведётся подзадачами площадки, а не прозой в теле: состояние читается счётчиком, а не вычитыванием. Так закрыт эпик #7 — восемь подзадач, счётчик 8/8, а не абзац о том, что осталось"
    },
    "029": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md · CLAUDE.md — компактные триггеры со ссылкой на канон в docs/"
    },
    "030": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_journal.py — изменение без фрагмента отвергается; changelog.d/ — фрагменты; scripts/build_changelog.py — сборка"
    },
    "031": {
      "status": "not-applicable",
      "why": "залпов параллельных исполнителей проект не делает"
    },
    "032": {
      "status": "active",
      "mechanism": "document",
      "where": "docs/decisions/001-attribution-of-merged-1.md и docs/decisions/006-merge-by-squash.md — вывод о поведении площадки сделан ЗАПУСКОМ: автор и подпись посмотрены на слитом изменении, способ слияния — по истории соседа, а не вычитан из документации. У роли внешнего взгляда этого пока нет: ревью читает код и не может прогнать проверки — названо задачей #33"
    },
    "033": {
      "status": "not-applicable",
      "why": "длинной волновой работы с бюджетом у проекта нет: прогоны идут на событие, а не пачками, и упереться в стену посреди волны нечему. Единственный исчерпаемый ресурс — квота площадки, и она держится порогом останова (scripts/ghrest.py), а не расчётом темпа. Правило вступит, если появится массовый обход (перенос механизмов по пяти проектам разом)"
    },
    "034": {
      "status": "not-applicable",
      "why": "исполнителей, между которыми делится роль, в проекте нет"
    },
    "035": {
      "status": "active",
      "mechanism": "gate",
      "where": "CONTRACT_VERSION — единственный источник объявленной версии контракта; scripts/version.py — версия проекта СЧИТАЕТСЯ по истории, а не вписывается, и механизм ничего в дерево не пишет; scripts/check_version.py — гейт на рукописное число, видящий и незакоммиченные файлы; tests/test_version.py и tests/test_gates_reject.py проверяют отказом"
    },
    "036": {
      "status": "not-applicable",
      "why": "дорогого окружения у проекта нет: все прогоны идут на одном раннере ubuntu-latest, а внешний взгляд — один и тот же прогон ревью на каждом изменении. Делить аудит на дешёвую и дорогую фазы нечего. Правило вступит вместе со вторым окружением — платным ревьюером или прогоном на матрице систем"
    },
    "037": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md, раздел «Перед толчком» и таблица пробелов — механизм считается работающим по ПРОГОНУ, а не по написанному коду (139), и строка уходит из таблицы только после него. Так и вышло со значком: `badges.yml` был написан задолго до того, как строка ушла. Обратная половина этого правила отвечена под номером 170 — зелёное на подделке тоже гипотеза"
    },
    "038": {
      "status": "active",
      "mechanism": "document",
      "where": "CLAUDE.md — раздел «Имя окна и его окружение»"
    },
    "039": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/*.py — коды возврата 0/1/2 у каждого механизма; tests/test_gates_reject.py прогоняет третий исход отдельно от второго"
    },
    "040": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — пропущенная проверка засчитывается ОТКАЗОМ, а не зелёным: «пропущен, а обязан был идти»; держит tests/test_gates_complete.py::test_skipped_is_rejected. В самом наборе пропусков нет ни одного — ни skip, ни xfail. Два послабления в прогонах названы поимённо: continue-on-error на шагах ревью в .github/workflows/review.yml, и причина записана решением docs/decisions/002-review-taken-from-two-neighbours.md — совещательный канал слияния не держит (084)"
    },
    "041": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_facts.py — витрина публикует НЕСКОЛЬКО честных чисел и ни одного усреднённого: правил всего и отвечено, разбивка по статусам, разбивка по механизмам, состав классов проверок. Значок несёт «отвечено/всего», а не долю и не «здоровье»; в самом модуле записано, почему выбрано это число, а не «сборка зелёная». Держит tests/test_facts.py: числа приходят из источников, а не из собранного файла"
    },
    "042": {
      "status": "active",
      "mechanism": "document",
      "where": "docs/decisions/001-attribution-of-merged-1.md — решение с четырьмя названными отвергнутыми вариантами"
    },
    "043": {
      "status": "active",
      "mechanism": "document",
      "where": "docs/decisions/001-attribution-of-merged-1.md — раздел «Последствия»: пересмотр это новая запись со ссылкой, а не правка"
    },
    "044": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md, раздел «Перед толчком» — утверждение проверяется ОТКРЫТИЕМ предмета, а не памятью: номер правила каталога сверяется файлом, число в прозе — замером с датой. Тот же порядок у решений: docs/decisions/001-attribution-of-merged-1.md и docs/decisions/006-merge-by-squash.md приняты после замера на живом предмете, а не по премисе находки"
    },
    "045": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/agent_pr.py, scripts/debt.py, scripts/check_required_context.py — объявленные исходы разведены с единицей (0/2/3), прогоны разбирают коды списком разрешённого; tests/test_gates_reject.py и tests/test_review_safety.py держат оба конца"
    },
    "046": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gaps_section.py — раздел «Чего в проекте ещё нет» проверяется механизмом: счёт в его строках сверяется с ответом каталогу, а две таблицы пробелов не противоречат друг другу. Проза целиком непроверяема и гейтом не трогается — но у неё есть подкласс, рассыхающийся молча и выглядящий при этом осознанным решением: ЧИСЛО (175, 005). Плюс README.md, раздел «Состояние: брать пока нечего»."
    },
    "047": {
      "status": "active",
      "mechanism": "document",
      "where": "CLAUDE.md — свод читается один раз при старте, правка живому окну не видна"
    },
    "048": {
      "status": "not-applicable",
      "why": "шкал по внешнему индикатору проект не строит: ни одна величина не калибруется по моменту переключения чужого сигнала. Числа здесь считаются по дереву целиком (scripts/build_facts.py), а не по видимой части выборки. Правило вступит, если появится метрика, привязанная к чужому событию — например, к сроку ответа площадки"
    },
    "049": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — состояние берётся опросом проверок на голове изменения, реестра проверок проект не ведёт; scripts/main_red.py — номер попытки прогона спрашивается у площадки, свой счётчик разошёлся бы с ней молча и в сторону бесконечных перезапусков; scripts/unlooked.py — разделы реестра пересобираются из живых артефактов каждым заходом, поэтому позеленевшее уходит само; scripts/check_required_context.py — имя обязательного контекста читается у площадки, а не из памяти"
    },
    "050": {
      "status": "not-applicable",
      "why": "числового потолка нарушений в гейтах нет ни одного: проверки отвечают «есть находка / нет находки», а не «находок не больше N», и подстраивать под нарушение нечего. Единственный порог противоположного рода — доля правил, держащихся гейтом (tests/test_bindings.py), и он пол, а не потолок: движется вверх. Правило вступит вместе с первым гейтом, считающим допуск"
    },
    "051": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/agent-pr.yml и .github/workflows/labels-sync.yml — ненастроенность даёт предупреждение, а не отказ; красным остаётся только третий исход"
    },
    "052": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/automerge.py — базу подтягивает ТОЛЬКО голова очереди и только перед слиянием (sync_head), а за один заход сливается одно изменение: остальные после него всё равно устарели. Держат tests/test_automerge.py::test_only_the_head_pulls_the_base и ::test_one_pass_merges_at_most_one_change. Цена обратного названа замером соседа на 09.09.2026 — 21 холостой прогон против 12 полезных на шести изменениях; след замера — docs/decisions/003-own-automerge-not-native-queue.md"
    },
    "053": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/automerge.py — ступень очереди есть НОМЕР ИСТОЧНИКА РАБОТЫ из контура 1 (docs/behaviour.md): один словарь на оба контура, а не второй порядок для слияния. Источник 0 и 4 приходят меткой, 3 выводится машинно из строки снятия находки, 5 — эвристика по тронутому ответу каталогу и объявлена эвристикой; 1 и 2 не выводятся до обращения к площадке и называются там, где заход их обнаружил — цена правила 052. Пропускная способность («трогает общий с соседом файл») решает ВНУТРИ ступени, а не между ними: приоритетом она не является. Приход решает последним. Держат tests/test_automerge.py::test_the_step_is_the_number_of_the_work_source, ::test_a_finding_repair_outruns_the_plan и ::test_a_shared_file_decides_inside_a_step_not_across_them"
    },
    "054": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/review_findings.py — шаг ревью только записывает сырые строки «НАХОДКА:» в живую задачу и идёт дальше; ничего не чинит и не оценивает. Разбор — отдельным проходом на границе работы: источник 3 в docs/behaviour.md, снятие записи по отпечатку в теле следующего изменения"
    },
    "055": {
      "status": "active",
      "mechanism": "document",
      "where": "docs/decisions/002-review-taken-from-two-neighbours.md — эталон не пишется своей рукой: форма спрашивается у соседей, и у ОБОИХ, потому что ни один из них не полон; таблица различий называет, что взято у каждого. Так же docs/decisions/005-what-of-the-graders-transport-is-transport.md: перед переносом разобрано, что у соседа транспорт, а что его предмет"
    },
    "056": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_reject.py и tests/test_gates_complete.py — у каждого исхода проверяется вторая половина: «записи нет на голове — прогон не стартовал, а не „зелено“», «под учётом нет ни одного файла — ошибка входа, а не „чисто“». Тексты живут в scripts/ci_complete.py, scripts/check_version.py, scripts/check_journal.py, а держит их отказом гейт, а не внимательность"
    },
    "057": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_bindings.py::test_active_names_its_mechanism_and_address — правило, признанное действующим и не держащееся ничем, несёт mechanism=none И причину в why: молча выпасть из ответа оно не может. Живой пример: 163 — вход описан копированием, команды подключения нет. Вторая половина — AGENTS.md, блок критических запретов с пометкой, какие из них машинно не проверяются. Форма совпадает с соседской намеренно (090): у четверых из пяти правило держится тем же приёмом."
    },
    "058": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ghrest.py — исчерпанная квота поднимает RateLimited и на этом останавливается: повтора нет ни одного, а текст отказа говорит «Ждать, а не повторять». Останов происходит ДО нуля — порог QUOTA_FLOOR, потому что на нуле операция уже брошена на середине, а счётчик продолжает расти. Полусделанного состояния после отказа не остаётся: механизмы не пишут ничего, пока запрос не вернулся. Держит tests/test_ghrest.py"
    },
    "059": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_secrets_declared.py — каждый секрет, к которому обращается прогон, назван в таблице AGENTS.md «Секреты: чем держатся и когда истекают»; объявление, пережившее свой секрет, тоже находка — владелец продлевал бы токен, которым никто не пользуется (005). Встроенный `GITHUB_TOKEN` освобождён поимённо и с причиной: его выдаёт площадка на каждый прогон, продлевать некому. Гейт сверяет СОСТАВ; сроки в таблице — знание владельца, из данных они не следуют. Для квоты площадки карта в scripts/ghrest.py: механизм останавливается ДО нуля и называет срок сброса."
    },
    "060": {
      "status": "not-applicable",
      "why": "волн нет, и разбирать между ними нечего"
    },
    "061": {
      "status": "not-applicable",
      "why": "исполнителю, запускающему продукт, задание здесь не выдаётся: продукт проекта — механизмы конвейера"
    },
    "062": {
      "status": "active",
      "mechanism": "document",
      "where": "docs/decisions/002-review-taken-from-two-neighbours.md и scripts/review_findings.py — внешний взгляд заведён как роль, а не как раздел документации: у него свой вопрос (что не так с этим изменением), свой артефакт (записи в живой задаче с отпечатком) и своё ВОЗРАЖЕНИЕ окну, переживающее слияние. Ролей, не прошедших эту приёмку, в проекте нет"
    },
    "063": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/agent_pr.py — автоматическое вмешательство (открыть изменение от лица владельца) включается конъюнкцией: названы репозиторий И ветка, И приставка объявлена, И токен владельца задан, И у коммитов есть связь с задачей. Любое невыполненное условие — «не трогать», и каждое отвечает своим исходом, а не общим отказом. Так же .github/workflows/review.yml: форк пропускается явно, обращение проверяется по author_association"
    },
    "064": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/labels.yml — состав файлом; scripts/labels.py — одно чтение состава на все механизмы; scripts/sync_labels.py приводит к нему площадку; scripts/check_pr_meta.py отвергает изменение без зоны; scripts/agent_pr.py проставляет зоны при открытии — метку больше не ставит человек по памяти; scripts/automerge.py выставляет метку присвоенного источника `source/<номер>` — это СЛЕД, а не вход: очередь решает по вычисленному источнику, и устаревшая метка увести слияние не может. Пишется только при расхождении: заход идёт на каждое событие, и платить запросом за неизменившееся состояние значит тратить квоту на ничто (017)"
    },
    "065": {
      "status": "not-applicable",
      "why": "меток входа для новых участников проект не объявляет (.github/labels.yml — список разрешительный, и такой метки в нём нет), потому что внешних участников у него пока нет: работают владелец и его окна. Заводить метку раньше аудитории значило бы объявить механизм, которого никто не читает (064). Правило вступит вместе с первым сторонним вкладом"
    },
    "066": {
      "status": "not-applicable",
      "why": "атомарной записи подменой файла у проекта нет: механизмы пишут либо в площадку через REST, либо собирают файл целиком одним вызовом write_text. Блокировок нет вовсе — параллельных писателей тоже. Правило вступит вместе с очередью слияний (шаг 8), где голову придётся брать под блокировку"
    },
    "067": {
      "status": "not-applicable",
      "why": "уборки после сбоя ни один механизм не делает: временных предметов они не создают, а созданное — открытое изменение, запись в живой задаче, ветка badges — не мусор, а результат, который переживает отказ намеренно. Ни одного except Exception и ни одного finally в scripts/ нет: отказ поднимается наверх своим родом и там же превращается в третий исход. Правило вступит вместе с шагом 8, где очередь возьмёт блокировку, которую придётся снимать"
    },
    "068": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/labels.yml — список разрешительный; scripts/check_pr_meta.py краснеет на метке, которой файл не объявляет"
    },
    "069": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/agent_pr.py — у тела изменения два писателя, механизм и человек, и механизм не кладёт поверх свой снимок: если отметки MARK в теле нет, значит его правил человек, и sync_description не трогает его вовсе. У тела живой задачи писатель один — scripts/review_findings.py, — и он читает тело, снимает разобранные записи и дописывает новые, а не заменяет содержимое собранным заново"
    },
    "070": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/labels.yml и scripts/labels.py — вывод зоны изменения из тронутых путей по природе догадка, и поле `paths` есть ТОЛЬКО у зон, чьи файлы уже существуют: у остальных его нет намеренно, потому что зона, все пути которой указывают в будущее, дала бы молчаливый пропуск. Остаточный риск назван письменно в самом файле, а настоящий барьер рядом — метку ставит автор, и гейт сверяет её с выведенной (scripts/check_pr_meta.py)"
    },
    "071": {
      "status": "active",
      "mechanism": "document",
      "where": "scripts/review_findings.py — намеренный дубль подписан в самом модуле: раздел «ФОРМАТ СОВПАДАЕТ С СОСЕДСКИМ НАМЕРЕННО» называет, с кем дубль (каталог держит свою реализацию того же механизма), что именно совпадает (строки «НАХОДКА:», «ВЕРДИКТ:», отпечаток, строка снятия) и чем оплачено объединение — переносом записей, когда сосед станет потребителем общего механизма"
    },
    "072": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_ghrest.py — две защиты на один предмет: AST-гейт test_no_mechanism_talks_to_the_platform_directly ловит намерение до запуска (импорт urllib.request мимо транспорта), живой HTTP-сервер в фикстуре ловит последствие после (разбор 404, квоты, неразобранного ответа). Каждая слепа там, где видит другая: гейт не знает, что механизм разберёт отказ неверно, фикстура не знает о механизме, который в неё не заглянул"
    },
    "073": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/ci.yml и labels-sync.yml — pip install с верхними границами: ruff<1, mypy<2, pytest<10, pyyaml<7; .pipeline.yml — диапазон совместимости с контрактом механизмов тоже с ОБЯЗАТЕЛЬНОЙ верхней границей, и scripts/pipeline_checks.py отвергает «>=X.Y» без неё: без верхней границы несовместимая версия применяется молча"
    },
    "074": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/release.py — необратимый шаг проверяется ИНВАРИАНТАМИ ЗАРАНЕЕ: заход разделён надвое, и все условия печатаются списком целиком до того, как что-либо сделано. Отвергается выпуск с грязного дерева, с уже стоящим тегом, без единого фрагмента и с номером не по правилу; мажор требует назвать первого потребителя. .github/workflows/release.yml — кнопка, и только кнопка: событие, выпускающее само, сделало бы выпуском каждое слияние. tests/test_release.py проверяет отказом каждое условие по отдельности"
    },
    "075": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_version.py · check_journal.py · check_pr_meta.py · ci_complete.py · build_changelog.py — отсутствие предмета даёт третий исход, а не «чисто»; tests/test_gates_reject.py прогоняет каждый такой случай"
    },
    "076": {
      "status": "not-applicable",
      "why": "получателя вне дерева у проекта пока нет: все сообщения механизмов адресованы окну и прогону, работающим в этом же репозитории, и путь changelog.d/<слаг>.<род>.md у них действительно есть. Правило вступит вместе с пакетом (шаг 9 в docs/roadmap.md): у ставящего его получателя будет дистрибутив, а не дерево, и подсказка, зовущая в tests/ или .github/, станет ложной"
    },
    "077": {
      "status": "not-applicable",
      "why": "проект одноязычный: строк с ключами и их переводов нет вовсе"
    },
    "078": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — отмена разобрана отдельно от отказа: погашенные группой отмены записи отбрасываются при наличии живой, но если отменены ВСЕ записи имени, это отказ, а не «зелено»; tests/test_gates_complete.py держит оба конца"
    },
    "079": {
      "status": "not-applicable",
      "why": "срока хранения записей проект нигде не задаёт: находки живут в задаче до снятия отпечатком, фрагменты журнала — до выпуска, факты витрины переписываются каждым слиянием. Очереди, из которой что-то выметалось бы по времени, нет. Реестр слитого без внешнего взгляда (scripts/unlooked.py) заведён 10.09 и уборки по времени тоже не имеет НАМЕРЕННО: запись, устаревающая сама, — это ровно то исчезновение, против которого реестр и написан. Запись становится терминальной иначе — появился вердикт либо задачу закрыл человек, — а роста без предела ограничивать нечем и незачем: он равен объёму работы, прошедшей мимо взгляда. Правило вступит вместе с хранилищем прогонов, если оно появится"
    },
    "080": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".rules/proposals.json — правила, родившиеся здесь, уезжают в каталог ФАЙЛОМ, который каталог опрашивает сам, а не памятью окна; tests/test_bindings.py держит форму: все поля контракта, инцидент с конкретикой и след, разрешимый в этом дереве (044). scripts/drift.py, источник «вердикты по предложениям» — принятое каталогом перестаёт быть предложением: у него появился номер, и отвечают по нему в bindings, а не в очереди на приём; отвергнутое несёт причину. Вердикт выносит каталог у себя и по своему расписанию, события об этом не приходит — потому это дрейф. Приём взят у грейдера (162)."
    },
    "081": {
      "status": "not-applicable",
      "why": "чужой код проект не исполняет; действия каталога подключаются ссылкой на версию, а не копией"
    },
    "082": {
      "status": "not-applicable",
      "why": "состава ролей у проекта нет: исполнителей двое — владелец и его окно, — плюс внешний взгляд прогоном. Пласты продукта названы зонами меток (.github/labels.yml), но владельца у зоны нет и назначать его некому. Правило вступит, когда над проектом будет работать больше одного человека; до тех пор отдельная роль на пласт была бы записью без исполнителя"
    },
    "083": {
      "status": "not-applicable",
      "why": "недетерминированного компонента в механизмах нет: вывод ревьюера читается строками, и его вердикт — вход, а не предмет проверки"
    },
    "084": {
      "status": "active",
      "mechanism": "gate",
      "where": ".pipeline.yml — класс проверки объявлен данными: у review и debt он advisory, и красное там слияния не держит; scripts/ci_complete.py выносит вердикт по обязательным, а совещательное красное помечает и печатает с адресатом. Тот же приём у открытия изменения: scripts/agent_pr.py без MERGE_QUEUE_TOKEN отдаёт третий исход, и .github/workflows/agent-pr.yml разбирает его как «не настроено», а не как поломку изменения — иначе краснела бы каждая ветка ни за что"
    },
    "085": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/review.yml — промпт объявляет проверяемый текст данными, а не указаниями, и список инструментов агента закрыт; tests/test_review_safety.py запрещает голый Bash и pull_request_target"
    },
    "086": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/findings.py — WEIGHTS: шкала закрытая, три градации, как роды у фрагментов журнала; вес живёт в самой записи живой задачи и читается строкой (ENTRY_RE). scripts/review_findings.py — вес берётся из строки находки, слово вне шкалы весом не считается и к ближайшему НЕ приводится, а неназванный записывается как «без веса»: подстановка самого лёгкого решила бы за ревьюера в сторону, удобную разбирающему (154). Порядок записей — по весу, а не по приходу (053). Вторая половина правила держалась и раньше: тяжесть ставит не тот, кто нашёл, — ревью записывает, разбирает и отклоняет окно, и отклонение оставляет след (026). Держат tests/test_review_findings.py::test_a_finding_without_a_weight_says_so, ::test_a_weight_outside_the_scale_is_not_a_weight и ::test_the_heaviest_finding_is_written_first"
    },
    "087": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/review_findings.py — повторный проход не переоткрывает найденное: у каждой записи отпечаток от заголовка, повтор в тело задачи не дописывается, а снятая запись уносится строкой «Разобрано: <отпечаток>» в теле изменения. Держит tests/test_review_findings.py. Вторая половина правила — подавать ревьюеру прошлые находки на ВХОД — пока не сделана: он их не видит, и защищает от переоткрытия только отпечаток"
    },
    "088": {
      "status": "not-applicable",
      "why": "фаз с отдельным критиком метода у проекта нет: работа идёт задачами, а не фазами аудита, и проверяющего, отвечающего «что не так с тем, КАК мы проверяли», заводить не над чем. Ближайшее к этому — разбор правил каталога, где каждое несделанное названо причиной, а не отсутствием. Правило вступит вместе с первым многофазным аудитом"
    },
    "089": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/badges.yml и scripts/build_facts.py — витрина собирается ИЗ источника и толкается в ветку-сироту badges; обратного хода нет. README.md называет адрес витрины потребителю (соседу, каталогу), а читателя дерева за содержимым туда не отправляет: состав классов проверок он берёт из .pipeline.yml, ответы каталогу — из .rules/bindings.json"
    },
    "090": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/ci.yml — джоб attribution подключён действием каталога, а не переписан здесь; scripts/ghrest.py — общий транспорт вместо пяти копий, и tests/test_ghrest.py краснеет на появлении шестой"
    },
    "091": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/debt.py — шаг долга печатает источники в порядке контура 1: конфликт на своём изменении (1) и красное на нём (2) идут ПЕРВЫМИ, затем находки (3), затем правила (5). До этого источники 1 и 2 механизма не имели вовсе: открытое красное видел только тот, кто откроет список изменений глазами. Тревога о застрявшем остаётся предметом службы (docs/decisions/004-schedules-stay-service-observes.md) — здесь читается СВОЁ состояние, а это порядок работ, а не тревога. Плюс AGENTS.md и docs/behaviour.md — контур 1."
    },
    "092": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/review_findings.py — находки внешнего взгляда живут в живой задаче-адресате, переживающей слияние, и у каждой есть вес и отпечаток; scripts/debt.py печатает порядок их разбора вместе с остальными источниками контура 1 (091), а сам порядок объявлен в docs/behaviour.md. «Что не так» и «что за чем» разведены намеренно: первое — данные механизма, второе — договор, и смешавшись, они перестают отвечать на оба вопроса (022)."
    },
    "093": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md — обобщение по третьему случаю названо в разделе про каталог"
    },
    "094": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/agent-pr.yml — приставка ветки теперь одна: `agent/**`. Вторая, `claude/**`, стояла переходной заглушкой (облачному окну имя ветки назначает платформа) и снята после того, как новый путь подтверждён прогонами, а не когда был написан. Возврата заглушки не будет тихого: tests/test_gates_contract.py::test_branch_prefixes_match_the_workflow сверяет список прогона со списком в scripts/agent_pr.py — разъехавшись, они дали бы худший из отказов, когда прогон стартует, а изменение не открывается и красного нигде нет"
    },
    "095": {
      "status": "not-applicable",
      "why": "сбора данных о пользователе у проекта нет ни одного: механизмы читают дерево и площадку, телеметрии не отправляют, и умолчания, выгодного продукту в ущерб пользователю, взяться неоткуда. Правило вступит, если у общих механизмов появится необязательная отправка замеров наружу"
    },
    "096": {
      "status": "not-applicable",
      "why": "своего хранилища данных у проекта нет: состояние живёт в трекере площадки и в дереве"
    },
    "097": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_reject.py — ложное «прошло»: гейт обязан отвергнуть подделанный вход; tests/test_gates_complete.py — ложное «не прошло»: здоровый вход обязан пройти. Обе ошибки заведены отдельными тестами, а не одной проверкой «работает»"
    },
    "098": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/pipeline_checks.py и .pipeline.yml — единица ответа проекта о проверках взята по УПОТРЕБЛЕНИЮ: один ответ на ДЖОБ, потому что именно имя джоба выбирает защита ветки, а не на прогон и не на шаг. Держит tests/test_gates_contract.py::test_job_name_equals_context_name; матричные имена в обязательные не попадают никогда (test_summary_is_not_a_matrix) — там одна запись даёт много имён, и единица распадается"
    },
    "099": {
      "status": "active",
      "mechanism": "document",
      "where": ".rules/bindings.json, поле «_зачем» — конфликт «где живёт ответ по правилу: у каталога или у проекта» разрешён по ПОСЛЕДСТВИЮ, а не по правильности: у каталога раздел без ответов остаётся осмысленным, у проекта без них пустеет предмет — здесь живёт механизм, и одно правило в разных проектах держится по-разному. Так же разведены docs/pipeline.md и docs/behaviour.md: состав и поведение"
    },
    "100": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — дедлайна два, и они разной длины: отсутствие записи проверки на голове означает «прогон не стартовал» и отвергается СРАЗУ, не дожидаясь предела ожидания, а предел (--timeout) относится только к тем, кто уже идёт. Третий, на работу джоба, задан в .github/workflows/ci.yml полем timeout-minutes у каждого шага"
    },
    "101": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ghrest.py — постоянный отказ отделён от временного и не повторяется: 404 — свой род, 401/403 без заголовков квоты — «причина другая», а не лимит. Исчерпанной квоте механизм не назначает своей паузы: срок берётся из retry-after либо x-ratelimit-reset площадки. Держат tests/test_ghrest.py: test_exhausted_quota_is_its_own_kind, test_forbidden_without_quota_headers_is_not_rate_limit, test_retry_after_alone_is_enough_for_rate_limit"
    },
    "102": {
      "status": "not-applicable",
      "why": "сравнений с послаблениями проект не делает: гейты сверяют имена джобов, статусы и адреса точно, и «почти совпало» здесь не бывает — расхождение и есть предмет проекта. Правило живёт там, где сравнивают вывод решения с эталоном; у соседа-грейдера такой шаг есть, здесь его нет и не планируется"
    },
    "103": {
      "status": "not-applicable",
      "why": "сторожа побочных эффектов у проекта нет: ни один механизм не сравнивает снимки «до» и «после» и никому ничего не приписывает. Механизмы читают площадку и дерево, а пишут только туда, куда их позвали. Правило вступит вместе со сторожем, который начнёт следить за изменениями состояния во время прогона"
    },
    "104": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows — ручная кнопка `workflow_dispatch` есть у каждого прогона, кроме claude.yml, который отвечает на обращение и запускается им: agent-pr.yml · automerge.yml · badges.yml · ci.yml · labels-sync.yml · main-red.yml · required-context.yml · review.yml · rules-inbox.yml · task-items.yml. У кнопки ревью и разбора задачи есть ВХОД — номер уже слитого изменения: кнопка без предмета смотрела бы в текущую ветку и ревью не заменяла"
    },
    "105": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/review.yml — внешний взгляд ведёт отдельный прогон с закрытым списком инструментов и без истории окна, а не второй заход того же окна по своему же коду. Разбор образцов и что взято у каждого соседа — docs/decisions/002-review-taken-from-two-neighbours.md. Слепое пятно этого ответа названо: прогнать проверки ревью пока не может (#33), и потому видит код, а не поведение"
    },
    "106": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md, таблица «чего в проекте ещё нет» — строка уходит оттуда после НАСТОЯЩЕГО прогона, а не после написания кода (139), и до тех пор пробел назван, а не выровнен (046). README.md показывает наружу ровно то, что прогон подтвердил: значок появился на витрине после первого прогона публикации, а не вместе с `badges.yml`"
    },
    "107": {
      "status": "active",
      "mechanism": "document",
      "where": "docs/decisions/005-what-of-the-graders-transport-is-transport.md — чужой механизм разобран на то, что у соседа транспорт, и то, что его собственный предмет: перенос вслепую дал бы «у автора работает» на нашей выборке. Так же docs/decisions/002: форма спрошена у ОБОИХ соседей, потому что каждый проверен только на том, что было у него"
    },
    "108": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_changelog.py — обе половины правила. Вышедшее переезжает ДОСЛОВНО: при выпуске фрагменты уходят в changelog.d/released/<версия>/ и остаются источником, а не сокращаются. Предел теперь есть и стоит у ПРЕДСТАВЛЕНИЯ, а не у источника: развёрнутыми собираются UNFOLDED_RELEASES последних выпусков, остальные названы ссылкой на свой каталог со счётом записей — оборвать раздел молча значило бы читаться как «раньше ничего не было». Держат tests/test_gates_reject.py::test_the_journal_unfolds_a_bounded_number_of_releases и ::test_a_short_history_folds_nothing"
    },
    "109": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_states.py — таблицы состояний трёх контуров в docs/behaviour.md разбираются механизмом: у каждого состояния колонка выходов непуста, состояние без выходов названо терминальным СЛОВОМ, а имя после стрелки обязано быть состоянием той же таблицы. Последнее и есть предмет правила: выход в несуществующее состояние выглядит описанным переходом и им не является. Переход в другой контур назван его словом и проверяется чтением — это объявленная граница гейта. Подтверждён двумя отказами (140): снятое слово «терминальное» и переименованное состояние в выходе."
    },
    "110": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/agent_pr.py — до первого обращения к площадке разобран вход и проверено всё, что может отказать: названы ли репозиторий и ветка, объявлена ли приставка ветки, есть ли токен владельца. Отсутствие токена даёт свой исход (EXIT_NOT_CONFIGURED) ДО открытия изменения, а не после. Тот же порядок у scripts/build_facts.py и scripts/check_version.py: пустой источник ловится до сборки. Держит tests/test_gates_reject.py"
    },
    "111": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/agent-pr.yml — конвейер сам открывает изменение и сам проставляет зоны, вместо того чтобы советовать окну сделать это; tests/test_ghrest.py держит запрет на вторую реализацию транспорта и чтения состава"
    },
    "112": {
      "status": "not-applicable",
      "why": "накоплений у получателя проект не заводит: механизмы пишут в дерево изменения, в задачу площадки и в ветку badges — всё это переписывается следующим прогоном и удаляется теми же средствами площадки. Файла на чужой машине, о котором пользователь не просил, нет ни одного. Правило вступит вместе с пакетом (шаг 9 в docs/roadmap.md): кеш и конфиг, заведённые установкой, обязаны сниматься командой"
    },
    "113": {
      "status": "active",
      "mechanism": "gate",
      "where": "docs/release.md — правила эволюции контракта записаны ДО первого потребителя: что считается несовместимым, как идёт миграция, чем отвечает потребитель. scripts/check_contract.py — гейт: правка поверхности без объявления фрагментом рода `contract` не проходит; scripts/contract.py снимает поверхность с дерева, а не решает её на глаз; .pipeline.yml несёт диапазон совместимости с обязательной верхней границей. tests/test_contract_surface.py проверяет обе стороны отказом"
    },
    "114": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_contract.py — правка поверхности делится на совместимую и НЕСОВМЕСТИМУЮ (scripts/contract.py::breaking: удалённый джоб, удалённый прогон, смена схемы ответа, снятая проверка). У несовместимой фрагмент рода contract обязан назвать ПЕРЕХОД — что было и что стало, — а не только факт правки: потребитель живёт на своей версии, и миграция идёт от неё, а не от нуля. Имя джоба он держит в защите ветки дословно, и удалённое превращает её в вечное ожидание контекста, которого никто не выдаст. От совместимого расширения переход не требуется — иначе миграция объявлялась бы на каждый новый джоб и писалась формально (051). Вторая половина — docs/release.md, раздел «Несовместимое изменение»: порядок из четырёх шагов и запрет механического поднятия числа (157)."
    },
    "115": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/paths.py — адреса настроек объявлены одним модулем, остальные механизмы их импортируют; tests/test_settings_anchor.py держит обе половины правила: ни один механизм не строит адрес настройки сам (второй якорь заводится именно так) и ни один не ищет настройку вверх по дереву — поиск нашёл бы файл соседнего проекта и молча принял бы его за свой. Раньше ответ был «держится построением»: замер 09.09.2026 показал у CONTRACT_VERSION три независимых адреса — в build_changelog.py, build_facts.py и check_version.py"
    },
    "116": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — сборщик результатов параллельной работы сверяет КОЛИЧЕСТВО: у каждого объявленного имени обязана быть запись на голове, и отсутствие записи означает «прогон не стартовал», а не «зелено»; пустой список записей — отказ входа. Сам сборщик проверяется наравне с работой: tests/test_gates_complete.py держит его вердикт, tests/test_gates_contract.py — то, что он собран опросом, а не через needs (иначе отказ соседа стал бы пропуском)"
    },
    "117": {
      "status": "not-applicable",
      "why": "заданий исполнителям проект не выдаёт — числовые границы задавать некому"
    },
    "118": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_changelog.py — исходник не исчезает в производном: при выпуске фрагменты ПЕРЕЕЗЖАЮТ из changelog.d/ в changelog.d/released/<версия>/ и остаются источником, а CHANGELOG.md пересобирается из них локально, без похода к площадке и без чтения тегов. Держит tests/test_gates_reject.py. Правило 125 отвечено тем же адресом с другой стороны — производное не хранилище"
    },
    "119": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/journal.py — маска входа PATH_RE берёт ровно changelog.d/<слаг>.<род>.md и НЕ рекурсивна, поэтому производное самого инструмента под неё не попадает: переехавшие при выпуске фрагменты лежат в changelog.d/released/<версия>/, собранный CHANGELOG.md — в корне и в EXEMPT_FILES гейта, а facts.json и значок вообще живут в ветке badges, вне дерева. Держит tests/test_gates_reject.py"
    },
    "120": {
      "status": "not-applicable",
      "why": "проект ведёт механизмы, а не каталог правил: каталог — соседний репозиторий, и правила его ведения относятся к нему"
    },
    "121": {
      "status": "active",
      "mechanism": "document",
      "where": "CLAUDE.md — окно эфемерно, и его закрытие ничего не закрывает: работа считается сделанной по слитому изменению и закрытой задаче, а состояние между окнами берётся из трекера, а не из пересказа"
    },
    "122": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "шаг badges — facts.json отдаёт числа сырыми, значок рядом показывает то же число отформатированным; scripts/build_facts.py собирает оба из одного источника"
    },
    "123": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/ci.yml — джоб attribution с baseline cb26ec4; .github/authors.txt — список согласованных имён"
    },
    "124": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/main_red.py — упал ровно один обязательный джоб: перезапускается он один и ОДИН раз, номер попытки спрашивается у площадки (`run_attempt`), а не хранится, поэтому бесконечный повтор невозможен по построению; зелёное со второго раза записывается находкой о мигании в живую задачу, а не считается починкой. .github/workflows/main-red.yml — право `actions: write` объявлено ровно здесь и только здесь. tests/test_main_red.py проверяет отказом: вторая попытка, несколько красных сразу и неразобранный адрес записи дают три РАЗНЫЕ причины не перезапускать"
    },
    "125": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_changelog.py --check — CHANGELOG.md производный, собирается из changelog.d/ и changelog.d/released/, а не хранит выпущенное сам"
    },
    "126": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/automerge.yml и scripts/automerge.py::wait_for_head — доказательство «условие снято» принимается от ДВУХ равносильных источников. Первый: событие `workflow_run` от прогона гейтов — приходит и с общей ветки, и с ветки изменения (замер 10.09.2026: за завершением ci на ветке заход очереди следовал 25 раз из 25, медиана задержки 2 с). Второй: опрос внутри захода — увидев «проверки идут», очередь ждёт голову до объявленного предела, а не выходит в надежде на следующее событие. Второй нужен потому, что заход ГИБНЕТ В ОЖИДАНИИ: группа держит один ожидающий, и новый его вытесняет — 45 из 120 заходов умерли, не начав работу; выживший приходит от прогона, завершившегося последним, и не обязан застать голову зелёной. Предел назван числом (WAIT_TRIES × WAIT_STEP) и короче предела джоба; исчерпав его, заход говорит «ещё идут» и уходит — тогда его добудит событие или кнопка (104)."
    },
    "127": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_version.py — маркер с чужим значением краснеет; docs/release.md — раздел «Маркер в прозе»"
    },
    "128": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_pr_meta.py — разметка проверяется на полноту: зона выводится из путей состава и обязана стоять, а не просто «метка есть»"
    },
    "129": {
      "status": "active",
      "mechanism": "gate",
      "where": ".rules/bindings.json — ответ по каждой записи каталога; scripts/pipeline_checks.py повторяет ту же форму для классов проверок; tests/test_bindings.py держит полноту ответа"
    },
    "130": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/rules-inbox.yml — новое правило каталога приходит не в чужой репозиторий, а ЗАДАЧЕЙ в трекер этого проекта, вместе с тремя числами долга по 177: без ответа, действующих без механизма, и разошедшийся контракт. Вторая половина правила — подбирать к новому правилу кандидатов из открытых задач — пока не сделана: список кандидатов проходная не собирает, и связывает правило с предметом окно"
    },
    "131": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/agent-pr.yml — изменение открывает конвейер токеном владельца; scripts/agent_pr.py не переходит на токен прогона даже при отсутствии секрета"
    },
    "132": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md — раздел «Ветка, изменение, коммит»"
    },
    "133": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md — граница изменения задаётся пересечением файлов"
    },
    "134": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md и CLAUDE.md — свод существует до того, как в проект придёт следующее окно"
    },
    "135": {
      "status": "active",
      "mechanism": "document",
      "where": "CLAUDE.md — раздел «Подпись и открытие изменения»: личность узнаётся записью, после первого изменения смотрят автора в общей ветке"
    },
    "136": {
      "status": "active",
      "mechanism": "document",
      "where": "docs/roadmap.md, шаг 1.1a — вердикт выносится после перебора ВСЕХ утверждений договора, а не первого найденного; тем же порядком идёт разбор правил каталога: ответ даётся по каждой записи"
    },
    "137": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_facts.py — пустой источник ловится на СЫРОМ содержимом файла до всякой сборки: «пуст — это ошибка входа, а не „версии нет“». Тот же порядок в scripts/check_version.py и scripts/labels.py. Числа витрины считаются по источнику (.rules/bindings.json, .pipeline.yml), а не по собранному facts.json, — иначе пустой разбор дал бы правдоподобный ноль. Держит tests/test_facts.py и tests/test_gates_reject.py"
    },
    "138": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_journal.py — фрагмент журнала едет ВМЕСТЕ с изменением, а не пишется при выпуске по памяти; changelog.d/README.md — форма записи; scripts/release.py собирает журнал из накопленных фрагментов, а не из воспоминаний. tests/test_gates_reject.py проверяет отказом изменение без фрагмента"
    },
    "139": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_bindings_addresses.py::test_a_gate_names_something_runnable — ответ «держится гейтом» обязан назвать адрес ИСПОЛНЯЕМОГО (.py, .yml, .yaml), а не документа: документ прогоном не подтверждается вовсе, он не запускается, и ответ, называющий гейтом абзац в своде, обещает проверку, которой нет. Список видов закрытый — «похоже на путь» приняло бы и документ. Вторая половина — tests/test_gaps_section.py: строка уходит из раздела пробелов, когда механизм подтверждён прогоном, а счёт в ней сверяется с ответом каталогу. Форма взята у каталога, где список видов тоже закрытый (162)."
    },
    "140": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_reject.py — 21 проверка на то, что механизмы обязаны отвергнуть; tests/test_gates_complete.py — вердикт сводного гейта на пропуске, отмене и отсутствии записи"
    },
    "141": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/findings.py — живая задача находится по целой строке-маркеру, а не по началу заголовка. Имена джобов сверяются равенством, и это держит tests/test_gates_contract.py::test_job_name_equals_context_name: имя джоба и имя контекста совпадают целиком, иначе в защите ветки окажется имя-призрак. Четыре места, где совпадение по началу нужно НАМЕРЕННО, названы этим в самом имени константы: PREFIXES в scripts/agent_pr.py, ZONE_PREFIX в scripts/labels.py, ALLOWED_PREFIXES в scripts/check_version.py, EXEMPT_PREFIXES в scripts/check_journal.py"
    },
    "142": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/review_findings.py — находки внешнего взгляда уезжают в живую задачу-адресата и снимаются строкой «Разобрано: <отпечаток>» в теле изменения; scripts/unlooked.py — сам факт отсутствия взгляда; scripts/main_red.py — краснота общей ветки, разведённая по классу проверки; .pipeline.yml — у КАЖДОЙ совещательной проверки объявлено поле `addressee` с разрешимым адресом, и scripts/pipeline_checks.py отвергает ответ без него: красное, которому негде пережить слияние, делает совещательную проверку вежливо выключенной. tests/test_pipeline_checks.py проверяет это отказом"
    },
    "144": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/changerefs.py — разбор прозы идёт по СТРУКТУРЕ разметки, а не по пунктуации: границей служат строка и забор кодового блока, а точка внутри имён (`v1.2.0`, `re.finditer`, `SECURITY.md`) ничего не режет. Держит tests/test_changerefs.py — три находки внешнего взгляда подряд пришли именно отсюда: непарная кавычка, незакрытый забор и два незакрытых забора в разных коммитах"
    },
    "145": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_complete.py и tests/test_gates_reject.py — прогнан каждый объявленный исход, включая третий"
    },
    "146": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_platform_shape.py — подделки ответа площадки сверяются со СНЯТЫМ ответом (tests/fixtures/check-runs.shape.json), а не с разумением окна: зелёный набор на подделке, собранной по памяти, подтверждает согласованность кода с нашим представлением о площадке, а не с площадкой. Снимок датирован, и что он НЕ ловит — изменение самой площадки — названо в докстроке, а не выдано за полноту"
    },
    "147": {
      "status": "active",
      "mechanism": "gate",
      "where": "Переключатель есть, и у отмены есть действие, а не только смысл. Согласие `automerge` ставит МЕХАНИЗМ при открытии (scripts/agent_pr.py): ветка с приставкой конвейера и есть заявленное согласие. Отмена выражается меткой `hold`, и она сильнее — шаг открытия, увидев её, согласие не ставит, а стоящее СНИМАЕТ: «метку ещё не ставили» и «поставили и сняли» по состоянию изменения неразличимы, и снятое человеком согласие вернулось бы следующим толчком. Очередь помеченное не двигает и печатает его на каждом заходе (scripts/automerge.py). Держат tests/test_agent_pr_consent.py. Чего нет: разбудить по стоящему НЕДЕЛЯМИ `hold` некому — это шаг 11, и пробел назван в AGENTS.md"
    },
    "148": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — состояние читается по ГОЛОВЕ: check-runs на коммите, а не поля записи изменения; пустой список записей означает «прогон не стартовал», а не «зелено». Отставшая ветка лечится сменой головы — подтянуть общую и дождаться прогона, — а не повтором проверки; тот же порядок у scripts/agent_pr.py, который сверяет описание с коммитами ветки, а не с телом записи"
    },
    "149": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/ — общая площадка забирается набором, а не вносится в исключения: файлы механизмов собираются в tmp_path (tests/test_facts.py, tests/test_gates_reject.py), а транспорт проверяется на СВОЁМ HTTP-сервере, поднятом на случайном порту (tests/test_ghrest.py), а не на площадке и не на фиксированном порту. Ни одного исключения по имени в наборе нет"
    },
    "150": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_reject.py — тридцать две проверки зовут САМ механизм и сверяют его вердикт и код возврата, а не пересказывают условие: гейты журнала, версии и состава проверок отвечают отказом на подделанный вход. Условие механизма в утверждении теста не повторяется нигде — иначе тест краснел бы на оговорке, которой механизм не применяет"
    },
    "151": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/review.yml — второй исполнитель починки добывает след САМ: джобу ревью поставлены интерпретатор и те же версии проверок, что в гейтах изменения, а список разрешённого называет оба имени интерпретатора — разрешение выдаётся по началу команды, и `python3` там другая строка. Держат tests/test_review_safety.py::test_a_permitted_run_is_actually_possible (что список разрешает запускать, то прогон обязан поставить) и ::test_both_interpreter_names_are_permitted. Подсказка требует назвать исход прогона словами — «проверки прогнаны» или «прогнать не удалось, вот почему»: вердикт по прогону и вердикт по чтению стоят разного, а выглядят одинаково (045). Подтверждено прогонами: отчёты ревью на изменениях этой смены называют число прогнанных проверок. Чего по-прежнему нет: собственный лог прогона площадки ревьюеру не отдаётся — он воспроизводит отказ запуском, а не читает чужой след, и на отказе, не воспроизводимом локально, останется без него"
    },
    "152": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_review_safety.py::test_a_shared_caller_pins_what_it_calls — закрепление требуется ровно там, где вызывающий берётся с ОБЩЕЙ ветки: `workflow_run`, `pull_request_target`, `schedule`. На `pull_request` файл прогона берётся из самого изменения, и закрепление там ничего не добавляет — гейт этого и не требует. .github/workflows/automerge.yml · main-red.yml · rules-inbox.yml закреплены по SHA; тег действия каталога — не подвижная метка, а версия контракта, к которой проект прибит сознательно, и это объявлено списком с причиной. ЗАМЕР 10.09.2026: правило держалось в прогоне ревью, где о нём помнили, и НЕ держалось в очереди и тревоге, где оно как раз и работает"
    },
    "153": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md — правило названо прямо; все документы ссылаются на записи каталога и не копируют их разбор"
    },
    "154": {
      "status": "active",
      "mechanism": "gate",
      "where": "changelog.d/README.md — род internal обязан назвать причину, и scripts/check_journal.py требует фрагмент; .github/labels.yml — раздел «чего здесь нет намеренно» с причиной у каждой невзятой метки"
    },
    "155": {
      "status": "active",
      "mechanism": "document",
      "where": "README.md — проект свой первый потребитель: механизм, которым не пользуется автор, находит дыры на чужом проекте"
    },
    "156": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/ci.yml — джоб attribution проверяет хвостовой блок трейлеров"
    },
    "157": {
      "status": "active",
      "mechanism": "gate",
      "where": ".pipeline.yml — потребитель объявляет диапазон совместимости `contract: \">=X.Y,<A.B\"`; scripts/pipeline_checks.py отвергает ответ без диапазона и контракт, вышедший за его границу, — сообщение зовёт ПЕРЕЧИТАТЬ ответы, а не подвинуть число: механическое поднятие оставляет ненужный обход жить вечно. scripts/family.py объявляет версию чужой сводки, под которую написан разрез, и её расхождение печатается рядом с числами. docs/release.md — порядок перечитывания"
    },
    "158": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ghrest.py — адрес прикрепляется в точке обращения: каждый отказ транспорта несёт метод и путь («GET repos/… → 404: …»), а не только код. Так же у гейтов: scripts/build_facts.py и scripts/check_version.py называют файл, которого не хватило, scripts/ci_complete.py — имя проверки и голову. Третий исход отделён от второго во всех механизмах и держится tests/test_gates_reject.py"
    },
    "159": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — verdict() копит находки в список по всем именам и читает его ПОСЛЕ перебора; раннего выхода нет ни одного. Тот же порядок в scripts/check_pipeline.py: findings() возвращает список целиком, и код возврата выбирается по нему, а не по первому найденному расхождению. Держит tests/test_gates_complete.py::test_both_failures_are_named — два отказа обязаны быть названы ОБА, а не «хотя бы один»"
    },
    "160": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/badges.yml — значок и facts.json пересобираются на каждое слияние, то есть чаще, чем идут изменения, и в дереве общей ветки не лежат: прогон кладёт их в ветку-сироту badges силовым толчком. Иначе каждое слияние конфликтовало бы на одном и том же файле, а сдвиг числа красил бы ветку как поломка"
    },
    "161": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_decisions_format.py — каждая запись решения обязана назвать ОТВЕРГНУТУЮ альтернативу; без неё это ход работы, и место ему в журнале, а не в истории. Форму записи держит тот же гейт: номер в заголовке, читатель, статус с датой и задачей. Ходы работы живут отдельно — фрагментами в changelog.d/"
    },
    "162": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/rules-inbox.yml — ночной прогон каталога приносит в задачу-«входящие» раздел «У соседей это уже решено»: по каждому правилу, признанному действующим и не держащемуся ничем, названы соседи по своду и АДРЕС их механизма. Вторая ось — scripts/family.py: разрез по сводке каталога показывает, что соседи держат машиной там, где мы отвечаем документом; из него вырос список переноса (#117). Спрашивается сводка каталога, а не поход по чужим репозиториям (001). Прежний ответ говорил «документ» и отстал от дерева: механизм стоял, ответ его не называл."
    },
    "163": {
      "status": "active",
      "mechanism": "none",
      "why": "проект отдаёт наружу именно механизмы, но вход (README.md) описывает их пока копированием: команды подключения нет, потому что вызов шага через workflow_call — шаг 12 в docs/roadmap.md, а первый потребитель ещё не подключён. Запасной путь при этом назван, а не спрятан: docs/roadmap.md держит порядок подключения и признаки готовности каждого этапа. Механизма нет — есть расписание"
    },
    "164": {
      "status": "active",
      "mechanism": "gate",
      "where": "CONTRACT_VERSION — версия КОНТРАКТА, а не проекта; scripts/version.py считает версию проекта отдельно (MAJOR.MINOR из релизного тега, PATCH — число принятых изменений), и оба числа публикуются фактом рядом: scripts/build_facts.py. Свести их в одно значило бы либо скрыть работу, либо объявить выпуском каждое изменение. scripts/check_version.py — гейт на рукописное число; docs/release.md — что именно версионируется"
    },
    "165": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_source_hygiene.py — списки путей из git читаются по NUL: гейт требует -z у каждого вызова с --name-only, --name-status, ls-files и diff-tree, и в коде (scripts/agent_pr.py, scripts/check_journal.py, scripts/check_version.py), и прямо в шаге прогона (.github/workflows/ci.yml). Список передаётся в scripts/check_pr_meta.py ФАЙЛОМ (--files-from), а не подстановкой: оболочка вырезает NUL. Охват печатают все три проверки — сколько путей прочитано и сколько пропущено"
    },
    "166": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/changerefs.py — связь изменения с задачей ищется как СВЯЗЬ, а не как подстрока: кодовые вставки и заборы вырезаются построчно, поэтому «Closes #12» в цитате подсказки гейта связью не считается. Держит tests/test_changerefs.py; сам разбор появился из находки — гейт зеленел на цитате в описании, то есть ровно там, где отношения нет"
    },
    "167": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_source_hygiene.py::test_shell_variable_names_are_ascii — имя переменной оболочки в шагах прогона обязано быть ASCII, хотя проект ведётся на русском. Опаснее не падение кодом 127, а тихий случай: переменная окружения с не-ASCII именем создаётся штатно, подстановка выходит пустой, и условие на ней всегда идёт одной веткой при зелёном гейте"
    },
    "168": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/ci.yml — ci-complete собран опросом, без needs; scripts/ci_complete.py; tests/test_gates_contract.py проверяет отсутствие needs и полноту опроса"
    },
    "169": {
      "status": "active",
      "mechanism": "gate",
      "where": ".rules/schedules.json и tests/test_schedules.py — роль каждого расписания объявлена данными (main либо safety-net), рядом названы цена в вызовах и причина именно этого часа. Гейт не ищет слова: сверяет объявленный cron с деревом, ловит незаявленное расписание и объявление, пережившее своё расписание, и считает арифметику — укладывается ли худший час в объявленную долю часового лимита (квота общая, 058). У страховки обязателен замер срабатываний: пока его нет, основной путь строят так, будто страховки нет. Форма взята у каталога целиком (162). Прежний ответ — docs/decisions/004-schedules-stay-service-observes.md — остаётся в силе: он про то, ЧТО уезжает в службу, а здесь про то, что осталось."
    },
    "170": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/fixtures/check-runs.shape.json — снятый ответ площадки с датой и адресом, откуда снят; tests/test_platform_shape.py сверяет с ним подделки набора: ключ, которого площадка не отдаёт, и тип, которого у неё не бывает, роняют гейт. Подделки берутся вызовом, а не списком руками — список разошёлся бы с самой подделкой. Чего гейт не ловит, названо прямо: изменение самой площадки — снимок стареет и обновляется рукой, и заменой живому прогону он не является (046)"
    },
    "171": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py и scripts/pipeline_checks.py — состав обязательных проверок читается из .pipeline.yml ДЕРЕВА самого изменения (--policy .pipeline.yml на выкаченной голове), а не с общей ветки. Иначе изменение, которое сам состав и меняет, стало бы неразрешимым: чтобы эталон обновился, оно должно уехать в main, а уехать не может — проверка красная. Держит tests/test_gates_contract.py::test_summary_takes_its_subject_from_data"
    },
    "172": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_own_name.py — перепись имён репозиториев по отслеживаемым файлам против канона площадки. Своё имя сверяется с GITHUB_REPOSITORY (это full_name, точный по регистру); переименованное ловится ОТВЕТОМ площадки, а не сравнением имён: старое имя не похоже на новое, и площадка держит по нему редирект. Локальный заход регистр не сверяет и говорит об этом: origin хранит написание клонировавшего. Приём взят у соседа, у которого правило родилось: после трёх переименований 28 ссылок устарели при восьми зелёных гейтах (162). Вторая половина — docs/release.md, раздел «Переходная заглушка»."
    },
    "173": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_pr_meta.py — третий ответ у связи теперь есть и проверяется: `Closes #N` при незакрытых пунктах чек-листа задачи отвергается, а пункт, названный закрытым строкой «Закрывает пункт: <текст>» в теле изменения, из счёта уходит — иначе последний этап закрыть было бы нечем. Разбор строки — в scripts/changerefs.py, там же где связь и снятие находки: отметка едет вместе с работой, а не отдельным жестом (002). Пункт называется текстом, а не номером строки: номер сдвигается от любой правки тела. Держат tests/test_gates_reject.py::test_closing_a_task_with_open_items_is_rejected и три соседних случая. Задача без чек-листа закрывается свободно — это названное состояние, а не пропуск (154); требование заводить чек-лист у задачи с этапами записано в AGENTS.md. Отметку ставит scripts/automerge.py в момент слияния: пункт становится сделанным ровно тогда, когда изменение слилось — раньше это обещание, позже уже история. Отказ разметки слияния не отменяет и заход не роняет, а ненайденный пункт называется вслух (045)"
    },
    "174": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/build_facts.py — проект публикует факты о себе сам, в ветку `badges`: объявленный контракт, посчитанная версия, ответы по правилам, классы проверок и разрез по общим механизмам семьи. Соседу и каталогу не приходится читать наше дерево, чтобы это узнать. .github/workflows/badges.yml — прогон; tests/test_facts.py — числа берутся из источников, а не пишутся руками"
    },
    "175": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gaps_section.py — таблица «чего в проекте ещё нет» в AGENTS.md проверяется на подкласс, сводимый к наличию: ни один пробел не называется ЧИСЛОМ, потому что счёт двигает механизм (шаг debt, facts.json в ветке badges), а вписанный рукой расходится с деревом на первой же разобранной записи и продолжает выглядеть решением. Проза целиком гейтом не трогается намеренно. Правило поймало обе строки при первом же применении: «ответа по 129 записям» устарело на 71 запись, а «первого прогона публикации нет» — на состоявшийся прогон и живую ветку badges"
    },
    "176": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_source_hygiene.py::test_subprocess_text_names_its_encoding — text=True без encoding берёт кодировку у локали раннера, и матрица прогонов этого не доказывает: дефект требует совпадения ячейки И данных, поэтому зелёное говорит «совпадения не случилось на этих данных». Ловится разбором исходника, а не прогоном, — гейт разбирает вызовы деревом AST во всех scripts/ и tests/"
    },
    "177": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/debt.py — три числа незакрытой работы по правилам читаются из задачи-«входящих» и печатаются шагом debt; docs/behaviour.md, контур 1 — источник 5 стоит выше плана"
    },
    "178": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/rules-inbox.yml и scripts/debt.py — числа об одной величине приходят с двух сторон: каталог считает по своей выгрузке, проект — по .rules/bindings.json. При расхождении первым проверяется СВОЁ чтение: поле answers_to называет версию выгрузки, по которой построены ответы, и сверяется каталогом. Именно так расхождение и всплыло — schema была вписана по разумению окна вместо заготовки каталога, а не потому, что величины разные; scripts/drift.py — «один снимок старее другого» проверяется механизмом, а не гипотезой: сводка семьи сверяется с живым ответом дерева, и расхождение называется правилами поимённо"
    },
    "179": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/ci.yml, agent-pr.yml, labels-sync.yml — группа отмены называет голову, а не только номер изменения"
    },
    "180": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_ghrest.py — гейты, ищущие обращения к площадке, определяют предмет по ИМПОРТАМ разбираемого файла (функция, собирающая имена импортированных модулей), а не по последнему звену имени вызова: своя функция с тем же именем ничего не доказывает, а импорт под псевдонимом иначе спрятал бы вызов от проверки"
    },
    "181": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_version.py — исключительное утверждение свода «версия живёт в CONTRACT_VERSION и больше нигде» связывает ВЕСЬ проект, и гейт так его и проверяет: находка краснеет, где бы число ни стояло, а не только в разделе о выпуске. Второе такое утверждение о том же предмете завести нечем — источник версии в дереве один. У других исключительных утверждений («трекер — единственный источник статусов») механизма нет, и держатся они чтением"
    },
    "182": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/ci.yml, джоб attribution — требование «изменение открывается от лица владельца» разложено надвое: намерение машине недоступно, но АВТОР и соавтор в истории следуют из данных целиком, и эту половину держит действие каталога (attribution@v1.2.0) с отметкой baseline. AGENTS.md называет вторую половину явно — часть запретов машинно не проверяется, и записаны они именно поэтому (057)"
    },
    "183": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_bindings.py::test_declared_address_resolves — проза, объявляющая, ЧЕМ держится правило, сверяется с деревом: адрес механизма обязан разрешаться, иначе ответ «держится гейтом по адресу X» при отсутствующем X хуже отсутствия ответа — он выглядит исполненным. Чужие адреса из проверки исключены намеренно: их правит владелец каталога (185)"
    },
    "184": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_bindings.py::test_negative_answers_name_the_reason — «не относится» без причины не проходит вовсе, а сами причины в .rules/bindings.json написаны разбором надвое: каждая называет, чего нет по букве (параллельных исполнителей, переводов, своего хранилища, сравнений с послаблениями), и границу, после которой обобщение заработает здесь — чаще всего шаг 8 или 9 из docs/roadmap.md. Форму держит гейт, разбор — прочтение причины человеком"
    },
    "185": {
      "status": "active",
      "mechanism": "gate",
      "where": ".rules/proposals.json — след предложения ведёт в дерево каталога и номера здесь не присваиваются; tests/test_bindings.py::test_proposals_assign_no_numbers держит это гейтом: поля id, number и rule в предложении — ошибка, номер присваивает каталог при приёме"
    },
    "186": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_changelog.py — у инструмента четыре режима (сборка, --check, --fragments, --release) и три кода, и переиспользования между режимами нет: код 1 существует ТОЛЬКО в сухом режиме и означает «расходится», а режим записи отвечает нулём и печатает свидетельство работы — какой файл собран и с какой версией контракта. Держит tests/test_gates_reject.py"
    },
    "187": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — пропущенная, отменённая и отсутствующая записи считаются отказом; tests/test_gates_complete.py прогоняет каждый из этих случаев"
    },
    "188": {
      "status": "not-applicable",
      "why": "текста, который обязан прочитать и на который обязан ответить кто-то ещё, автоматика проекта не публикует: описание изменения и записи в живой задаче адресованы человеку и окну, а бот отвечает на обращение ЧЕЛОВЕКА (.github/workflows/claude.yml), а не на публикацию механизма. Правило вступит, если конвейер начнёт звать ревью комментарием — тогда успешный код ответа перестанет доказывать доставку смысла"
    },
    "189": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/agent-pr.yml и scripts/agent_pr.py — приставка ветки одна, `agent/**`; выданная платформой `claude/<имя-окна>` изменения больше не открывает, то есть ветка, названная ОКНОМ, в конвейер не входит вовсе. Гейт различает не смысл имени, а его приставку: «по задаче, а не по окну» дальше держится сводом (CLAUDE.md, раздел «Ветка в облачном окне») и связью изменения с задачей (scripts/check_pr_meta.py). Прежний ответ был «отвергнуто» — «имя ветки назначает платформа»; это перестало быть правдой, когда окно начало резать свою ветку, и подтверждено прогоном на изменении #35"
    },
    "190": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".rules/bindings.json и .rules/proposals.json собраны scripts/onboard_consumer.py каталога, .github/workflows/rules-inbox.yml подключён его действием — форма ответа спрошена у соседа, а не воспроизведена по памяти"
    },
    "191": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/agent_pr.py::sync_description — вторая половина правила держится точно: механизм ПЕРЕЧИТЫВАЕТ тело изменения перед записью и по отметке MARK отличает своё от правленного человеком, чужое не затирая. Заготовок со стоп-краном первого рода — тех, что механизм создаёт вместо человека и держит красным до заполнения, — у проекта нет: описание изменения он заполняет сам из коммитов тем же заходом"
    },
    "192": {
      "status": "active",
      "mechanism": "document",
      "where": "docs/decisions/001-attribution-of-merged-1.md — замер авторства снят при одном исполнителе (изменение открывало приложение), и правка передавала действие другому (токену владельца), то есть обесценивала именно тот замер, на который опиралась. Поэтому решение требует смотреть автора в общей ветке ПОСЛЕ первого же изменения нового пути: личность записи узнаётся записью, а не чтением команды (135, CLAUDE.md)"
    },
    "193": {
      "status": "not-applicable",
      "why": "массовой автоматической починки данных в проекте нет: механизмы правят по одному предмету за изменение и проверяются отказом. Правило вступит вместе с первой такой починкой"
    },
    "194": {
      "status": "not-applicable",
      "why": "внешних ресурсов, чью принадлежность надо доказывать, у проекта нет: имени в чужом реестре он не занимает — ни пакета, ни образа, ни аккаунта. Единственный внешний механизм, на который он ссылается, — действие каталога, и оно адресуется полным путём репозитория с закреплённой версией. Правило вступит вместе с публикацией пакета (шаг 9 в docs/roadmap.md)"
    },
    "195": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_source_hygiene.py — починка соседей не оставляет: гейты идут по ВСЕМ модулям и всем прогонам сразу, а не по тому файлу, где нашли. Так и вышло: список путей без -z нашёлся в scripts/agent_pr.py, а закрыт вместе с шагом «тронутые файлы» в .github/workflows/ci.yml; кодировка у text=True — сразу в пяти вызовах. Сосед, который и прежде не работал, тоже красный: гейт не спрашивает, был ли он зелёным раньше"
    },
    "196": {
      "status": "active",
      "mechanism": "document",
      "where": "README.md — ссылка на значок уехала в общую ветку ОТДЕЛЬНЫМ изменением после первого прогона публикации, а не вместе с .github/workflows/badges.yml: пока прогона не было, витрина честно говорила, что значка нет. Порядок держит таблица пробелов в AGENTS.md — строка уходит оттуда после прогона (139), а проверкой «файл существует» это не лечится: на изменении артефакта и не должно быть"
    }
  }
}
