{
  "schema": "1.4",
  "answers_to": "1.7",
  "_answers_to": "Версия ВЫГРУЗКИ каталога (export/rules.json → contracts.export), по которой построены ответы. Каталог сверяет её со своей и печатает отставание: подъём контракта — повод перечитать ответы, а не только формат (157). Читается из выгрузки, а не по памяти: первая редакция этого файла объявила schema 1.0, когда каталог ждал 1.2, и расхождение всплыло только на прогоне входящих.",
  "project": "ArtVsMark/Engineering-Pipeline-Mechanisms",
  "catalogue": "https://github.com/ArtVsMark/Engineering-Incidents-Playbook",
  "_": "Ответ ЭТОГО проекта по каждому правилу каталога. Статус — active, rejected, not-applicable или unreviewed; у двух отрицательных обязательна причина в поле why, у active — mechanism и where.",
  "_зачем": "Ответ живёт здесь, а не в каталоге, потому что здесь живёт механизм: одно правило в разных проектах держится по-разному.",
  "rules": {
    "001": {
      "status": "active",
      "mechanism": "gate",
      "where": "packages/transport/ghrest.py — единственный транспорт к площадке, REST стандартной библиотекой; tests/test_ghrest.py держит это гейтом: ни один механизм не импортирует urllib.request и не собирает заголовок с токеном сам. Раньше ответ был «pipeline»: транспорт соблюдался внимательностью, и пять копий обвязки с четырьмя разными разборами отказа — цена этого"
    },
    "002": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/ci.yml — джоб на каждый шаг; tests/test_gates_reject.py — гейты проверены тем, что обязаны отвергнуть"
    },
    "003": {
      "status": "active",
      "mechanism": "gate",
      "where": "AGENTS.md — про переключатель сказано В САМОМ СВОДЕ, а не только в прогоне: приставка `agent/<задача>` названа вместе с тем, что по ней открывается изменение, и рядом сказано, что ветку без неё scripts/agent_pr.py отвергает. CLAUDE.md договаривает последствие другого имени прямым словом: толчок в выданную платформой ветку изменения НЕ ОТКРОЕТ. .github/workflows/agent-pr.yml — сам переключатель; scripts/agent_pr.py — отказ ветке без приставки; tests/test_gates_contract.py::test_branch_prefixes_match_the_workflow не даёт двум спискам разъехаться. Приставка одна: `claude/**` была переходной заглушкой и снята (094)",
      "why": "Ответ дополнен 17.09.2026. Механизм был назван верно и НЕПОЛНО: правило требует, чтобы переключатель был написан В ПРАВИЛАХ — «первой строкой, и с объяснением, что случится при другом имени», — а ответ называл только прогон и гейт. Замер того же дня: свод это уже говорил (AGENTS.md, раздел про имя ветки; CLAUDE.md, раздел про ветку в облачном окне), и документарная половина стояла неподписанной"
    },
    "004": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/automerge.py — конфликтная голова ПРОПУСКАЕТСЯ, очередь идёт дальше и ничего не замораживает: изменение возвращается в контур 1 источником 1 (docs/behaviour.md). Держит tests/test_automerge.py::test_a_conflict_is_stepped_over_not_escalated. Тот же приём у сводного гейта: scripts/ci_complete.py не роняет опрос на красной совещательной, а помечает её и выносит вердикт по обязательным"
    },
    "005": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_facts.py — числа витрины СОБИРАЮТСЯ, а не вписываются: шесть значков (правила, семья, версия, выпуск, скрипты, покрытие) рисуются из дерева каждым прогоном, и рукописного числа в них нет ни одного. scripts/check_derived_refs.py — ссылка на производное краснеет, если производное не нарисовано: значок-призрак хуже отсутствующего. scripts/check_version.py — число версии вне CONTRACT_VERSION и вне маркера краснеет (ALLOWED — список разрешительный). Числа в прозе живут по другому уговору: они ЗАМЕРЫ С ДАТОЙ, а не текущие величины, и форму «замер ДД.ММ.ГГГГ» требует tests/test_rulebook_fresh.py у каждого навыка",
      "why": "Ответ дополнен 17.09.2026. Назван был только гейт версии — а первая область правила это «метрики в README, витринах и отчётах: число тестов, покрытие, количество релизов». Их у нас шесть, все собираются механизмом, и ни один не был назван этим ответом. Ответ отвечал на край правила, а его середину оставлял неподписанной (046)"
    },
    "006": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_window_lifetime.py — срок жизни окна считается по трейлеру `Claude-Session` в истории общей ветки, и превышение объявленного правилом предела называется; шаг `window-lifetime` в `ci.yml`, держит tests/test_window_lifetime.py. Разбор следа окна вынесен в scripts/window.py — его же читает оклик, и второй образец трейлера разошёлся бы с первым молча (090). Класс проверки объявлен в `.pipeline.yml` словом `unreviewed` (scripts/pipeline_checks.py — там же его смысл): слияния она не держит, потому что перезапуск решает человек, а красное, которое правкой изменения не чинится, гнало бы окно чинить неисправимое (051). ПРЕЖНИЙ ОТВЕТ БЫЛ `document` С ПРИЧИНОЙ «механизма нет и быть не может — счётчика сессии площадка не даёт», И ЗАМЕР ПОД НИМ ДЕРЕВОМ НЕ ПОДТВЕРЖДАЕТСЯ: замер 14.09.2026 по полной истории — трейлер окна в 309 коммитах из 340, различных окон три, изменений на окно 6, 44 и 258, срок жизни окна 2 ч, 7 ч и 4 сут 13 ч. Последнее — у верхней границы объявленного срока, то есть величина легла ровно туда, где правило её нормирует. Нашёл внешний взгляд находкой `51a6454` на #335"
    },
    "007": {
      "status": "not-applicable",
      "why": "механизма, решающего за окно, что чужая работа брошена, у проекта нет: исполнитель один, и «зависло или ждёт разрешения» видит владелец, а не автоматика. Реестра сессий площадка проекту не отдаёт. Возраст окна проекту с 14.09.2026 ВИДЕН — его меряет scripts/check_window_lifetime.py по трейлеру `Claude-Session`, — но к этому правилу он не относится: «окно живёт долго» и «работа брошена» разные состояния, и второе из первого не выводится (046). Правило вступит вместе с очередью (шаг 8), где голова может стоять из-за окна, ждущего человека"
    },
    "008": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_docs_shape.py::test_no_collapsed_blocks — сворачиваемых блоков в документах нет ни одного. Основной читатель здесь окно: оно читает документ текстом и целиком, и `<details>` для него — заголовок без содержимого, причём пропажа выглядит пустым разделом, а не скрытым"
    },
    "009": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — записи проверок берутся с filter=latest и сворачиваются по ИМЕНИ, а не суммируются: повторный прогон кладёт на голову вторую запись того же имени, и счётчик по записям соврал бы. Границу этого свёртывания держит tests/test_gates_complete.py: отменённые записи filter=latest не отсекает, и они разбираются отдельно. То же в scripts/changerefs.py и scripts/squash_body.py — связи и трейлеры собираются без повторов"
    },
    "010": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — пустой список записей на голове считается отказом, а не «зелено»: «прогон не стартовал» и «все прошли» разведены; tests/test_gates_complete.py::test_no_records_on_the_head_is_rejected держит именно пустой вход, а ::test_missing_record_is_rejected — недостачу одного имени"
    },
    "011": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/ci_complete.py — опрос условный: с пределом ожидания и выходом по вердикту, а не бесконечный; прочие прогоны идут от событий, расписаний в проекте нет"
    },
    "012": {
      "status": "active",
      "mechanism": "gate",
      "where": ".claude/hooks/push_guard.py — хук PreToolUse, объявленный в .claude/settings.json: толчок в ветку, отличную от текущей головы, отвергается ДО вызова git; общая ветка отвергается из любой головы (131). Конвейер такого не ловит по построению — он видит артефакт, а к его запуску ветка уже сдвинута. Прежний ответ говорил «предмет вне досягаемости: чья ветка, знает человек» — и описывал не тот предмет: признак «чужая» машине недоступен, а «не совпадает с головой» доступен вполне. Инцидент свой: 10.09.2026 окно с ветки agent/items-sweep толкнуло в agent/marking-moves-out-of-the-queue, и конвейер открыл второе изменение на ту же работу (#116). Приём взят у трёх соседей, файл свой: у них запреты свои (162). Разбор идёт по словам команды, а не подстрокой: `git push` в теле записываемого файла — данные (085)."
    },
    "013": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_contract.py::test_workflows_do_not_embed_code — ни один шаг прогона не несёт встроенной программы: логика зовётся файлом из scripts/. Цена встроенного кода здесь двойная: оболочка меняет экранирование по дороге, и вдобавок его не касаются ни ruff, ни mypy, ни набор тестов — три гейта разом перестают его видеть"
    },
    "014": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_journal.py — починка, тронувшая механизм, обязана принести проверку: без входа, на котором гейт краснел до правки, убирается ПОВЕДЕНИЕ, а не разбор, и дефект возвращается молча. Держат tests/test_gates_reject.py::test_a_fix_without_a_test_is_rejected и ::test_a_fix_with_a_test_passes; починка прозы проверки не требует — требовать её значило бы заводить пустые проверки ради гейта. Замер 09.09.2026 по сорока последним коммитам общей ветки: починок с кодом 15, все принесли проверку — гейт закрепил соблюдаемое, а не ввёл новое. Половина правила механизмом не держится и названа пробелом в AGENTS.md: САМ замер «гейт краснел до починки» дереву доказать нечем"
    },
    "015": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/review.yml — у всех трёх запусков агента `claude_args: --allowedTools` СПИСОК ЗАКРЫТЫЙ и права записи в нём нет: `Read`, `Grep`, `Glob`, прогон набора и `git diff`/`log`/`show`. Исполнитель отдаёт ТЕКСТ — находки и вердикт, — а дерево правят скрипты хоста: scripts/review_findings.py, scripts/unlooked.py, scripts/late_look.py. Держит это tests/test_review_safety.py::test_the_reviewer_stays_a_reader — ни одному запуску не разрешён пишущий инструмент; список разрешительный, и не перечисленное запрещено само (068), так что отдельного запрета не нужно",
      "why": "Ответ пересмотрен 17.09.2026 и сменён с «неприменимо». Прежняя причина — «параллельных исполнителей проект не запускает: окно одно» — утверждение О ДЕРЕВЕ, и оно было ложным уже в день, когда его написали: .github/workflows/review.yml зовёт `anthropics/claude-code-action` ТРИЖДЫ — джобы `review`, `verify`, `late-look`. Премиса не устарела, а была неверна (044). Само же требование правила проект исполнял с самого начала, не зная об этом: право записи агенту не выдавалось ни разу"
    },
    "016": {
      "status": "active",
      "mechanism": "gate",
      "where": "packages/transport/report.py — обрезка вывода одна на все механизмы и называет полный объём; tests/test_report.py запрещает срез строки-вывода на месте, в обход общего механизма"
    },
    "017": {
      "status": "active",
      "mechanism": "gate",
      "where": "packages/transport/ghrest.py — остаток квоты читается из заголовков ответа при каждом запросе, исчерпание отличается от прочих отказов отдельным родом (RateLimited) и называет срок сброса; tests/test_ghrest.py держит разбор отказа"
    },
    "018": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/ci.yml — узлы и цепочка разведены по окружениям: набор в scripts/ и tests/ проверяет механизмы по одному на раннере, а цепочку — площадка, и только она. Поэтому решение принимается по прогону, а не по зелёному набору (139), а с #48 гейты идут ещё и на общей ветке: изменение и main — разные окружения, и зелёное на изменении не значит зелёного на main"
    },
    "019": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_pipeline.py — обход идёт от ПОВЕРХНОСТЕЙ: по каждой проверке дерева обязан быть ответ в .pipeline.yml, пустая клетка называется словом «unreviewed», и обратное держится тоже — у каждого ответа есть проверка, которая его выдаёт. Там же отдельно: предмет опроса не пуст (075). Это ровно приём правила — перечислить поверхности заранее и спросить по каждой «кто её проверяет», чтобы пустая клетка была видна сразу. Сверка с соседями построена так же — от шагов скелета (docs/pipeline.md), и находкой замера оказались именно отсутствующие шаги, которых в файлах нет вовсе. СУЖДЕНИЕМ остаётся сам СПИСОК поверхностей: что считать поверхностью, машине не решить, и правило называет эту цену прямо (182). ОБХОД ПОВЕРХНОСТЕЙ У ПРАВИЛА ВТОРОЙ, И ОН ЖИВЁТ В docs/roles.md: там по каждому пласту продукта спрошен владелец вопроса, а полноту карты против правила 082 держит tests/test_roles_map.py. Здесь обход идёт от проверок дерева, там — от направлений; оба отвечают на «кто её проверяет», и второй видит пласты, у которых проверки нет вовсе."
    },
    "020": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/unlooked.py::queue_of и реестр #89 — на повторный заход берутся ТОЛЬКО изменения без вердикта, и порядок в очереди по дате СЛИЯНИЯ, а не по номеру. Уже просмотренное из очереди уходит само (scripts/unlooked.py::looked, retire), то есть повтор идёт по недостающему, а не по всей волне. Полный перезапуск здесь стоил бы прогона агента на каждое уже просмотренное изменение. Держит tests/test_unlooked.py::test_the_queue_skips_what_was_already_looked_at — просмотренное в очередь не возвращается",
      "why": "Ответ пересмотрен 17.09.2026 и сменён с «неприменимо». Прежняя причина — «волн параллельных исполнителей нет» — опирается на ту же ложную премису, что и 015: агент запускается в .github/workflows/review.yml трижды, а поздний взгляд идёт МАТРИЦЕЙ по очереди изменений. Замер того же дня: очередь берёт до трёх изменений за заход (LOOK_AT_ONCE), и просмотренное в неё не возвращается — дельта считается механизмом, а не рукой"
    },
    "021": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_docs_shape.py::test_every_document_declares_its_reader — строка «Читатель:» обязана стоять в первых 12 строках каждого документа дерева; исключения названы поимённо (CHANGELOG.md — производное). Адресаты сами: AGENTS.md (агент) · CLAUDE.md (окно) · README.md (посетитель) · docs/pipeline.md и docs/behaviour.md (ведущий конвейер). Гейт перенесён от соседей: у троих из пяти правило уже держалось машиной (162)."
    },
    "022": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_doc_links.py — ссылка внутри своих документов ведёт к существующему файлу и якорю; адреса разделов площадки (../../issues/N) из проверки выведены списком разрешённого. Каноны темы: docs/pipeline.md · docs/behaviour.md · docs/release.md — своды на них ссылаются и не пересказывают. Смысловое расхождение копий машиной не берётся — граница правила сама это называет, и держит его вычитка"
    },
    "023": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_docs_shape.py::test_the_storefront_stays_within_five_minutes — витрина держится в пределе 1000 слов прозы: «первые пять минут» при 200 словах в минуту. Счёт в СЛОВАХ, а не в строках: перенос — форматирование, и гейт по строкам краснел бы от переливки абзацев (051); адрес ссылки словами не считается. Замер 13.09.2026: README вырос 2 → 81 → 90 → 98 → 102 → 111 строк за три дня, ни разу не убыв, и сегодня в нём 700 слов — запас около трети. Храповика «не выше достигнутого» нет намеренно: витрине законно расти, пока она остаётся витриной. Что именно нужно человеку в первые пять минут, машина не решает — гейт держит РЕГРЕСС, который правило само называет неизбежным; связность и уместность разделов остаются работой человека (195). ГДЕ ПРЕДЕЛА НЕТ — названо поимённо (замер 14.09.2026, tests/test_docs_shape.py: UNCAPPED). Потолок стоит у того, что читают ЦЕЛИКОМ И ВСЕГДА: витрина, ядро, свод окна. У канона (docs/) его нет намеренно — канон читают по надобности и частями, рост там не означает поломки, а гейт, краснеющий на законном, учат обходить (051); регресс канона держат деление по читателю (021) и один канонический документ на вопрос (022). У записей решений предела нет по прямому запрету: запись — датированный факт, сокращать её нельзя вовсе (043). Цена границы названа: docs/pipeline.md 2062 → 3178 слов за шесть дней, docs/release.md 855 → 2290, и ни один документ проекта ни разу не убыл."
    },
    "024": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_no_work_log.py — у действующего документа нет раздела-летописи: журнал сделанного живёт в CHANGELOG.md, статусы — в трекере, а документ говорит, как есть СЕЙЧАС. Раздел «что мы уже сделали» внутри канона превращает его в летопись: читатель не отличает описание устройства от рассказа о пути к нему, и с каждым заходом такой раздел растёт. Список заголовков закрытый, а не «всё, похожее на историю»: записи решений — исключение по построению, там раздел «Последствия» часть записи, а не журнал (043). Плюс AGENTS.md, раздел «Открытая работа»."
    },
    "025": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/journal.py — LINK_LINE_RE, форма строки со связью; scripts/build_changelog.py — номер задачи обязан быть ПОСЛЕДНЕЙ строкой фрагмента: там он датировка и там нужен; scripts/check_journal.py требует самого фрагмента у изменения. В объяснительных документах номер стоит только там, где назван пробел с адресом (046) — «чего ещё нет» в AGENTS.md, перечень отложенного в задаче эпика, — а не как отметка о том, когда абзац написан"
    },
    "026": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_bindings.py::test_negative_answers_name_the_reason — отклонить правило каталога без причины нельзя: у rejected и not-applicable поле why обязательно, и след отклонения живёт в .rules/bindings.json рядом с ответом. У находок ревью тот же приём: запись снимается строкой «Разобрано: <отпечаток>» в теле изменения (scripts/review_findings.py), то есть отклонение едет вместе с работой и остаётся в истории, а не стирается из тела задачи молча"
    },
    "027": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/review_findings.py — render_body печатает «Пусто», а не оставляет пустой раздел; .rules/proposals.json объявляет пустой список законным состоянием; tests/test_review_findings.py::test_empty_body_says_so_explicitly"
    },
    "028": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/task_shape.py::without_a_checklist — шаг debt на каждом изменении называет открытые задачи, которые ведут три и более пункта прозой, а не галочками. Эпик с дочерними задачами и задача с любыми галочками кандидатами не считаются: у первого счёт ведёт трекер, вторая его уже ведёт. AGENTS.md, раздел «Открытая работа» — комплексная задача ведётся подзадачами площадки. Задача, которую ведёт механизм, кандидатом не считается вовсе: её тело пересобирается каждым заходом, и галочка, поставленная рукой, исчезнет на следующем, а состояние её и так счётчик. Признак — общая метка живой задачи (findings.is_kept_by_a_mechanism), а не догадка"
    },
    "029": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_docs_shape.py::test_the_main_file_stays_a_trigger — свод AGENTS.md держится в пределе 2300 слов прозы, и test_the_main_file_sends_the_reader_to_the_canon проверяет, что он отсылает к канону, а не пересказывает его. Объём главного файла — НАЛОГ НА ЛЮБУЮ РАБОТУ: его читают целиком и при каждом старте окна. Число взято из РЯДА, а не из правила (в отличие от витрины, где правило само назначает пять минут): замер 13.09.2026 по 47 правкам — 985 → 1237 → 1808 → 2185 слов за четыре дня, рост вдвое и ни одного отката. Запас намеренно мал, около абзаца: добавка крупнее требует вынести равнозначное в канон — ровно тот разговор, которого правило и просит. ЧТО именно вынести, гейт не судит: критерий деления — «нужно, чтобы не сделать ошибку прямо сейчас» против «нужно, когда занимаешься предметно» — суждение о читателе (182, 195). Врезка «здесь ни одно их утверждение не повторяется» стоит в AGENTS.md с 09.09. ВТОРАЯ ПОЛОВИНА НАЛОГА ЗАКРЫТА 14.09.2026: предел появился и у свода окна (test_the_window_file_stays_a_trigger, 900 слов при 828 на замере), а у суммы потолок СВОЙ и ниже суммы частей: test_the_start_tax_is_held_as_a_whole держит 3150 слов при 3059 на замере 15.09.2026, то есть запас у половин общий — одна вправе занять свободное у другой, обе разом нет. Первая редакция ставила сюда 2300 + 900 и покраснеть не могла ни при каком входе; нашёл внешний взгляд (18823c0), и гейт, который не может найти ничего, считается поломкой, а не зеленью (075). Способность покраснеть одному держит test_the_start_tax_can_go_red_on_its_own (140)."
    },
    "030": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_journal.py — изменение без фрагмента отвергается; changelog.d/ — фрагменты; scripts/build_changelog.py — сборка"
    },
    "031": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/unlooked.py — размер волны позднего взгляда объявлен числом в `LOOK_AT_ONCE` (3) и с причиной: каждый заход это прогон агента, три за заход рассасывают очередь за дни, а не за месяцы. Число читает scripts/unlooked.py::queue_of, и рукой его на месте вызова не подвинуть. .github/workflows/review.yml, джоб `late-look` — `max-parallel: 1` в матрице: волна идёт по одному, а не залпом. Число не догадка: у каталога пять, у нас три, и причина у нашего числа своя — цена прогона, а не пропускная способность площадки. Оба числа держит набор: tests/test_unlooked.py — очередь не длиннее `LOOK_AT_ONCE`, tests/test_review_safety.py::test_a_matrix_of_agents_runs_as_a_wave_not_a_salvo — матрица агентов идёт по одному. Второе не держалось ничем до 17.09.2026: снятая строка `max-parallel` выпустила бы очередь залпом, и красного не было бы нигде",
      "why": "Ответ пересмотрен 17.09.2026 и сменён с «неприменимо». Прежняя причина — «залпов параллельных исполнителей проект не делает» — верна по букве и отвечает не на тот вопрос: правило требует не отсутствия залпов, а ВОЛНЫ ФИКСИРОВАННОГО РАЗМЕРА, и размер у нас объявлен числом с причиной. Сказать «залпов нет» и промолчать про число значит скрыть механизм внутри ответа о его отсутствии"
    },
    "032": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_review_safety.py::test_a_permitted_run_is_actually_possible и ::test_both_interpreter_names_are_permitted — роль внешнего взгляда объявляет прогон проверок, и прогоны держат, что объявленное ВОЗМОЖНО: если список разрешает «python -m pytest», в джобе обязаны быть setup-python и установка проверок, а разрешение выдаётся обоим именам интерпретатора. Механизм построен 09.09.2026 по задаче #33 и закрыл её: до того ревью на #32 честно сказало, что прогнать не смогло, и всё-таки вынесло «находок 0» — вердикт чтением выглядел как вердикт после прогона (045). Что РОЛЬ ДЕЙСТВИТЕЛЬНО прогнала продукт, а не только могла, из дерева не прочесть: это остаётся её собственным отчётом (182). ГДЕ ЭТОТ ЗАПУСК НЕ ДОХОДИТ, НАЗВАНО В docs/roles.md: вход у взгляда один — от правил с ответом «document», которые собирает scripts/review_map.py, — и пласты, не бывшие правилами каталога, ему не называет никто. В карте они помечены «роли нет»."
    },
    "033": {
      "status": "not-applicable",
      "why": "длинной волновой работы с бюджетом у проекта нет: прогоны идут на событие, а не пачками, и упереться в стену посреди волны нечему. Единственный исчерпаемый ресурс — квота площадки, и она держится порогом останова (packages/transport/ghrest.py), а не расчётом темпа. Правило вступит, если появится массовый обход (перенос механизмов по пяти проектам разом)"
    },
    "034": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/review.yml — зона каждого запуска агента названа его входом и мала по построению: `review` смотрит ОДИН диф, `verify` — ОДНУ находку (`--verify <отпечаток>`), `late-look` — ОДНО изменение (`matrix.pr`). Дробление держит scripts/review_findings.py: находки разбираются поштучно по отпечатку, а не пачкой, и scripts/unlooked.py::queue_of — очередь отдаёт изменения по одному",
      "why": "Ответ пересмотрен 17.09.2026 и сменён с «неприменимо». Прежняя причина — «исполнителей, между которыми делится роль, в проекте нет» — та же ложная премиса, что у 015 и 020. Исполнителей трое, и роль между ними поделена ровно по правилу: смотрит один, проверяет находку другой, догоняет слитое третий. Замер 17.09.2026: ни одному из трёх не поручено больше одного предмета за запуск. Вид механизма назван КОНВЕЙЕРОМ, а не гейтом, и это замер, а не осторожность: дробление зоны задано формой прогона — входом каждого запуска, — и ни один тест не краснеет на том, что зону расширили. Гейт на это построить можно (вход шага агента читается из файла прогона), и он не построен: назвать это гейтом значило бы объявить отказ, которого нет"
    },
    "035": {
      "status": "active",
      "mechanism": "gate",
      "where": "CONTRACT_VERSION — единственный источник объявленной версии контракта; scripts/version.py — версия проекта СЧИТАЕТСЯ по истории, а не вписывается, и механизм ничего в дерево не пишет; scripts/check_version.py — гейт на рукописное число, видящий и незакоммиченные файлы; tests/test_version.py и tests/test_gates_reject.py проверяют отказом"
    },
    "036": {
      "status": "not-applicable",
      "why": "дорогого окружения у проекта нет: все прогоны идут на одном раннере ubuntu-latest, а внешний взгляд — один и тот же прогон ревью на каждом изменении. Делить аудит на дешёвую и дорогую фазы нечего. Правило вступит вместе со вторым окружением — платным ревьюером или прогоном на матрице систем"
    },
    "037": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/preflight.py — локальный заход называет, ЧЕГО он не проверяет: шаги, чей предмет у площадки (разметка изменения, база, записи проверок на голове, канон имени, выгрузка каталога), печатаются отдельным списком и не выдаются за пройденные. Зелёное здесь — гипотеза о зелёном там, и разница названа вслух. Вторая половина — tests/test_platform_shape.py: подделки площадки сверяются со снятым её ответом, и находка, полученная на подделке с ключом, которого площадка не отдаёт, до настоящей поверхности не доезжает (170). Прежний ответ описывал не тот предмет — он говорил о подтверждении механизма прогоном, а это правило 139."
    },
    "038": {
      "status": "active",
      "mechanism": "document",
      "where": "CLAUDE.md — раздел «Имя окна и его окружение»",
      "document_reason": "measured-refusal",
      "why": "Ответ перечитан 14.09.2026 по пункту 17.7, и признак ПРОВЕРЕН ЗАМЕРОМ. Имя окна проекту не видно: замер 14.09.2026 по 279 слитым изменениям (всего их 285, номера идут до #335) — ноль упоминаний приставки окружения ([WEB]/[LOCAL]/[CLI]) и ноль веток вида claude/*, по которым имя окна когда-то читалось. След окна из дерева ушёл намеренно: работа идёт в ветке agent/<задача>, названной ЗАДАЧЕЙ, а не окном (189), и приставки конвейера в имени окна нет (003). То есть предмета для гейта здесь не просто нет — он убран решением, и вернуть его значило бы назвать ветку окном ради проверки имени окна."
    },
    "039": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/*.py — коды возврата 0/1/2 у каждого механизма; tests/test_gates_reject.py прогоняет третий исход отдельно от второго"
    },
    "040": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — пропущенная проверка засчитывается ОТКАЗОМ, а не зелёным: «пропущен, а обязан был идти»; держит tests/test_gates_complete.py::test_skipped_is_rejected. В самом наборе пропусков нет ни одного — ни skip, ни xfail. Два послабления в прогонах названы поимённо: continue-on-error на шагах ревью в .github/workflows/review.yml, и причина записана решением docs/decisions/002-review-taken-from-two-neighbours.md — совещательный канал слияния не держит (084)"
    },
    "041": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_facts.py — витрина публикует НЕСКОЛЬКО честных чисел и ни одного усреднённого: правил всего и отвечено, разбивка по статусам, разбивка по механизмам, состав классов проверок. Значок несёт «отвечено/всего», а не долю и не «здоровье»; в самом модуле записано, почему выбрано это число, а не «сборка зелёная». Держит tests/test_facts.py: числа приходят из источников, а не из собранного файла"
    },
    "042": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_decisions_format.py::test_record_has_all_four_parts — запись решения состоит из контекста, самого решения, отвергнутых вариантов и последствий; заголовки канонические, а не список синонимов"
    },
    "043": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_decisions_edit.py — изменение не переписывает контекст, решение и отвергнутые варианты уже принятой записи и не удаляет её; статус и последствия менять можно — ими запись помечается заменённой и ими же копится опыт"
    },
    "044": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md, раздел «Перед толчком» — утверждение проверяется ОТКРЫТИЕМ предмета, а не памятью: номер правила каталога сверяется файлом, число в прозе — замером с датой. Тот же порядок у решений: docs/decisions/001-attribution-of-merged-1.md и docs/decisions/006-merge-by-squash.md приняты после замера на живом предмете, а не по премисе находки",
      "document_reason": "measured-refusal",
      "why": "Ответ перечитан 13.09.2026 по пункту 5.1. Проверка премисы — ДЕЙСТВИЕ получателя находки, и из дерева не читается: воспроизвёл ли он утверждение прежде, чем чинить, знает только он. Но практическая граница правила «премиса не подтвердилась — закрывается как неверная, С ЗАПИСЬЮ ПРИЧИНЫ» была до 13.09.2026 НЕВЫПОЛНИМА: тело уплотнения собиралось из одних отпечатков, и причина, написанная в ветке, до общей ветки не доезжала — замер по тридцати последним телам показал тридцать голых отпечатков и ни одной причины. Теперь строка снятия переносится как написана (scripts/changerefs.py::resolutions_in, scripts/squash_body.py), и два исхода снятия — «починено» и «премиса неверна» — в истории различимы (039). ТРЕБОВАТЬ причину от каждого снятия гейтом нельзя: починка её не требует, и проверка краснела бы на законном (051)."
    },
    "045": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/agent_pr.py, scripts/debt.py, scripts/check_required_context.py — объявленные исходы разведены с единицей (0/2/3), прогоны разбирают коды списком разрешённого; tests/test_gates_reject.py и tests/test_review_safety.py держат оба конца. РЕЖИМ ЗАХОДА НАЗЫВАЕТСЯ В КАЖДОЙ СТРОКЕ: пробный заход печатается приставкой «(пробный заход)» — scripts/automerge.py, scripts/drift.py, scripts/hail.py, scripts/items.py, — и по логу видно, в каком режиме прошёл КОНКРЕТНЫЙ запуск, а не какой был задуман",
      "why": "Ответ дополнен 17.09.2026. Признак нарушения у правила назван прямо: «в логе нет строки, по которой видно, в каком режиме прошёл конкретный запуск», — и это ДРУГОЙ предмет, чем коды возврата, которыми ответ ограничивался. Замер того же дня: пробный заход называет себя приставкой в каждой строке у четырёх механизмов, то есть требование исполнялось и не было подписано"
    },
    "046": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gaps_section.py — раздел «Чего в проекте ещё нет» проверяется механизмом: счёт в его строках сверяется с ответом каталогу, а две таблицы пробелов не противоречат друг другу. Проза целиком непроверяема и гейтом не трогается — но у неё есть подкласс, рассыхающийся молча и выглядящий при этом осознанным решением: ЧИСЛО (175, 005). Плюс README.md, раздел «Состояние: брать пока нечего»."
    },
    "047": {
      "status": "active",
      "mechanism": "gate",
      "skill": ".claude/skills/rulebook-reread",
      "where": "scripts/check_rulebook_fresh.py — шаг `rulebook-fresh` называет правки `AGENTS.md`/`CLAUDE.md`, случившиеся ПОД уже работающим окном: между первым его коммитом и головой изменения, чужим окном (своя правка окну известна). Вторая половина — навык `.claude/skills/rulebook-reread`: он читается В МОМЕНТ ВЫЗОВА, а не при старте окна, и потому показывает свод живым; этого у документа нет по построению. Перезапуск шаг не объявляет и решать за человека не берётся — класс `unreviewed`, слияния не держит (051). Разбор и отвергнутые варианты — docs/decisions/026-the-rulebook-is-reread-without-a-restart.md",
      "why": "Ответ перечитан 16.09.2026, и предел снят: был `not-yet` — «машинная половина есть и не построена», — теперь она построена. ПРЕДМЕТ ЗАМЕРЕН: по 381 коммиту общей ветки правок свода 56, окон четыре, и семь раз свод правило ЧУЖОЕ окно, пока это ещё работало (шесть у одного, одно у другого, все 09.09.2026, когда два окна шли вперемешку). Гейт находит все семь. Ловушка, названная прежним ответом, обойдена: перезапуск из смены номера сессии НЕ выводится — считаются правки свода в границах жизни окна, и чередование окон ложных срабатываний не даёт. Граница «своя правка не считается» названа отдельно: без неё девять правок текущего окна покрасили бы исправную работу. Чего механизм НЕ делает: он не перезапускает окно и не решает за человека — правило требует перезапуска, а механизм делает устаревание ВИДИМЫМ и даёт починку без перезапуска; это отступление объявлено в решении 026, а не спрятано"
    },
    "048": {
      "status": "not-applicable",
      "why": "шкал по внешнему индикатору проект не строит: ни одна величина не калибруется по моменту переключения чужого сигнала. Числа здесь считаются по дереву целиком (scripts/build_facts.py), а не по видимой части выборки. Правило вступит, если появится метрика, привязанная к чужому событию — например, к сроку ответа площадки"
    },
    "049": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — состояние берётся опросом проверок на голове изменения, реестра проверок проект не ведёт; scripts/main_red.py — номер попытки прогона спрашивается у площадки, свой счётчик разошёлся бы с ней молча и в сторону бесконечных перезапусков; scripts/unlooked.py — разделы реестра пересобираются из живых артефактов каждым заходом, поэтому позеленевшее уходит само; scripts/check_required_context.py — имя обязательного контекста читается у площадки, а не из памяти"
    },
    "050": {
      "status": "active",
      "mechanism": "gate",
      "where": ".rules/schedules.json — доля часового лимита `share` и есть числовой потолок; tests/test_schedules.py считает худший час и отвергает превышение, а ежечасный заход складывает с каждым фиксированным часом. Ответ «неприменимо» устарел в тот день, когда доля завелась: до неё потолков в дереве не было ни одного"
    },
    "051": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/agent-pr.yml и .github/workflows/labels-sync.yml — ненастроенность даёт предупреждение, а не отказ; красным остаётся только третий исход"
    },
    "052": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/automerge.py — базу подтягивает ТОЛЬКО голова очереди и только перед слиянием (sync_head), а за один заход сливается одно изменение: остальные после него всё равно устарели. Держат tests/test_automerge.py::test_only_the_head_pulls_the_base и ::test_one_pass_merges_at_most_one_change. Цена обратного названа замером соседа на 09.09.2026 — 21 холостой прогон против 12 полезных на шести изменениях; след замера — docs/decisions/003-own-automerge-not-native-queue.md"
    },
    "053": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/automerge.py — ступень очереди есть НОМЕР ИСТОЧНИКА РАБОТЫ из контура 1 (docs/behaviour.md): один словарь на оба контура, а не второй порядок для слияния. Источник 0 и 4 приходят меткой, 3 выводится машинно из строки снятия находки, 5 — эвристика по тронутому ответу каталогу и объявлена эвристикой; 1 и 2 не выводятся до обращения к площадке и называются там, где заход их обнаружил — цена правила 052. Пропускная способность («трогает общий с соседом файл») решает ВНУТРИ ступени, а не между ними: приоритетом она не является. Приход решает последним. Держат tests/test_automerge.py::test_the_step_is_the_number_of_the_work_source, ::test_a_finding_repair_outruns_the_plan и ::test_a_shared_file_decides_inside_a_step_not_across_them"
    },
    "054": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/review_findings.py — шаг ревью только записывает сырые строки «НАХОДКА:» в живую задачу и идёт дальше; ничего не чинит и не оценивает. Разбор — отдельным проходом на границе работы: источник 3 в docs/behaviour.md, снятие записи по отпечатку в теле следующего изменения"
    },
    "055": {
      "status": "active",
      "mechanism": "gate",
      "where": "Ответ разделён надвое (182). МАШИНОЙ держится то, что можно ВЗЯТЬ у источника, а не помнить: имена правил берутся из выгрузки каталога (scripts/check_rule_links.py — по памяти их написали 29 раз), своё имя репозитория спрашивается у площадки (scripts/check_own_name.py), ответы соседей читаются из снимка каталога (scripts/family.py), а с 13.09.2026 набор вопросов витрины сверяется с эталоном каталога составом, а не одним номером контракта (scripts/drift.py, источник «набор вопросов витрины»): каталог вправе добавить вопрос, не тронув схему, и до этого обе стороны видели своё зелёное. СУЖДЕНИЕМ остаётся подтверждение РУКОПИСНОГО эталона: замер 13.09.2026 — в дереве 29 порогов, у 9 основание названо прозой, и проверка по словам («замер», «правило», «взято у») дала бы ложные отказы, потому что основание — смысл текста, а не наличие слова (051). Форма эталонов спрошена у обоих соседей — docs/decisions/002-review-taken-from-two-neighbours.md."
    },
    "056": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_reject.py и tests/test_gates_complete.py — у каждого исхода проверяется вторая половина: «записи нет на голове — прогон не стартовал, а не „зелено“», «под учётом нет ни одного файла — ошибка входа, а не „чисто“». Тексты живут в scripts/ci_complete.py, scripts/check_version.py, scripts/check_journal.py, а держит их отказом гейт, а не внимательность"
    },
    "057": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_bindings.py::test_active_names_its_mechanism_and_address — правило, признанное действующим и не держащееся ничем, несёт mechanism=none И причину в why: молча выпасть из ответа оно не может. Правил с mechanism=none сейчас в ответе нет ни одного: последним был 163, закрытый гейтом 13.09.2026. Пустота названа, а не выдана за живой случай (154, находка внешнего взгляда на #319); гейт при этом остаётся — форма обязана держаться и на пустом множестве, иначе первое же mechanism=none приедет молча. Вторая половина — AGENTS.md, блок критических запретов с пометкой, какие из них машинно не проверяются. Форма совпадает с соседской намеренно (090): у четверых из пяти правило держится тем же приёмом."
    },
    "058": {
      "status": "active",
      "mechanism": "gate",
      "where": "packages/transport/ghrest.py — исчерпанная квота поднимает RateLimited и на этом останавливается: повтора нет ни одного, а текст отказа говорит «Ждать, а не повторять». Останов происходит ДО нуля — порог QUOTA_FLOOR, потому что на нуле операция уже брошена на середине, а счётчик продолжает расти. Полусделанного состояния после отказа не остаётся: механизмы не пишут ничего, пока запрос не вернулся. Держит tests/test_ghrest.py"
    },
    "059": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_secrets_declared.py — каждый секрет, к которому обращается прогон, назван в таблице AGENTS.md «Секреты: чем держатся и когда истекают»; объявление, пережившее свой секрет, тоже находка — владелец продлевал бы токен, которым никто не пользуется (005). Встроенный `GITHUB_TOKEN` освобождён поимённо и с причиной: его выдаёт площадка на каждый прогон, продлевать некому. Гейт сверяет СОСТАВ; сроки в таблице — знание владельца, из данных они не следуют. Для квоты площадки карта в packages/transport/ghrest.py: механизм останавливается ДО нуля и называет срок сброса. ВТОРАЯ ПОЛОВИНА БУКВЫ — «режим при исчерпании: КОПИТЬ, а не ждать» — держится packages/transport/ghrest.py: механизм останавливается ДО нуля квоты и называет срок сброса, а не ждёт на месте; записи при этом не теряются — они копятся в живых задачах-адресатах, и следующий заход берёт их оттуда. Ожидания на исчерпанном ресурсе у проекта нет ни одного."
    },
    "060": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "Две оси разбора ведутся ДВУМЯ реестрами, и они не сливаются. Механическая — scripts/unlooked.py и живая задача #89: кто не посмотрел, чей заход не состоялся, осечки канала накопителем. Качественная — scripts/review_findings.py и живая задача #23: ЧТО вышло, с отпечатком, премисой и вердиктом верификатора, который находку умеет опровергнуть, а не только починить. Первенство качественной оси задано порядком работ: docs/behaviour.md, контур 1 — находки берутся прежде плана",
      "why": "Ответ пересмотрен 17.09.2026 и сменён с «неприменимо». Прежняя причина — «волн нет, и разбирать между ними нечего» — опирается на ложную премису об отсутствии исполнителей (см. 015) и отвечает не на тот вопрос: правило спрашивает не про волны, а про РАЗБОР ПО ДВУМ ОСЯМ, и признак нарушения у него — «качество результатов впервые оценивается после последней волны». У нас оценивается на каждом изменении. Чего механизм НЕ делает: он не задерживает следующий заход до разбора предыдущего — очередь позднего взгляда пойдёт дальше, даже если находки прошлого захода не разобраны. Держит это порядок работ, то есть документ, а не гейт. Вид механизма назван КОНВЕЙЕРОМ: обе оси ведутся прогонами и живыми задачами, но ни одна не краснеет — реестр наполняется, а слияния не держит. Это объявленная граница канала (084), а не упущение"
    },
    "061": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/review.yml и .github/workflows/claude.yml — в тексте задания каждого запуска агента стоит блок «ГРАНИЦА ОКРУЖЕНИЯ»: разрешённые команды поимённо, слово о том, что список ЗАКРЫТЫЙ, и предупреждение, что отказ окружения — про окружение, а не про проверяемое изменение. Держит tests/test_review_safety.py::test_the_task_names_the_environments_limit — команда, добавленная в разрешение и не названная в задании, краснеет; и test_the_task_says_the_list_is_closed — перечень без слова о закрытости читается как примеры. Список в задании — НАМЕРЕННЫЙ ДУБЛЬ `claude_args`, подписанный таковым в самом задании (071): канонический список один, а адресату он не виден",
      "why": "Ответ пересмотрен 17.09.2026 и сменён с «неприменимо». Прежняя причина — «исполнителю, запускающему продукт, задание здесь не выдаётся» — ложна: заданий четыре, и всем разрешён прогон набора. ИНЦИДЕНТ ПРАВИЛА БЫЛ ОПЛАЧЕН ЗАРАНЕЕ и записан нашей же рукой в review.yml: разрешение выдаётся по началу команды, и `python3 -m pytest` — другая строка, чем `python -m pytest`, — «на ней ревью и осталось без прогона, разрешение имея». Починен был тогда список, а не задание. ЗАМЕР 17.09.2026: разрешённых команд по заданиям 26, названо в их текстах было 4. Гейт нашёл при этом ЧЕТВЁРТОГО агента, которого замер не видел: у ответа на обращение (claude.yml) ключа `prompt` нет вовсе — указания едут надбавкой к системному промпту, и проверка по одному ключу зеленела бы на нём (075). Чего механизм НЕ делает: он не проверяет, что агент блок ПРОЧИТАЛ, — это не следует из дерева"
    },
    "062": {
      "status": "active",
      "mechanism": "document",
      "where": "docs/decisions/002-review-taken-from-two-neighbours.md и scripts/review_findings.py — внешний взгляд заведён как роль, а не как раздел документации: у него свой вопрос (что не так с этим изменением), свой артефакт (записи в живой задаче с отпечатком) и своё ВОЗРАЖЕНИЕ окну, переживающее слияние. Ролей, не прошедших эту приёмку, в проекте нет ПРИЁМКА СВЕРЯЕТСЯ С КАРТОЙ: docs/roles.md перечисляет направления правила 082 и по каждому ставит исход, а tests/test_roles_map.py отвергает направление, которого правило не называет, — то есть роль, придуманную здесь и приёмки не проходившую. Это половина механизма, и вторую гейтом не взять: «своё возражение конкретной существующей роли» читается, а не считается, поэтому ответ остаётся «document». Перевести его в «gate» 16.09.2026 предлагалось и отвергнуто замером: гейт держит имя направления, а не приёмку, и объявить обратное значило бы завысить ответ — ровно та подмена, которую в тот же день отвергли по 082 и 088 (044).",
      "document_reason": "measured-refusal",
      "why": "Ответ перечитан 13.09.2026 по пункту 5.1, и признак ПРОВЕРЕН ЗАМЕРОМ. Предмет правила — ЗАВЕДЕНИЕ роли, а не её работа: приёмка проходится один раз, при появлении. За 338 слитых изменений роль заводили ОДИН раз — внешний взгляд, docs/decisions/002; других решений о ролях в проекте нет. Гейт на такой предмет краснел бы раз в историю проекта и всё остальное время проходил вхолостую (075). Сама приёмка машине и недоступна: «способна ли роль возразить» решается смыслом её вопроса — три написанные реплики спора, а не наличие полей. Что возражение СУЩЕСТВУЮЩЕЙ роли переживает слияние, держится отдельно и другим правилом: у совещательного класса объявлен разрешимый адресат (142, scripts/check_pipeline.py), и замер это подтверждает — 413 снятий находок строкой «Разобрано» за историю."
    },
    "063": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/agent_pr.py — автоматическое вмешательство (открыть изменение от лица владельца) включается конъюнкцией: названы репозиторий И ветка, И приставка объявлена, И токен владельца задан, И у коммитов есть связь с задачей. Любое невыполненное условие — «не трогать», и каждое отвечает своим исходом, а не общим отказом. Так же .github/workflows/review.yml: изменение из форка пропускается явно — условие джоба требует, чтобы голова лежала в этом же репозитории. Обращение проверяется в .github/workflows/claude.yml: author_association из закрытого списка OWNER/MEMBER/COLLABORATOR"
    },
    "064": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/labels.yml — состав файлом; scripts/labels.py — одно чтение состава на все механизмы; scripts/sync_labels.py приводит к нему площадку; scripts/check_pr_meta.py отвергает изменение без зоны; scripts/agent_pr.py проставляет зоны при открытии — метку больше не ставит человек по памяти; scripts/automerge.py выставляет метку присвоенного источника `source/<номер>` — это СЛЕД, а не вход: очередь решает по вычисленному источнику, и устаревшая метка увести слияние не может. Пишется только при расхождении: заход идёт на каждое событие, и платить запросом за неизменившееся состояние значит тратить квоту на ничто (017)"
    },
    "065": {
      "status": "not-applicable",
      "why": "меток входа для новых участников проект не объявляет (.github/labels.yml — список разрешительный, и такой метки в нём нет), потому что внешних участников у него пока нет: работают владелец и его окна. Заводить метку раньше аудитории значило бы объявить механизм, которого никто не читает (064). Правило вступит вместе с первым сторонним вкладом"
    },
    "066": {
      "status": "not-applicable",
      "why": "атомарной записи подменой файла у проекта нет: механизмы пишут либо в площадку через REST, либо собирают файл целиком одним вызовом write_text. Блокировок нет вовсе — параллельных писателей тоже. Правило вступит вместе с очередью слияний (шаг 8), где голову придётся брать под блокировку"
    },
    "067": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_arm.py::test_the_arming_is_always_taken_back — обе половины правила разом: снятие взведения идёт ДАЖЕ когда разбор ответа упал, и причина при этом ПРОБРАСЫВАЕТСЯ (проверка ждёт исключения, а не тишины). Уборка живёт в scripts/arm.py:probe — `finally` без `except`: широкий перехват без проброса спрятал бы отказ, узкий пропустил бы прерывание, и ни того ни другого здесь нет. Это единственная уборка в дереве, и держит её одна проверка: второй такой предмет обязан приехать со своим прогоном (tests/test_new_is_tested через scripts/check_new_is_tested.py)",
      "why": "Ответ пересмотрен 16.09.2026 и сменён с «неприменимо». Прежняя причина утверждала факт — «ни одного except Exception и ни одного finally в scripts/ нет», — и факт ПРОТУХ: scripts/arm.py с `finally` приехал позже ответа, а ответ этого не заметил. Предмет правила — операция с временным состоянием — у проекта появился вместе с замером взведения (решение 011): изменение взводится и снимается, и брошенное взведённым площадка слила бы сама. Правило применимо и УЖЕ соблюдено; ответ отставал от дерева (005)"
    },
    "068": {
      "status": "active",
      "mechanism": "gate",
      "where": "Списки в проекте разрешительные ПОВСЮДУ, а не в одном месте: .github/labels.yml — метка, которой файл не объявляет, краснеет (scripts/check_pr_meta.py); .rules/rerun.json — перезапускается только объявленное имя, обязательных там нет и быть не может; .rules/leniency.json — послабления при сравнении перечислены; .rules/protection.json — обход защиты законен только названный с причиной; scripts/kinds.py — виды механизма перечислены, а не выведены вычитанием; scripts/check_version.py — места, где число версии законно, объявлены константой ALLOWED; .github/workflows/review.yml и claude.yml — `--allowedTools` у каждого запуска агента, и не перечисленное запрещено само",
      "why": "Ответ дополнен 17.09.2026. Назван был один список из восьми. Правило же про ПРИЁМ, и признак проблемы у него — «список пополняется после каждого инцидента»: ответ, называющий один список, не даёт следующему окну увидеть, что приём здесь устроен одинаково везде. При этом заходе гейт ответов поймал ДВА адреса, которых в нашем дереве нет: они были списаны из ЧУЖОГО ответа в сводке семьи — у соседа такой механизм есть, у нас его нет. Ровно та ошибка, от которой гейт и стоит: адрес выглядел добросовестным"
    },
    "069": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/agent_pr.py — у тела изменения два писателя, механизм и человек, и механизм не кладёт поверх свой снимок: если отметки MARK в теле нет, значит его правил человек, и sync_description не трогает его вовсе. У тела живой задачи писатель один — scripts/review_findings.py, — и он читает тело, снимает разобранные записи и дописывает новые, а не заменяет содержимое собранным заново"
    },
    "070": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/labels.yml и scripts/labels.py — вывод зоны изменения из тронутых путей по природе догадка, и поле `paths` есть ТОЛЬКО у зон, чьи файлы уже существуют: у остальных его нет намеренно, потому что зона, все пути которой указывают в будущее, дала бы молчаливый пропуск. Остаточный риск назван письменно в самом файле, а настоящий барьер рядом — метку ставит автор, и гейт сверяет её с выведенной (scripts/check_pr_meta.py)"
    },
    "071": {
      "status": "active",
      "mechanism": "document",
      "where": "scripts/review_findings.py — намеренный дубль подписан в самом модуле: раздел «ФОРМАТ СОВПАДАЕТ С СОСЕДСКИМ НАМЕРЕННО» называет, с кем дубль (каталог держит свою реализацию того же механизма), что именно совпадает (строки «НАХОДКА:», «ВЕРДИКТ:», отпечаток, строка снятия) и чем оплачено объединение — переносом записей, когда сосед станет потребителем общего механизма",
      "document_reason": "measured-refusal",
      "why": "Ответ перечитан 13.09.2026 по пункту 5.1, и признак ПРОВЕРЕН ЗАМЕРОМ. Машина повтор отличит — тем же разбором, что ищет копии чужого разбора. Замер повторяется командой — взять все отслеживаемые .md дерева, нарезать на куски по десять слов образцом scripts/check_foreign_why.py::pieces и пересечь попарно; по всем 328 документам: пар с общим куском в десять слов — 102, но живых документов среди них 22 пары, а после вычета адресов ссылок (они разбираются по словам и совпадают у всех, кто ссылается на одно правило) настоящих совпадений остаётся три, и все три — короткие формулы правил вроде «одна тема на изменение», законно повторённые в своде и в описании поведения. Ни одного дубля, требующего подписи, не нашлось. Гейт на таком предмете краснел бы на законном (051), а НАМЕРЕННОСТЬ дубля — то, что подписывает автор, — машине по-прежнему недоступна: она видит повтор, но не замысел."
    },
    "072": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_ghrest.py — две защиты на один предмет: AST-гейт test_no_mechanism_talks_to_the_platform_directly ловит намерение до запуска (импорт urllib.request мимо транспорта), живой HTTP-сервер в фикстуре ловит последствие после (разбор 404, квоты, неразобранного ответа). Каждая слепа там, где видит другая: гейт не знает, что механизм разберёт отказ неверно, фикстура не знает о механизме, который в неё не заглянул"
    },
    "073": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/ci.yml и labels-sync.yml — pip install с верхними границами: ruff<1, mypy<2, pytest<10, pyyaml<7; .pipeline.yml — диапазон совместимости с контрактом механизмов тоже с ОБЯЗАТЕЛЬНОЙ верхней границей, и scripts/pipeline_checks.py отвергает «>=X.Y» без неё: без верхней границы несовместимая версия применяется молча"
    },
    "074": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/release.py — необратимый шаг проверяется ИНВАРИАНТАМИ ЗАРАНЕЕ: заход разделён надвое, и все условия печатаются списком целиком до того, как что-либо сделано. Отвергается выпуск с грязного дерева, с уже стоящим тегом, без единого фрагмента и с номером не по правилу; мажор требует назвать первого потребителя. .github/workflows/release.yml — кнопка, и только кнопка: событие, выпускающее само, сделало бы выпуском каждое слияние. tests/test_release.py проверяет отказом каждое условие по отдельности. ПОДТВЕРЖДЁН ПРОГОНОМ 15.09.2026, а не только отказами набора: выпуск 1.1.0 прошёл кнопкой целиком — сухой заход напечатал состав, необратимый поставил тег после того, как коммит доехал, и страницу выпуска завёл механизм (автор — человек, тело — сводка и адреса источника). До того страница у тега 1.0.0 появилась раньше механизма, и его настоящего захода не было ни одного (находка e5a6c9b)."
    },
    "075": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_version.py · check_journal.py · check_pr_meta.py · ci_complete.py · build_changelog.py — отсутствие предмета даёт третий исход, а не «чисто»; tests/test_gates_reject.py прогоняет каждый такой случай"
    },
    "076": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_package_at_the_consumer.py::test_a_package_module_names_no_path_of_our_tree — ни один модуль локального пакета не называет путей нашего дерева (`tests/`, `scripts/`, `docs/`, `changelog.d/`): у ставящего пакет есть дистрибутив, а не дерево, и такая подсказка у него ложна",
      "why": "Ответ перечитан 16.09.2026, и статус сменился с «неприменимо» на «действует»: прежний ответ сам называл условие — «правило вступит вместе с пакетом», — и условие НАСТУПИЛО 15.09.2026, когда общий низ стал пакетом (решение 024). Перечитать было нечему: механизм перечитывания идёт по номеру выгрузки каталога, а не по изменениям самого проекта; нашёл это владелец вопросом о неприменимых. ЗАМЕР того же дня: нарушений ноль — ни один модуль пакета путей дерева не называет. Гейт всё равно заведён, и причина названа: класс НЕ ВИДЕН ИЗНУТРИ ДЕРЕВА — подсказка, зовущая в `tests/`, у нас разрешается, а у получателя нет, и «у автора работает» здесь штатный исход (051)"
    },
    "077": {
      "status": "not-applicable",
      "why": "проект одноязычный: строк с ключами и их переводов нет вовсе"
    },
    "078": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — отмена разобрана отдельно от отказа: погашенные группой отмены записи отбрасываются при наличии живой, но если отменены ВСЕ записи имени, это отказ, а не «зелено»; tests/test_gates_complete.py держит оба конца. scripts/main_red.py — вторая половина, и она про признак нарушения самого правила: `cancelled` не попадает ни в красные, ни в перезапускаемые, то есть автоматика НЕ ПОВТОРЯЕТ того, что человек остановил; держит tests/test_main_red.py",
      "why": "Ответ дополнен 17.09.2026. Назван был только сборщик вердикта — а признак нарушения у правила другой: «автоматика повторяет то, что человек остановил». Замер того же дня: перезапуск одиночного красного отменённые прогоны не берёт вовсе, и эта половина ответом не называлась"
    },
    "079": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/review_findings.py — уборка реестра находок читает слитое ОТ СВОЕЙ ОТМЕТКИ «Убрано до: #N», а не по окну последних тридцати закрытых: срок жизни снятия считается от того, когда его ПРОЧЛИ, а не когда опубликовали. Отметку двигает только прочитанное, и записывает её вызывающий — заход, не дошедший до записи, не объявляет прочитанным то, чего не унёс. Окно осталось пределом ЗАПРОСА: если за заход слито больше страницы, о неувиденном говорится вслух (045). Держит tests/test_review_findings.py — test_a_resolution_is_read_from_the_sweep_mark_not_a_fixed_window, test_the_sweep_mark_survives_a_round_trip и test_a_full_page_beyond_the_sweep_mark_is_said_out_loud. ЧТО ОСТАЛОСЬ ВЫТЕСНЕНИЕМ ПО ПОЗИЦИИ: раздел «Красное, пережившее слияние» в scripts/main_red.py — зеркало окна слитого, и запись оттуда исчезает вместе с уходом изменения из окна. Это объявлено в теле самой задачи вместе с выходом: «если запись нужна дольше, из неё заводят задачу». Зеркало по устройству выводится из живых артефактов (049), и накопителем его делать — отдельное решение, а не следствие этого",
      "why": "Ответ пересмотрен 16.09.2026 и сменён с «неприменимо». Прежняя причина — «срока хранения записей проект нигде не задаёт» — была неверна: вытеснение есть, и считалось оно по ПОЗИЦИИ в очереди слияний, то есть от публикации записи. Признак правила наблюдался дважды за сутки: снятие находки `a98cee5` (#348, 14.09) ушло за окно раньше, чем его прочла уборка, и запись осталась висеть неразобранной; красное на голове `229fb95` исчезло из реестра, когда общая ветка уехала. Проверено прогоном по дереву, а не чтением: ghrest.MERGED_WINDOW читают четыре механизма (044)"
    },
    "080": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".rules/proposals.json — правила, родившиеся здесь, уезжают в каталог ФАЙЛОМ, который каталог опрашивает сам, а не памятью окна; tests/test_bindings.py держит форму: все поля контракта, инцидент с конкретикой и след, разрешимый в этом дереве (044). scripts/drift.py, источник «вердикты по предложениям» — принятое каталогом перестаёт быть предложением: у него появился номер, и отвечают по нему в bindings, а не в очереди на приём; отвергнутое несёт причину. Вердикт выносит каталог у себя и по своему расписанию, события об этом не приходит — потому это дрейф. Приём взят у грейдера (162). ИСТОЧНИК ПОДТВЕРЖДЁН ПРОГОНОМ, А НЕ ОБЪЯВЛЕН (139): 11.09.2026 он прочитан против живого ответа каталога и назвал все четыре принятых предложения — правила 198, 199, 200, 201. До этого замера ответ здесь стоял «pipeline/active» на непроверенном источнике, и разбор формы ответа в нём был выдуман: вердикты искались под ключом «proposals», номер в полях «number»/«id». Форма снята с каталога и лежит в tests/fixtures/catalogue-proposals.shape.json"
    },
    "081": {
      "status": "not-applicable",
      "why": "чужой код проект не исполняет; действия каталога подключаются ссылкой на версию, а не копией"
    },
    "082": {
      "status": "active",
      "mechanism": "gate",
      "skill": ".claude/skills/role-coverage",
      "where": "docs/roles.md — карта направлений: по каждому вопросу правила один из объявленных исходов, и восемь из них помечены «роли нет». Полноту карты против самого правила и форму записи держит tests/test_roles_map.py: список направлений читается из файла правила в каталоге, адрес файла берётся из выгрузки, молчание канала — свой исход, а не зелёное. Обход АРТЕФАКТОВ — первый способ правила — уже стоял тремя гейтами и не был подписан этим ответом: tests/test_docs_shape.py требует «Читатель:» у документа, tests/test_pipeline_checks.py отвергает совещательную проверку без адресата, tests/test_decisions_format.py — читателя у записи решения. Вторая половина, обход ВОПРОСОВ, гейтом не берётся: её предмета в дереве нет по построению, и держит её навык .claude/skills/role-coverage — он читается в момент вызова. Навык не краснеет, и это объявлено в нём самом, а не умолчано (002)",
      "why": "Ответ пересмотрен 16.09.2026 и сменён с «неприменимо». Прежняя причина — «состава ролей нет: исполнителей двое» — отвечала на вопрос о ШТАТЕ, а правило спрашивает о вопросах: «роль — это не должность, а вопрос, который кто-то обязан задать», и «покрытие не равно отдельной роли: направление может быть профилем существующей». Премиса была догадкой, и держалась она до первого чтения самого правила (044). Замер того же дня: у всех 35 документов проекта объявлен читатель, у всех десяти совещательных проверок — адресат. То есть первый способ правила проект уже исполнял, не зная об этом; карта назвала одиннадцать мест, где вопрос не задаёт никто"
    },
    "083": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/review_findings.py — приёмка вывода ревьюера идёт СВОЙСТВАМИ, а не сверкой с эталонным ответом, которого у недетерминированного компонента нет. Свойства держит tests/test_review_findings.py: число в строке вердикта обязано сойтись с числом строк находок, и расхождение объявляется наружу аннотацией (test_a_verdict_that_disagrees_with_its_list_is_announced); вес берётся из закрытой шкалы, слово вне её не приводится к ближайшему, а объявляется «без веса» (test_a_weight_outside_the_scale_is_not_a_weight); отпечаток находки не зависит от переносов и пробелов, то есть переформулировка того же заголовка даёт ту же запись (test_fingerprint_survives_reflow). Выборка — вторая половина правила — читается человеком: находки разбираются по одной, и премиса каждой проверяется до починки (044)",
      "why": "Ответ пересмотрен 16.09.2026 и сменён с «неприменимо». Прежняя причина — «недетерминированного компонента в механизмах нет: вердикт ревьюера — вход, а не предмет проверки» — разводила вход и предмет там, где правило их не разводит: оно как раз про приёмку того, что пришло от недетерминированного источника. Ревьюер — часть конвейера, его вывод механизмы читают и записывают в реестр, и проверяют они его ровно свойствами. Правило применимо и уже соблюдено; ответ отвечал на другой вопрос (044)"
    },
    "084": {
      "status": "active",
      "mechanism": "gate",
      "where": ".pipeline.yml — класс проверки объявлен данными: у review и debt он advisory, и красное там слияния не держит; scripts/ci_complete.py выносит вердикт по обязательным, а совещательное красное помечает и печатает с адресатом. Тот же приём у открытия изменения: scripts/agent_pr.py без MERGE_QUEUE_TOKEN отдаёт третий исход, и .github/workflows/agent-pr.yml разбирает его как «не настроено», а не как поломку изменения — иначе краснела бы каждая ветка ни за что. НАЗВАННОЕ ОТСТУПЛЕНИЕ: правило требует «повторов нет», а .rules/rerun.json один повтор разрешает. Противоречия здесь нет, и молчать о нём нельзя (154): повтор идёт на ОБЩЕЙ ВЕТКЕ после слияния, основную работу он не задерживает ни на секунду, список разрешительный, и имя попадает в него только если красное этой проверки НЕ означает дефекта дерева. Обязательных там нет и быть не может. Разбор — docs/decisions/025-a-lone-advisory-red-is-rerun-once.md"
    },
    "085": {
      "status": "active",
      "mechanism": "gate",
      "where": "Две половины, и обе названы. КАНАЛ УКАЗАНИЙ: каждый шаг, зовущий модель, объявляет чужой текст данными — .github/workflows/review.yml и task-items.yml оговоркой в промпте, claude.yml надбавкой к системному промпту (там промпта нет: задачу ставит сам текст обращения). Держит tests/test_review_safety.py::test_every_model_call_declares_its_input_as_data; образец пометки и список зовущих действий взяты у check_untrusted_prompt.py каталога, чтобы два понимания одного правила не разошлись (090). КАНАЛ ИСПОЛНЕНИЯ: список инструментов агента закрыт и без голого Bash, у ревью нет права записи, а недоверенный ввод не попадает в текст команды — это наша половина, у каталога её нет. Послушание модели не держит никто, и это признано вслух (182)."
    },
    "086": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/findings.py — WEIGHTS: шкала закрытая, три градации, как роды у фрагментов журнала; вес живёт в самой записи живой задачи и читается строкой (ENTRY_RE). scripts/review_findings.py — вес берётся из строки находки, слово вне шкалы весом не считается и к ближайшему НЕ приводится, а неназванный записывается как «без веса»: подстановка самого лёгкого решила бы за ревьюера в сторону, удобную разбирающему (154). Порядок записей — по весу, а не по приходу (053). Вторая половина правила держалась и раньше: тяжесть ставит не тот, кто нашёл, — ревью записывает, разбирает и отклоняет окно, и отклонение оставляет след (026). Держат tests/test_review_findings.py::test_a_finding_without_a_weight_says_so, ::test_a_weight_outside_the_scale_is_not_a_weight и ::test_the_heaviest_finding_is_written_first ОПРОВЕРГАТЕЛЬ ЗАВЕДЁН 16.09.2026, и это вторая половина правила: шаг 12f `verify` в .github/workflows/review.yml входит от ОДНОЙ находки и пытается опровергнуть её премису, отвечая словом из закрытой шкалы (scripts/review_findings.py — premise_of, verified). Прежде шкала была, а отдельного проверяющего с установкой «опровергай по умолчанию» не было: замер того дня — находка `1fd43a4` описывала дефект, закрытый двумя днями раньше, и заметил это человек. Ответ верификатора снятием не является: находку снимает работа (154)."
    },
    "087": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/review_findings.py — повторный проход не переоткрывает найденное: у каждой записи отпечаток от заголовка, повтор в тело задачи не дописывается, а снятая запись уносится строкой «Разобрано: <отпечаток>» в теле изменения. Держит tests/test_review_findings.py. Вторая половина правила — подавать ревьюеру прошлые находки на ВХОД — пока не сделана: он их не видит, и защищает от переоткрытия только отпечаток"
    },
    "088": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_reject.py — каждый гейт прогоняется тем, что он ОБЯЗАН отвергнуть: это и есть вопрос критика «где заявленный метод оказался мнимым», заданный машинно. tests/test_outcomes_run.py — прогнан каждый ОБЪЯВЛЕННЫЙ исход, включая третий: фаза, не покрывшая свой же список состояний, краснеет. docs/roles.md — направление «Критик метода» значится профилем окна, а не отдельным исполнителем. Приём «проверено откатом» обязателен в теле изменения и в пояснении механизма: заявление «гейт работает» не принимается без прогона на подделанном входе (140, 145). ПРЕДЕЛ НАЗВАН: сами проверки набора отказом не прогоняются — подделанного входа у них нет, — и 16.09.2026 это дважды стоило зелёной впустую проверки; обе поймал откат рукой, а не набор. Закрывать это мутационным прогоном сегодня не на чем, и пробел объявлен, а не выровнен молчанием (046)",
      "why": "Ответ пересмотрен 16.09.2026 и сменён с «неприменимо». Прежняя причина — «фаз с отдельным критиком метода нет: работа идёт задачами, а не фазами аудита» — отвечала про ШТАТ, ровно как отвергнутая в тот же день причина по 082: правило спрашивает не «кто этот проверяющий», а «задан ли вопрос о методе». Признак нужды, названный самим правилом, в тот день был налицо: фаза называется дорогим словом («проверено откатом»), а след её дважды оказался пуст — тест области разбора счёта осечек и первая редакция гейта дорожной карты были зелены при снятом предмете (044, 075)"
    },
    "089": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/badges.yml и scripts/build_facts.py — витрина собирается ИЗ источника и толкается в ветку-сироту badges; обратного хода нет. README.md называет адрес витрины потребителю (соседу, каталогу), а читателя дерева за содержимым туда не отправляет: состав классов проверок он берёт из .pipeline.yml, ответы каталогу — из .rules/bindings.json"
    },
    "090": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/ci.yml — джоб attribution подключён действием каталога, а не переписан здесь; packages/transport/ghrest.py — общий транспорт вместо пяти копий, и tests/test_ghrest.py краснеет на появлении шестой"
    },
    "091": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/debt.py — шаг долга печатает источники в порядке контура 1: конфликт на своём изменении (1) и красное на нём (2) идут ПЕРВЫМИ, затем находки (3), затем правила (5). До этого источники 1 и 2 механизма не имели вовсе: открытое красное видел только тот, кто откроет список изменений глазами. Тревога о застрявшем остаётся предметом службы (docs/decisions/004-schedules-stay-service-observes.md) — здесь читается СВОЁ состояние, а это порядок работ, а не тревога. Плюс AGENTS.md и docs/behaviour.md — контур 1."
    },
    "092": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/review_findings.py — находки внешнего взгляда живут в живой задаче-адресате, переживающей слияние, и у каждой есть вес и отпечаток; scripts/debt.py печатает порядок их разбора вместе с остальными источниками контура 1 (091), а сам порядок объявлен в docs/behaviour.md. «Что не так» и «что за чем» разведены намеренно: первое — данные механизма, второе — договор, и смешавшись, они перестают отвечать на оба вопроса (022)."
    },
    "093": {
      "status": "active",
      "mechanism": "gate",
      "where": "Ответ разделён надвое (182). ШОВ остаётся суждением: «известна ли развилка» — знание о будущем, и машине оно недоступно. ТРЕТИЙ СЛУЧАЙ держится машиной — tests/test_third_case.py: форма тела функции без имён, повторённая трижды, обязана быть либо обобщена, либо записана в KEPT_APART с причиной (154); «иногда три разных лучше одного натянутого» — слова самого правила, поэтому гейт держит РАЗГОВОР, а не единственный исход. Замер 13.09.2026 по всем 473 функциям дерева (порог — пять операторов в теле): форм, повторённых трижды, — одна, и ею оказалась работа той же смены (drift.py, три чтения наших файлов); дважды повторённых — четыре, и они законны. Третий случай не находится глазом: первые две реализации помнит один заход, третью пишет другой через смену, и она выглядит первой. Найденная тройка обобщена в scripts/drift.py (ours_file), список исключений пуст."
    },
    "094": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/agent-pr.yml — приставка ветки теперь одна: `agent/**`. Вторая, `claude/**`, стояла переходной заглушкой (облачному окну имя ветки назначает платформа) и снята после того, как новый путь подтверждён прогонами, а не когда был написан. Возврата заглушки не будет тихого: tests/test_gates_contract.py::test_branch_prefixes_match_the_workflow сверяет список прогона со списком в scripts/agent_pr.py — разъехавшись, они дали бы худший из отказов, когда прогон стартует, а изменение не открывается и красного нигде нет"
    },
    "095": {
      "status": "not-applicable",
      "why": "сбора данных о пользователе у проекта нет ни одного: механизмы читают дерево и площадку, телеметрии не отправляют, и умолчания, выгодного продукту в ущерб пользователю, взяться неоткуда. Правило вступит, если у общих механизмов появится необязательная отправка замеров наружу"
    },
    "096": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/paths.py — адрес каждого хранилища объявлен ОДИН раз и назван поимённо, а полноту списка держит tests/test_settings_anchor.py::test_the_declared_list_covers_every_address: хранилище, не названное здесь, механизм строить не должен (068, 115). Разделение идёт именно по жизни данных: .rules/ — девять файлов, по предмету на файл (расписания, перезапуск, защита, ряд прогонов, исключительные утверждения); changelog.d/ — фрагменты до выпуска, changelog.d/released/ — после; живые задачи площадки — состояние, переживающее слияние. Разницу способов записи держат сами механизмы: раздел-ЗЕРКАЛО пересобирается из живых артефактов каждым заходом (scripts/main_red.py, 049), раздел-НАКОПИТЕЛЬ копится и объявляет себя накопителем (scripts/unlooked.py, счёт осечек канала)",
      "why": "Ответ пересмотрен 16.09.2026 и сменён с «неприменимо». Прежняя причина — «своего хранилища данных нет: состояние живёт в трекере и в дереве» — отвечала про СУБД, а правило спрашивает, не слиты ли в одно место данные с разной жизнью. Проект их разделяет и разделяет намеренно: смешать зеркало с накопителем один раз уже пытались, и разница между ними названа в теле самих задач. Правило применимо и уже соблюдено (044)"
    },
    "097": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_reject.py — ложное «прошло»: гейт обязан отвергнуть подделанный вход; tests/test_gates_complete.py — ложное «не прошло»: здоровый вход обязан пройти. Обе ошибки заведены отдельными тестами, а не одной проверкой «работает»"
    },
    "098": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/pipeline_checks.py и .pipeline.yml — единица ответа проекта о проверках взята по УПОТРЕБЛЕНИЮ: один ответ на ДЖОБ, потому что именно имя джоба выбирает защита ветки, а не на прогон и не на шаг. Держит tests/test_gates_contract.py::test_job_name_equals_context_name; матричные имена в обязательные не попадают никогда (test_summary_is_not_a_matrix) — там одна запись даёт много имён, и единица распадается"
    },
    "099": {
      "status": "active",
      "mechanism": "document",
      "where": ".rules/bindings.json, поле «_зачем» — конфликт «где живёт ответ по правилу: у каталога или у проекта» разрешён по ПОСЛЕДСТВИЮ, а не по правильности: у каталога раздел без ответов остаётся осмысленным, у проекта без них пустеет предмет — здесь живёт механизм, и одно правило в разных проектах держится по-разному. Так же разведены docs/pipeline.md и docs/behaviour.md: состав и поведение",
      "document_reason": "measured-refusal",
      "why": "Ответ перечитан 13.09.2026 по пункту 5.1, и признак ПРОВЕРЕН ЗАМЕРОМ. Практическая граница правила «проверка после слияния: не опустел ли какой-то раздел» измерима, и замер сделан: из шести объявленных родов записи журнала пусты ноль… кроме одного — `removed` (0 записей при 173 fixed, 84 added, 26 contract, 20 changed, 14 internal). Пустота эта ЗАКОННА: проект за всю историю ничего не снимал с договора, и повода для рода просто не было. Отличить «раздел опустел от неверной классификации» от «повода не было» машина не может — она не знает, должен ли раздел иметь предмет, — и гейт краснел бы на законном (051). Сам же конфликт классификации разрешается по ПОСЛЕДСТВИЮ пустого раздела, а это суждение о последствии, а не о данных."
    },
    "100": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — дедлайна два, и они разной длины: отсутствие записи проверки на голове означает «прогон не стартовал» и отвергается СРАЗУ, не дожидаясь предела ожидания, а предел (--timeout) относится только к тем, кто уже идёт. Третий, на работу джоба, задан в .github/workflows/ci.yml полем timeout-minutes у каждого шага"
    },
    "101": {
      "status": "active",
      "mechanism": "gate",
      "where": "packages/transport/ghrest.py — постоянный отказ отделён от временного и не повторяется: 404 — свой род, 401/403 без заголовков квоты — «причина другая», а не лимит. Исчерпанной квоте механизм не назначает своей паузы: срок берётся из retry-after либо x-ratelimit-reset площадки. Держат tests/test_ghrest.py: test_exhausted_quota_is_its_own_kind, test_forbidden_without_quota_headers_is_not_rate_limit, test_retry_after_alone_is_enough_for_rate_limit"
    },
    "102": {
      "status": "active",
      "mechanism": "gate",
      "where": ".rules/leniency.json — таблица послаблений: что именно прощается, где, почему и чем замерено; пять записей. Держит её tests/test_leniency.py: у каждой строки есть причина и замер с днём, имя из строки живёт по названному адресу (переименование расходится с таблицей молча), и всякое пословное сходство в scripts/ обязано быть объявлено — разбор ищет объемлющую ФУНКЦИЮ вызова, а не файл. Строгий режим — `scripts/review_findings.py --strict`: сверяет заголовки дословно и выключает и сходство, и совпадение адресов; проверяется он прогоном на замеренной паре (сходство 0.923 при пороге 0.85), а не наличием флага, и отдельно проверяется, что режим доходит до поиска записи в реестре. ПРЕДЕЛ НАЗВАН в самой таблице: полноты по нормализациям гейт не держит — предикат «это сравнение с послаблением» по дереву не отличим от обычного приведения регистра, и новая нормализация может не попасть в таблицу молча (046)",
      "why": "Ответ пересмотрен 16.09.2026 и сменён с «неприменимо». Прежняя причина — «сравнений с послаблениями проект не делает: гейты сверяют имена, статусы и адреса точно» — была неверна фактически: в дереве пять послаблений, и сильнейшее из них решает, считать ли две находки одной. Цена ошибки здесь несимметрична: лишняя запись стоит строки в реестре, слипшиеся находки — потерянной находки. Проверено прогоном по дереву, а не чтением (044, 005)"
    },
    "103": {
      "status": "not-applicable",
      "why": "сторожа побочных эффектов у проекта нет: ни один механизм не сравнивает снимки «до» и «после» и никому ничего не приписывает. Механизмы читают площадку и дерево, а пишут только туда, куда их позвали. Правило вступит вместе со сторожем, который начнёт следить за изменениями состояния во время прогона"
    },
    "104": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows — ручная кнопка `workflow_dispatch` есть у каждого прогона, кроме claude.yml, который отвечает на обращение и запускается им: agent-pr.yml · automerge.yml · badges.yml · ci.yml · labels-sync.yml · main-red.yml · required-context.yml · review.yml · rules-inbox.yml · task-items.yml. У кнопки ревью и разбора задачи есть ВХОД — номер уже слитого изменения: кнопка без предмета смотрела бы в текущую ветку и ревью не заменяла"
    },
    "105": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/review.yml — внешний взгляд ведёт отдельный прогон с закрытым списком инструментов и без истории окна, а не второй заход того же окна по своему же коду. Разбор образцов и что взято у каждого соседа — docs/decisions/002-review-taken-from-two-neighbours.md. Прогонять проверки роль умеет с 09.09.2026: у джоба есть интерпретатор, установка проверок и разрешение обеим формам команды, и держат это прогоны tests/test_review_safety.py (ответ по 032). Слепое пятно ответа названо иначе: прогнала ли роль на самом деле, из дерева не прочесть — это её собственный отчёт."
    },
    "106": {
      "status": "active",
      "mechanism": "gate",
      "where": "БУКВА ПРАВИЛА — «сначала настоящий прогон, потом огласка», и на витрине это значит: значок не показывают, пока за ним нет работающего механизма. tests/test_showcase.py именно это и держит: у вопроса посетителя значок обязан быть И показан в README, И собираться scripts/build_facts.py — значок, которого никто не собирает, застывает, а с витрины застывший и отсутствующий неотличимы. Гейт поймал это на первом же заходе: значок выпуска собирался, а в витрине его не было. Набор вопросов один на семью и взят у каталога (.rules/showcase.json), у которого это правило держится тем же приёмом (162)."
    },
    "107": {
      "status": "active",
      "mechanism": "document",
      "where": "docs/decisions/005-what-of-the-graders-transport-is-transport.md — чужой механизм разобран на то, что у соседа транспорт, и то, что его собственный предмет: перенос вслепую дал бы «у автора работает» на нашей выборке. Так же docs/decisions/002: форма спрошена у ОБОИХ соседей, потому что каждый проверен только на том, что было у него",
      "document_reason": "measured-refusal",
      "why": "Ответ перечитан 13.09.2026 по пункту 5.1, и признак ПРОВЕРЕН ЗАМЕРОМ. Практическая граница правила «первоисточник указывается» соблюдается: из 18 решений проекта 17 опираются на чужой опыт, и первоисточник назван у всех — нарушений ноль, то есть гейту нечего ловить (075). Проверять это машиной пришлось бы по прозе: решение может заимствовать, не называя соседа словом, а упоминание соседа не всегда заимствование, — и красное шло бы на стиль, а не на предмет (051). ГЛАВНОЕ ЖЕ НЕ В ЭТОМ: границы чужой выборки машине неизвестны. Что у соседа проверено, а что просто не ломалось, знает только тот, кто заимствует, — и называет это он, разбирая чужой механизм на транспорт и собственный предмет (docs/decisions/005, 002)."
    },
    "108": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_changelog.py — обе половины правила. Вышедшее переезжает ДОСЛОВНО: при выпуске фрагменты уходят в changelog.d/released/<версия>/ и остаются источником, а не сокращаются. Предел теперь есть и стоит у ПРЕДСТАВЛЕНИЯ, а не у источника: развёрнутыми собираются UNFOLDED_RELEASES последних выпусков, остальные названы ссылкой на свой каталог со счётом записей — оборвать раздел молча значило бы читаться как «раньше ничего не было». Держат tests/test_gates_reject.py::test_the_journal_unfolds_a_bounded_number_of_releases и ::test_a_short_history_folds_nothing"
    },
    "109": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_states.py — таблицы состояний трёх контуров в docs/behaviour.md разбираются механизмом: у каждого состояния колонка выходов непуста, состояние без выходов названо терминальным СЛОВОМ, а имя после стрелки обязано быть состоянием той же таблицы. Последнее и есть предмет правила: выход в несуществующее состояние выглядит описанным переходом и им не является. Переход в другой контур назван его словом и проверяется чтением — это объявленная граница гейта. Подтверждён двумя отказами (140): снятое слово «терминальное» и переименованное состояние в выходе."
    },
    "110": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/agent_pr.py — до первого обращения к площадке разобран вход и проверено всё, что может отказать: названы ли репозиторий и ветка, объявлена ли приставка ветки, есть ли токен владельца. Отсутствие токена даёт свой исход (EXIT_NOT_CONFIGURED) ДО открытия изменения, а не после. Тот же порядок у scripts/build_facts.py и scripts/check_version.py: пустой источник ловится до сборки. Держит tests/test_gates_reject.py"
    },
    "111": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/agent-pr.yml — конвейер сам открывает изменение и сам проставляет зоны, вместо того чтобы советовать окну сделать это; tests/test_ghrest.py держит запрет на вторую реализацию транспорта и чтения состава"
    },
    "112": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_package_at_the_consumer.py::test_a_package_module_leaves_nothing_on_the_consumer_machine — модули пакета не заводят файлов на чужой машине: ни записи, ни каталога, ни временного файла. Формы названы поимённо, а не запретом всякого обращения к файловой системе: чтение законно",
      "why": "Ответ перечитан 16.09.2026 вместе с 076 и по той же причине: он называл условие «правило вступит вместе с пакетом», условие наступило 15.09.2026, а перечитать было нечему. ЗАМЕР: пакет не пишет на диск ничего — единственные обращения наружу это чтение по сети. Правило требует: кеш или конфиг, заведённый установкой, обязан сниматься той же командой. Проще этого не заводить вовсе, и гейт держит именно это"
    },
    "113": {
      "status": "active",
      "mechanism": "gate",
      "where": "docs/release.md — правила эволюции контракта записаны ДО первого потребителя: что считается несовместимым, как идёт миграция, чем отвечает потребитель. scripts/check_contract.py — гейт: правка поверхности без объявления фрагментом рода `contract` не проходит; scripts/contract.py снимает поверхность с дерева, а не решает её на глаз; .pipeline.yml несёт диапазон совместимости с обязательной верхней границей. tests/test_contract_surface.py проверяет обе стороны отказом"
    },
    "114": {
      "status": "active",
      "mechanism": "gate",
      "where": "БУКВА ПРАВИЛА — ИДЕМПОТЕНТНОСТЬ СТУПЕНИ, и предмет у неё здесь есть: догоняющие обходы. scripts/items.py::sweep и mark — отметка применяется только к пункту, который её не проходил: уже отмеченный остаётся отмеченным, а запись не отправляется вовсе, если тело задачи не изменилось; на этом обход и держится. То же у scripts/unlooked.py::scan — отметка обхода не даёт перечитывать просмотренное, а запись снимается сама, когда вердикт появился. Безусловный прогон здесь ломал бы уже сделанное: переписывал бы задачи на каждом слиянии. Проверено tests/test_items.py::test_a_second_sweep_costs_nothing. ОБОБЩЕНИЕ — миграция потребителя от ЕГО версии, а не от нуля: scripts/check_contract.py требует у несовместимой правки назвать переход «было → стало», и docs/release.md — раздел «Несовместимое изменение»: инструкция «поставьте свежее и настройте заново» переписывает потребителю уже работающее."
    },
    "115": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/paths.py — адреса настроек объявлены одним модулем, остальные механизмы их импортируют; tests/test_settings_anchor.py держит обе половины правила: ни один механизм не строит адрес настройки сам (второй якорь заводится именно так) и ни один не ищет настройку вверх по дереву — поиск нашёл бы файл соседнего проекта и молча принял бы его за свой. Раньше ответ был «держится построением»: замер 09.09.2026 показал у CONTRACT_VERSION три независимых адреса — в build_changelog.py, build_facts.py и check_version.py. Гейт tests/test_settings_anchor.py ловит и адрес, написанный ГОЛОЙ строкой, а не только Path(«…»): второй якорь заводится и так — расходятся адреса, а не их типы (найдено на #170)"
    },
    "116": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — сборщик результатов параллельной работы сверяет КОЛИЧЕСТВО: у каждого объявленного имени обязана быть запись на голове, и отсутствие записи означает «прогон не стартовал», а не «зелено»; пустой список записей — отказ входа. Сам сборщик проверяется наравне с работой: tests/test_gates_complete.py держит его вердикт, tests/test_gates_contract.py — то, что он собран опросом, а не через needs (иначе отказ соседа стал бы пропуском)"
    },
    "117": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/review.yml и .github/workflows/claude.yml — в тексте каждого задания стоит блок «ТВОИ ЧИСЛА»: предел размера ответа и срок захода. Оба взяты у тех, кто ими владеет, а не вписаны рукой: предел находки — объявлен в scripts/findings.py константой `SAID_LIMIT` и подобран ЗАМЕРОМ по живому реестру (14 записей: медиана 138 знаков, девятый дециль 200, потолок 303 — предел поставлен по потолку, 300); срок — `timeout-minutes` того самого джоба, то есть число, которое площадка и так применит. Держит tests/test_review_safety.py::test_the_task_carries_its_numbers — сдвинутый срок джоба, сдвинутый канон предела и снятый блок чисел краснеют все три. Третий вопрос правила — список типов файлов, которые открывать нельзя, — закрыт разрешительным списком инструментов и его блоком «ГРАНИЦА ОКРУЖЕНИЯ» (061): перечисляется разрешённое, а не запрещённое (068)",
      "why": "Ответ пересмотрен 17.09.2026 и сменён с «неприменимо». Прежняя причина — «заданий исполнителям проект не выдаёт» — ложна: заданий четыре. ЗАМЕР ТОГО ЖЕ ДНЯ: чисел в них не стояло НИ ОДНОГО. Половина, названная утром долгом, построена целиком, и оба числа взяты замером или у владельца — рукописное число здесь устаревало бы молча (005). НАЗВАННАЯ ГРАНИЦА ЗАМЕРА: выборка предела тонкая, четырнадцать записей, потому что разобранное из реестра уходит обходом и в нём не копится; число подлежит пересмотру, когда выборка вырастет, и это не повод не называть его сегодня (046). Чего механизм НЕ делает: он не проверяет, что исполнитель числа СОБЛЮЛ. Бюджет обращений к инструментам в штуках не назван и назван во ВРЕМЕНИ — это единица, которую площадка действительно применяет, а число обращений нигде не записывается, и подобрать его замером сегодня не из чего"
    },
    "118": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_changelog.py — исходник не исчезает в производном: при выпуске фрагменты ПЕРЕЕЗЖАЮТ из changelog.d/ в changelog.d/released/<версия>/ и остаются источником, а CHANGELOG.md пересобирается из них локально, без похода к площадке и без чтения тегов. Держит tests/test_gates_reject.py. Правило 125 отвечено тем же адресом с другой стороны — производное не хранилище"
    },
    "119": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/journal.py — маска входа PATH_RE берёт ровно changelog.d/<слаг>.<род>.md и НЕ рекурсивна, поэтому производное самого инструмента под неё не попадает: переехавшие при выпуске фрагменты лежат в changelog.d/released/<версия>/, собранный CHANGELOG.md — в корне и в EXEMPT_FILES гейта scripts/check_journal.py, а facts.json и значок вообще живут в ветке badges, вне дерева. Держит tests/test_gates_reject.py"
    },
    "120": {
      "status": "not-applicable",
      "why": "проект ведёт механизмы, а не каталог правил: каталог — соседний репозиторий, и правила его ведения относятся к нему"
    },
    "121": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/task_shape.py::closed_with_live_units — шаг debt ведёт ревизию закрытого: называет закрытые задачи, у которых остались открытые пункты или подзадачи. Обратная сторона looks_done, у которой предмет противоположный — работа сделана, а контейнер открыт. Окно ревизии названо в самой строке: утверждение верно ровно для этих задач. Ревизия считается ДВУМЯ числами: свежее закрытое и закрытое до того, как у проекта появился счётчик пунктов (task_shape.ITEMS_SINCE — 10.09.2026, день заведения items.py). До него отметить пункты было нечем, ответ по таким задачам известен заранее, и держать их в общем числе значит держать постоянное слагаемое, которое перестают читать (051). Из ревизии они не исчезают — названы отдельно и с причиной (046). Приём тот же, что у отметки атрибуции cb26ec4"
    },
    "122": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "шаг badges — facts.json отдаёт числа сырыми, значок рядом показывает то же число отформатированным; scripts/build_facts.py собирает оба из одного источника"
    },
    "123": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/ci.yml — джоб attribution с отметкой cb26ec48dd6a53c0b47af7dd0ec7cda2e07a748b; отпечаток ЦЕЛИКОМ, а не короткий: короткий однажды перестаёт быть однозначным, и отметка тогда указывает не туда. Тот же полный отпечаток стоит в .github/workflows/ci.yml и в docs/decisions/001-attribution-of-merged-1.md — три места, одно написание. .github/authors.txt — список согласованных имён"
    },
    "124": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/main_red.py — упал ровно один обязательный джоб: перезапускается он один и ОДИН раз, номер попытки спрашивается у площадки (`run_attempt`), а не хранится, поэтому бесконечный повтор невозможен по построению; зелёное со второго раза записывается находкой о мигании в живую задачу, а не считается починкой. .github/workflows/main-red.yml — право `actions: write` объявлено ровно здесь и только здесь. tests/test_main_red.py проверяет отказом: вторая попытка, несколько красных сразу и неразобранный адрес записи дают три РАЗНЫЕ причины не перезапускать"
    },
    "125": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_changelog.py --check — CHANGELOG.md производный, собирается из changelog.d/ и changelog.d/released/, а не хранит выпущенное сам"
    },
    "126": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/automerge.py::take_back и hand_over — заморозка СНИМАЕТ взведение, а не только не выдаёт новое: взведённое площадка сольёт сама, как только проверки позеленеют, и заморозка, которая только молчит, ничего не держит. Путь разморозки — следующий заход: он видит зелёную общую ветку и взводит голову заново, то есть доказательство «условие снято» принимается от ДВУХ равносильных источников — события `workflow_run` от прогона гейтов (.github/workflows/automerge.yml) (замер 10.09.2026: 25 раз из 25, медиана 2 с) и расписания/кнопки, которые тот же заход повторяют. Ожидание зелёного внутри захода отсюда УШЛО по решению docs/decisions/011-merging-is-handed-to-the-platform.md: ждёт площадка, и ждёт бесплатно. Держит это tests/test_automerge.py — снятие при заморозке, при отзыве согласия и перед собственным слиянием проверяется тремя отдельными случаями"
    },
    "127": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_version.py — маркер с чужим значением краснеет; docs/release.md — раздел «Маркер в прозе»"
    },
    "128": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_pr_meta.py — разметка проверяется на полноту: зона выводится из путей состава и обязана стоять, а не просто «метка есть»"
    },
    "129": {
      "status": "active",
      "mechanism": "gate",
      "where": ".rules/bindings.json — ответ по каждой записи каталога; scripts/pipeline_checks.py повторяет ту же форму для классов проверок; tests/test_bindings.py держит полноту ответа. Контракт назван целиком, а не одной его частью: `.rules/bindings.json` несёт `schema` — версию формы ответа, `answers_to` — версию ВЫГРУЗКИ, по которой ответы построены, `project` и `catalogue` — обратную ссылку на источник. Все четыре сверяются с живым каталогом: scripts/drift.py::catalogue_moved печатает отставание по каждому номеру порознь, а scripts/drift.py::reader_is_behind — по номеру, под который написан наш читатель чужой сводки",
      "why": "Ответ дополнен 17.09.2026. Правило перечисляет контракт потребления по частям — «экспорт, ответ о судьбе каждого правила, обратная ссылка в задачу-источник и версия схемы», — а ответ называл только вторую. Три остальные в дереве были и стояли неподписанными"
    },
    "130": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/rules-inbox.yml — новое правило каталога приходит не в чужой репозиторий, а ЗАДАЧЕЙ в трекер этого проекта, вместе с тремя числами долга по 177: без ответа, действующих без механизма, и разошедшийся контракт. Вторая половина правила — подбирать к новому правилу кандидатов из открытых задач — пока не сделана: список кандидатов проходная не собирает, и связывает правило с предметом окно"
    },
    "131": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/agent-pr.yml — изменение открывает конвейер токеном владельца; scripts/agent_pr.py не переходит на токен прогона даже при отсутствии секрета"
    },
    "132": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_journal.py::say_if_compound — изменение, везущее больше одной записи журнала НАРУЖУ, получает предупреждение: две записи — это два «зачем», объявленные самой работой, а не догадка машины о смысле правки. ПРЕДУПРЕЖДЕНИЕ, А НЕ ОТКАЗ — требование самого правила: широкая тема неделима, и ложный отказ на ней дороже пропуска (051, 084). Замер 13.09.2026 по 120 слитым: одна запись наружу у 101 изменения, две — у шести, четыре — у одного. ПЕРВЫЙ ПРИЗНАК ЗАМЕР ОТВЕРГ: зоны (area/*) вложены друг в друга — правка automerge.py даёт разом area/gates и area/merge, — и порог в четыре зоны дал бы пять ложных срабатываний из 120 и ни одного истинного (044). Внутренняя запись второй темой не считается: ею сопровождают чужую работу. «Одна ли это тема» по-прежнему решает автор — механизм только спрашивает; пересечение файлов как более сильная граница — ответ по 133. Прогоны — tests/test_gates_reject.py, обе стороны: предупреждение есть и отказа нет."
    },
    "133": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md — граница изменения задаётся пересечением файлов",
      "document_reason": "measured-refusal",
      "why": "Ответ перечитан 13.09.2026 по пункту 5.1, и признак ПРОВЕРЕН ЗАМЕРОМ, а не объявлен невозможным. Машинная половина считается: файлы связаны, если тронуты одним коммитом, и изменение распадается на компоненты связности. Замер повторяется командой — по последним 25 слитым первопредкам взять `git show --name-only --format= <sha>`, связать файлы, тронутые одним коммитом ветки, и посчитать компоненты: распались 6, и распались бы так же без журнала и ответов каталогу, то есть признак ловит не оформление, а настоящее смешение предметов. Гейтом он всё равно не становится: решение 008 разрешает везти хвост мелких правок одним изменением, и сигнал срабатывал бы на каждом четвёртом заходе, большинство из которых законны. Проверка, краснеющая на законном, приучает себя обходить (051) — поэтому предел здесь measured-refusal, а не impossible: половина есть и отвергнута замером. Граница остаётся решением автора ДО того, как файлы тронуты."
    },
    "134": {
      "status": "active",
      "mechanism": "document",
      "where": "AGENTS.md и CLAUDE.md — свод существует до того, как в проект придёт следующее окно",
      "document_reason": "measured-refusal",
      "why": "Ответ перечитан 13.09.2026 по пункту 5.1, и признак ПРОВЕРЕН ЗАМЕРОМ. Половина правила — «свод существует до того, как придёт следующее окно» — держится: AGENTS.md и CLAUDE.md есть, и форму их держат гейты (читатель — 021, канон — 022, предел — 029). Вторая половина проекту НЕ ВИДНА в принципе: что окно прочитало при старте и подействовало ли на эту сессию наполнение, сделанное в её середине, из дерева не читается. Замер: 38 веток окна и 338 слитых изменений — и ни по одному из них дерево не знает, что окно читало; счётчика сессии площадка не даёт вовсе. Возраст окна при этом с 14.09.2026 измерим по трейлеру `Claude-Session` (scripts/check_window_lifetime.py), и это ничего здесь не меняет: след говорит, КАКОЕ окно работало и сколько, а не ЧТО оно прочитало — принять одно за другое значило бы выдать проверку наличия документов за проверку прочтения (046). Механизм здесь был бы проверкой наличия документов, которая уже стоит, под именем проверки прочтения, которой быть не может (046)."
    },
    "135": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/attribution-history.yml — личность записи узнаётся ЗАПИСЬЮ: первопредками общей ветки, а не тем, что окно объявило о себе. Гейт на изменении этого не видит по построению — он спрашивает коммиты ветки, а в общую ветку едет уплотнение, тело которому составляет очередь. Замер 10.09.2026: из ста первопредков после объявленной отметки шестнадцать шли без строки соавтора, и все они старше того дня, когда очередь научилась переносить трейлеры. Отметка `since` объявлена в прогоне: старый хвост переписать нечем, а вечное красное — фон, а не сигнал (051). Плюс CLAUDE.md, раздел «Подпись и открытие изменения»."
    },
    "136": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/review_findings.py — вердикт сверяется с ПЕРЕЧИСЛЕНИЕМ: «находок N» и число строк находок обязаны сойтись, иначе расхождение уходит аннотацией, которую отдаёт REST. Держит это tests/test_review_findings.py.",
      "why": "Ответ перечитан 13.09.2026 по пункту 5.1 и РАЗДЕЛЁН НАДВОЕ (182). Машинная половина: вердикт не расходится с перечислением — это считается и говорится наружу. Человеческая: полон ли сам перебор предметов правила, машине недоступна — она не знает, какие предметы у правила есть. Прежний ответ называл только вторую половину и потому объявлял механизм невозможным целиком."
    },
    "137": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_facts.py — пустой источник ловится на СЫРОМ содержимом файла до всякой сборки: «пуст — это ошибка входа, а не „версии нет“». Тот же порядок в scripts/check_version.py и scripts/labels.py. Числа витрины считаются по источнику (.rules/bindings.json, .pipeline.yml), а не по собранному facts.json, — иначе пустой разбор дал бы правдоподобный ноль. Держит tests/test_facts.py и tests/test_gates_reject.py"
    },
    "138": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_journal.py — фрагмент журнала едет ВМЕСТЕ с изменением, а не пишется при выпуске по памяти; changelog.d/README.md — форма записи; scripts/release.py собирает журнал из накопленных фрагментов, а не из воспоминаний. tests/test_gates_reject.py проверяет отказом изменение без фрагмента"
    },
    "139": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_bindings_addresses.py::test_a_gate_names_something_runnable — ответ «держится гейтом» обязан назвать адрес ИСПОЛНЯЕМОГО (.py, .yml, .yaml), а не документа: документ прогоном не подтверждается вовсе, он не запускается, и ответ, называющий гейтом абзац в своде, обещает проверку, которой нет. Список видов закрытый — «похоже на путь» приняло бы и документ. Вторая половина — tests/test_gaps_section.py: строка уходит из раздела пробелов, когда механизм подтверждён прогоном, а счёт в ней сверяется с ответом каталогу. Форма взята у каталога, где список видов тоже закрытый (162)."
    },
    "140": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_reject.py — набор проверок на то, что механизмы обязаны отвергнуть; tests/test_gates_complete.py — вердикт сводного гейта на пропуске, отмене и отсутствии записи"
    },
    "141": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/findings.py — живая задача находится по целой строке-маркеру, а не по началу заголовка. Имена джобов сверяются равенством, и это держит tests/test_gates_contract.py::test_job_name_equals_context_name: имя джоба и имя контекста совпадают целиком, иначе в защите ветки окажется имя-призрак. Четыре места, где совпадение по началу нужно НАМЕРЕННО, названы этим в самом имени константы: PREFIXES в scripts/agent_pr.py, ZONE_PREFIX в scripts/labels.py, ALLOWED_PREFIXES в scripts/check_version.py, EXEMPT_PREFIXES в scripts/check_journal.py"
    },
    "142": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/review_findings.py — находки внешнего взгляда уезжают в живую задачу-адресата и снимаются строкой «Разобрано: <отпечаток>» в теле изменения; scripts/unlooked.py — сам факт отсутствия взгляда; scripts/main_red.py — краснота общей ветки, разведённая по классу проверки; .pipeline.yml — у КАЖДОЙ совещательной проверки объявлено поле `addressee` с разрешимым адресом, и scripts/pipeline_checks.py отвергает ответ без него: красное, которому негде пережить слияние, делает совещательную проверку вежливо выключенной. tests/test_pipeline_checks.py проверяет это отказом"
    },
    "144": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/changerefs.py — разбор прозы идёт по СТРУКТУРЕ разметки, а не по пунктуации: границей служат строка и забор кодового блока, а точка внутри имён (`v1.2.0`, `re.finditer`, `SECURITY.md`) ничего не режет. Держит tests/test_changerefs.py — три находки внешнего взгляда подряд пришли именно отсюда: непарная кавычка, незакрытый забор и два незакрытых забора в разных коммитах"
    },
    "145": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_complete.py и tests/test_gates_reject.py — прогнан каждый объявленный исход, включая третий"
    },
    "146": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_platform_shape.py — подделки ответа площадки сверяются со СНЯТЫМ ответом (tests/fixtures/check-runs.shape.json), а не с разумением окна: зелёный набор на подделке, собранной по памяти, подтверждает согласованность кода с нашим представлением о площадке, а не с площадкой. Снимок датирован, и что он НЕ ловит — изменение самой площадки — названо в докстроке, а не выдано за полноту"
    },
    "147": {
      "status": "active",
      "mechanism": "gate",
      "where": "Переключатель есть, и у отмены есть действие, а не только смысл. Согласие `automerge` ставит МЕХАНИЗМ при открытии (scripts/agent_pr.py): ветка с приставкой конвейера и есть заявленное согласие. Отмена выражается меткой `hold`, и она сильнее — шаг открытия, увидев её, согласие не ставит, а стоящее СНИМАЕТ: «метку ещё не ставили» и «поставили и сняли» по состоянию изменения неразличимы, и снятое человеком согласие вернулось бы следующим толчком. Очередь помеченное не двигает и печатает его на каждом заходе (scripts/automerge.py). Держат tests/test_agent_pr_consent.py. Чего нет: разбудить по стоящему НЕДЕЛЯМИ `hold` некому — это шаг 11, и пробел назван в AGENTS.md"
    },
    "148": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — состояние читается по ГОЛОВЕ: check-runs на коммите, а не поля записи изменения; пустой список записей означает «прогон не стартовал», а не «зелено». Отставшая ветка лечится сменой головы — подтянуть общую и дождаться прогона, — а не повтором проверки; тот же порядок у scripts/agent_pr.py, который сверяет описание с коммитами ветки, а не с телом записи"
    },
    "149": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/ — общая площадка забирается набором, а не вносится в исключения: файлы механизмов собираются в tmp_path (tests/test_facts.py, tests/test_gates_reject.py), а транспорт проверяется на СВОЁМ HTTP-сервере, поднятом на случайном порту (tests/test_ghrest.py), а не на площадке и не на фиксированном порту. Ни одного исключения по имени в наборе нет"
    },
    "150": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_reject.py — каждая проверка зовёт САМ механизм и сверяет его вердикт и код возврата, а не пересказывают условие: гейты журнала, версии и состава проверок отвечают отказом на подделанный вход. Условие механизма в утверждении теста не повторяется нигде — иначе тест краснел бы на оговорке, которой механизм не применяет"
    },
    "151": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/review.yml — второй исполнитель починки добывает след САМ: джобу ревью поставлены интерпретатор и те же версии проверок, что в гейтах изменения, а список разрешённого называет оба имени интерпретатора — разрешение выдаётся по началу команды, и `python3` там другая строка. Держат tests/test_review_safety.py::test_a_permitted_run_is_actually_possible (что список разрешает запускать, то прогон обязан поставить) и ::test_both_interpreter_names_are_permitted. Подсказка требует назвать исход прогона словами — «проверки прогнаны» или «прогнать не удалось, вот почему»: вердикт по прогону и вердикт по чтению стоят разного, а выглядят одинаково (045). Подтверждено прогонами: отчёты ревью на изменениях этой смены называют число прогнанных проверок. Чего по-прежнему нет: собственный лог прогона площадки ревьюеру не отдаётся — он воспроизводит отказ запуском, а не читает чужой след, и на отказе, не воспроизводимом локально, останется без него"
    },
    "152": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_review_safety.py::test_a_shared_caller_pins_what_it_calls — закрепление требуется ровно там, где вызывающий берётся с ОБЩЕЙ ветки: `workflow_run`, `pull_request_target`, `schedule`. На `pull_request` файл прогона берётся из самого изменения, и закрепление там ничего не добавляет — гейт этого и не требует. .github/workflows/automerge.yml · main-red.yml · rules-inbox.yml закреплены по SHA; тег действия каталога — не подвижная метка, а версия контракта, к которой проект прибит сознательно, и это объявлено списком с причиной. ЗАМЕР 10.09.2026: правило держалось в прогоне ревью, где о нём помнили, и НЕ держалось в очереди и тревоге, где оно как раз и работает"
    },
    "153": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_foreign_why.py — дословный кусок чужого разбора без ссылки на то самое правило отвергается; цитата со ссылкой законна, гейт судит потерю адреса, а не заимствование. Окно в десять слов выбрано замером 13.09.2026: на живом дереве четыре цитаты, все со ссылками.",
      "why": "Ответ перечитан 13.09.2026 по пункту 5.1: он говорил «механизм невозможен», а механизм оказался возможен — выгрузка каталога несёт разбор каждого правила, и сверить с ним свои документы можно машиной. Прежняя причина была верна лишь пока источник считался недоступным."
    },
    "154": {
      "status": "active",
      "mechanism": "gate",
      "where": ".rules/bindings.json — ПЕРВЫЙ ПРЕДМЕТ ПРАВИЛА ЭТО НАШ СОБСТВЕННЫЙ ОТВЕТ КАТАЛОГУ: у `mechanism: none` обязательны `why` и `machine_half` — что именно следует из данных целиком и почему всё-таки не построено. Держит tests/test_bindings.py::test_active_names_its_mechanism_and_address — он требует ОБА поля, `why` и `machine_half`, — и ::test_negative_answers_name_the_reason: «не держится ничем» без причины отвергается. Тот же уговор за пределами ответа: changelog.d/README.md — род internal обязан назвать причину, и scripts/check_journal.py требует фрагмент; .github/labels.yml — раздел «чего здесь нет намеренно» с причиной у каждой невзятой метки",
      "why": "Ответ дополнен 17.09.2026. Назывались ПОХОЖИЕ случаи — журнал и метки, — а буква правила говорит ровно о нашем ответе каталогу: «правило, объявленное не обеспеченным механизмом, называет почему». Ответ отвечал аналогией и обходил собственную середину. ПОПРАВЛЕНО 17.09.2026 по находке внешнего взгляда на #427: ответ утверждал, что гейт требует `machine_half` наравне с `why`, а гейт требовал только `why` — поле стояло в контракте каталога и не держалось ничем. Починен ГЕЙТ, а не ответ: требование контракта было верным, неверным было утверждение о том, что оно проверяется"
    },
    "155": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_family_pinning.py — проект раздаёт не файлы, а СПОСОБ подключения («подключение идёт версией, а не переносом файлов руками», README.md), и гейт держит, что у себя он применён тем же: каждое обращение к механизму семьи закреплено тегом vN.N.N, и версия у одного механизма одна на всё дерево. Что файл семьи не скопирован руками, машина отсюда не скажет — эту половину держит scripts/drift.py и вычитка"
    },
    "156": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/ci.yml — джоб attribution проверяет хвостовой блок трейлеров"
    },
    "157": {
      "status": "active",
      "mechanism": "gate",
      "where": ".pipeline.yml — потребитель объявляет диапазон совместимости `contract: \">=X.Y,<A.B\"`; scripts/pipeline_checks.py отвергает ответ без диапазона и контракт, вышедший за его границу, — сообщение зовёт ПЕРЕЧИТАТЬ ответы, а не подвинуть число: механическое поднятие оставляет ненужный обход жить вечно. scripts/family.py объявляет версию чужой сводки, под которую написан разрез, и её расхождение печатается рядом с числами. docs/release.md — порядок перечитывания"
    },
    "158": {
      "status": "active",
      "mechanism": "gate",
      "where": "packages/transport/ghrest.py — адрес прикрепляется в точке обращения: каждый отказ транспорта несёт метод и путь («GET repos/… → 404: …»), а не только код. Так же у гейтов: scripts/build_facts.py и scripts/check_version.py называют файл, которого не хватило, scripts/ci_complete.py — имя проверки и голову. Третий исход отделён от второго во всех механизмах и держится tests/test_gates_reject.py"
    },
    "159": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — verdict() копит находки в список по всем именам и читает его ПОСЛЕ перебора; раннего выхода нет ни одного. Тот же порядок в scripts/check_pipeline.py: findings() возвращает список целиком, и код возврата выбирается по нему, а не по первому найденному расхождению. Держит tests/test_gates_complete.py::test_both_failures_are_named — два отказа обязаны быть названы ОБА, а не «хотя бы один»"
    },
    "160": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/badges.yml — значок и facts.json пересобираются на каждое слияние, то есть чаще, чем идут изменения, и в дереве общей ветки не лежат: прогон кладёт их в ветку-сироту badges силовым толчком. Иначе каждое слияние конфликтовало бы на одном и том же файле, а сдвиг числа красил бы ветку как поломка"
    },
    "161": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_decisions_format.py — каждая запись решения обязана назвать ОТВЕРГНУТУЮ альтернативу; без неё это ход работы, и место ему в журнале, а не в истории. Форму записи держит тот же гейт: номер в заголовке, читатель, статус с датой и задачей. Ходы работы живут отдельно — фрагментами в changelog.d/"
    },
    "162": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/rules-inbox.yml — ночной прогон каталога приносит в задачу-«входящие» раздел «У соседей это уже решено»: по каждому правилу, признанному действующим и не держащемуся ничем, названы соседи по своду и АДРЕС их механизма. Вторая ось — scripts/family.py: разрез по сводке каталога показывает, что соседи держат машиной там, где мы отвечаем документом; из него вырос список переноса (#117). Спрашивается сводка каталога, а не поход по чужим репозиториям (001). Прежний ответ говорил «документ» и отстал от дерева: механизм стоял, ответ его не называл."
    },
    "163": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_shipped.py — вход README.md сверяется с деревом: инструмент объявляет себя маркером «ОТДАЁТСЯ НАРУЖУ» началом строки в шапке, и каждый помеченный обязан быть назван на входе. Замер 13.09.2026: помеченных ноль — ни одного шага с workflow_call, ни одного действия, ни собираемого пакета, — поэтому гейт судит ВХОД: пока отдавать нечего, README обязан говорить это словом (154), а появится первый помеченный — назвать его и снять объявление пустоты. Полноту описания подключения гейт не судит: это связность текста (195). Прогоны — tests/test_shipped.py, включая то, что упоминание маркера объявлением не является."
    },
    "164": {
      "status": "active",
      "mechanism": "gate",
      "where": "CONTRACT_VERSION — версия КОНТРАКТА, а не проекта; scripts/version.py считает версию проекта отдельно (MAJOR.MINOR из релизного тега, PATCH — число принятых изменений), и оба числа публикуются фактом рядом: scripts/build_facts.py. Свести их в одно значило бы либо скрыть работу, либо объявить выпуском каждое изменение. scripts/check_version.py — гейт на рукописное число; docs/release.md — что именно версионируется"
    },
    "165": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_source_hygiene.py — списки путей из git читаются по NUL: гейт требует -z у каждого вызова с --name-only, --name-status, ls-files и diff-tree, и в коде (scripts/agent_pr.py, scripts/check_journal.py, scripts/check_version.py), и прямо в шаге прогона (.github/workflows/ci.yml). Список передаётся в scripts/check_pr_meta.py ФАЙЛОМ (--files-from), а не подстановкой: оболочка вырезает NUL. Охват печатают все три проверки — сколько путей прочитано и сколько пропущено"
    },
    "166": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/changerefs.py — связь изменения с задачей ищется как СВЯЗЬ, а не как подстрока: кодовые вставки и заборы вырезаются построчно, поэтому «Closes #12» в цитате подсказки гейта связью не считается. Держит tests/test_changerefs.py; сам разбор появился из находки — гейт зеленел на цитате в описании, то есть ровно там, где отношения нет"
    },
    "167": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_source_hygiene.py::test_shell_variable_names_are_ascii — имя переменной оболочки в шагах прогона обязано быть ASCII, хотя проект ведётся на русском. Опаснее не падение кодом 127, а тихий случай: переменная окружения с не-ASCII именем создаётся штатно, подстановка выходит пустой, и условие на ней всегда идёт одной веткой при зелёном гейте"
    },
    "168": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/ci-complete.yml — сводный гейт собран опросом, без needs, и живёт ОТДЕЛЬНЫМ прогоном: группа отмены ci гасила его вместе с гейтами, и на голове оставалось cancelled там, где защита ждёт вердикт; scripts/ci_complete.py; tests/test_gates_contract.py проверяет отсутствие needs и полноту опроса"
    },
    "169": {
      "status": "active",
      "mechanism": "gate",
      "where": ".rules/schedules.json и tests/test_schedules.py — роль каждого расписания объявлена данными (main либо safety-net), рядом названы цена в вызовах и причина именно этого часа. Гейт не ищет слова: сверяет объявленный cron с деревом, ловит незаявленное расписание и объявление, пережившее своё расписание, и считает арифметику — укладывается ли худший час в объявленную долю часового лимита (квота общая, 058). У страховки обязателен замер срабатываний: пока его нет, основной путь строят так, будто страховки нет. Форма взята у каталога целиком (162). Прежний ответ — docs/decisions/004-schedules-stay-service-observes.md — остаётся в силе: он про то, ЧТО уезжает в службу, а здесь про то, что осталось."
    },
    "170": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/fixtures/check-runs.shape.json — снятый ответ площадки с датой и адресом, откуда снят; tests/test_platform_shape.py сверяет с ним подделки набора: ключ, которого площадка не отдаёт, и тип, которого у неё не бывает, роняют гейт. Подделки берутся вызовом, а не списком руками — список разошёлся бы с самой подделкой. Чего гейт не ловит, названо прямо: изменение самой площадки — снимок стареет и обновляется рукой, и заменой живому прогону он не является (046)"
    },
    "171": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py и scripts/pipeline_checks.py — состав обязательных проверок читается из .pipeline.yml ДЕРЕВА самого изменения (--policy .pipeline.yml на выкаченной голове), а не с общей ветки. Иначе изменение, которое сам состав и меняет, стало бы неразрешимым: чтобы эталон обновился, оно должно уехать в main, а уехать не может — проверка красная. Держит tests/test_gates_contract.py::test_summary_takes_its_subject_from_data"
    },
    "172": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_own_name.py — перепись имён репозиториев по отслеживаемым файлам против канона площадки. Своё имя сверяется с GITHUB_REPOSITORY (это full_name, точный по регистру); переименованное ловится ОТВЕТОМ площадки, а не сравнением имён: старое имя не похоже на новое, и площадка держит по нему редирект. Локальный заход регистр не сверяет и говорит об этом: origin хранит написание клонировавшего. Приём взят у соседа, у которого правило родилось: после трёх переименований 28 ссылок устарели при восьми зелёных гейтах (162). Вторая половина — docs/release.md, раздел «Переходная заглушка»."
    },
    "173": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_pr_meta.py — третий ответ у связи теперь есть и проверяется: `Closes #N` при незакрытых пунктах чек-листа задачи отвергается, а пункт, названный закрытым строкой «Закрывает пункт: <текст>» в теле изменения, из счёта уходит — иначе последний этап закрыть было бы нечем. Разбор строки — в scripts/changerefs.py, там же где связь и снятие находки: отметка едет вместе с работой, а не отдельным жестом (002). Пункт называется текстом, а не номером строки: номер сдвигается от любой правки тела. Держат tests/test_gates_reject.py::test_closing_a_task_with_open_items_is_rejected и три соседних случая. Задача без чек-листа закрывается свободно — это названное состояние, а не пропуск (154); требование заводить чек-лист у задачи с этапами записано в AGENTS.md. Отметку ставит scripts/automerge.py в момент слияния: пункт становится сделанным ровно тогда, когда изменение слилось — раньше это обещание, позже уже история. Отказ разметки слияния не отменяет и заход не роняет, а ненайденный пункт называется вслух (045)"
    },
    "174": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/build_facts.py — проект публикует факты о себе сам, в ветку `badges`: объявленный контракт, посчитанная версия, ответы по правилам, классы проверок и разрез по общим механизмам семьи. Соседу и каталогу не приходится читать наше дерево, чтобы это узнать. .github/workflows/badges.yml — прогон; tests/test_facts.py — числа берутся из источников, а не пишутся руками"
    },
    "175": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gaps_section.py — таблица «чего в проекте ещё нет» в AGENTS.md проверяется на подкласс, сводимый к наличию: ни один пробел не называется ЧИСЛОМ, потому что счёт двигает механизм (шаг debt, facts.json в ветке badges), а вписанный рукой расходится с деревом на первой же разобранной записи и продолжает выглядеть решением. Проза целиком гейтом не трогается намеренно. Правило поймало обе строки при первом же применении: «ответа по 129 записям» устарело на 71 запись, а «первого прогона публикации нет» — на состоявшийся прогон и живую ветку badges"
    },
    "176": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_source_hygiene.py::test_subprocess_text_names_its_encoding — text=True без encoding берёт кодировку у локали раннера, и матрица прогонов этого не доказывает: дефект требует совпадения ячейки И данных, поэтому зелёное говорит «совпадения не случилось на этих данных». Ловится разбором исходника, а не прогоном, — гейт разбирает вызовы деревом AST во всех scripts/ и tests/"
    },
    "177": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/debt.py — три числа незакрытой работы по правилам читаются из задачи-«входящих» и печатаются шагом debt; docs/behaviour.md, контур 1 — источник 5 стоит выше плана"
    },
    "178": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/rules-inbox.yml и scripts/debt.py — числа об одной величине приходят с двух сторон: каталог считает по своей выгрузке, проект — по .rules/bindings.json. При расхождении первым проверяется СВОЁ чтение: поле answers_to называет версию выгрузки, по которой построены ответы, и сверяется каталогом. Именно так расхождение и всплыло — schema была вписана по разумению окна вместо заготовки каталога, а не потому, что величины разные; scripts/drift.py — «один снимок старее другого» проверяется механизмом, а не гипотезой: сводка семьи сверяется с живым ответом дерева, и расхождение называется правилами поимённо"
    },
    "179": {
      "status": "active",
      "mechanism": "gate",
      "where": "ЗАМЕР 17.09.2026 ПО ВСЕМ ПРОГОНАМ: отменяют предыдущий семь, и предмет правила — только они. Голову в группе называют три: .github/workflows/ci.yml, ci-complete.yml, review.yml. Четыре объявлены СОСТОЯНИЕМ и голову не называют намеренно — badges.yml (пересобирает витрину), drift.yml (сводит внешнее состояние), rules-inbox.yml (разбирает чужую выгрузку), attribution-history.yml (считает по истории общей ветки): это граница, названная самим правилом, и голова там вернула бы гонку за свежесть. Держит tests/test_gates_contract.py::test_a_cancelling_group_names_the_head_or_declares_why_not — отменяющий прогон либо называет голову, либо стоит в разрешительном списке состояний с причиной (068, 154); мёртвую запись списка отвергает сосед",
      "why": "Ответ пересмотрен 17.09.2026 после находки внешнего взгляда на #427 — и находка оказалась точнее, чем названа. Прежний ответ перечислял ci.yml, agent-pr.yml и labels-sync.yml: из них agent-pr не отменяет и головы не имеет, а labels-sync голову имеет, но НЕ ОТМЕНЯЕТ — оба вне предмета правила. Настоящие исключения, которых ответ не называл вовсе, — четыре прогона, которые отменяют и голову не называют по замыслу. МЕХАНИЗМ БЫЛ СЛАБЕЕ ОТВЕТА: голову проверяли поимённо у двух прогонов из семи, и новый отменяющий прогон без головы прошёл бы молча. Поэтому вид сменён с `pipeline` на `gate`: проверка теперь идёт по всем отменяющим, а не по названным"
    },
    "180": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_ghrest.py — гейты, ищущие обращения к площадке, определяют предмет по ИМПОРТАМ разбираемого файла (функция, собирающая имена импортированных модулей), а не по последнему звену имени вызова: своя функция с тем же именем ничего не доказывает, а импорт под псевдонимом иначе спрятал бы вызов от проверки"
    },
    "181": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_version.py — исключительное утверждение свода «версия живёт в CONTRACT_VERSION и больше нигде» связывает ВЕСЬ проект, и гейт так его и проверяет: находка краснеет, где бы число ни стояло, а не только в разделе о выпуске. Второе такое утверждение о том же предмете завести нечем — источник версии в дереве один. ОСТАЛЬНЫЕ СЧЁТНЫЕ УТВЕРЖДЕНИЯ ОБЪЯВЛЕНЫ ДАННЫМИ с 15.09.2026: .rules/claims.json перечисляет их, а tests/test_claims.py сверяет каждое с деревом: право перезапуска джоба, приставка claude/** в фильтрах ветвей, число входов у замера взведения, число прогонов, пишущих в дерево. НИ ОДНО ЧИСЛО ЗДЕСЬ НЕ ПОВТОРЯЕТСЯ — ни величины утверждений, ни длина самого реестра: они живут в реестре, и копия в прозе устарела бы молча. Датированный замер прозе законен, но длина файла того же дерева замером не является: её видно, открыв файл (914fd5f) — ровно тот класс, против которого эта проверка и завелась (005, 022; нашёл внешний взгляд, edec399). Утверждение о настройке вне дерева («единственный обязательный контекст защиты») называет держателем scripts/check_required_context.py, а не считается вторым разбором (022). Повод — замер 15.09.2026: badges.yml утверждал «единственное право на запись во всём конвейере», и это перестало быть верным в день появления второго писателя; нашла правка, а не гейт. ЧЕГО НЕТ И НАЗВАНО: поиска исключительных утверждений в прозе. «Единственный читатель этой метки — человек» и «единственный обязательный контекст» отличаются не словами, а предметом, и красное на каждом «единственный» учат обходить (051); несчётное держится чтением и объявлено в реестре словом none с причиной (154)."
    },
    "182": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".github/workflows/ci.yml, джоб attribution — требование «изменение открывается от лица владельца» разложено надвое: намерение машине недоступно, но АВТОР и соавтор в истории следуют из данных целиком, и эту половину держит действие каталога (attribution@v1.2.0) с отметкой baseline. AGENTS.md называет вторую половину явно — часть запретов машинно не проверяется, и записаны они именно поэтому (057)"
    },
    "183": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_bindings.py::test_declared_address_resolves — проза, объявляющая, ЧЕМ держится правило, сверяется с деревом: адрес механизма обязан разрешаться, иначе ответ «держится гейтом по адресу X» при отсутствующем X хуже отсутствия ответа — он выглядит исполненным. Чужие адреса из проверки исключены намеренно: их правит владелец каталога (185)"
    },
    "184": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_bindings.py::test_negative_answers_name_the_reason — «не относится» без причины не проходит вовсе, а сами причины в .rules/bindings.json написаны разбором надвое: каждая называет, чего нет по букве (параллельных исполнителей, переводов, своего хранилища, сравнений с послаблениями), и границу, после которой обобщение заработает здесь — чаще всего — передача механизмов наружу, то есть появление потребителя вне дерева. Форму держит гейт, разбор — прочтение причины человеком"
    },
    "185": {
      "status": "active",
      "mechanism": "gate",
      "where": ".rules/proposals.json — след предложения ведёт в дерево каталога и номера здесь не присваиваются; tests/test_bindings.py::test_proposals_assign_no_numbers держит это гейтом: поля id, number и rule в предложении — ошибка, номер присваивает каталог при приёме. Вторая половина — АДРЕС, КОТОРЫЙ ОБЯЗАН РАЗРЕШАТЬСЯ: scripts/check_rule_links.py сверяет каждую нашу ссылку на правило каталога с живой выгрузкой — имя файла берётся у владельца дерева, а не пишется по памяти, и ссылка на несуществующий слаг краснеет. Ловит это регулярно: за одну смену 17.09.2026 гейт поймал пять выдуманных имён правил",
      "why": "Ответ дополнен 17.09.2026. Назван был след ПРЕДЛОЖЕНИЯ, а правило шире: «след — это адрес в чужом дереве», и признак нарушения у него — адрес, который не разрешается. Главный наш механизм против этого — сверка ссылок на правила — ответом не назывался"
    },
    "186": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/build_changelog.py — у инструмента четыре режима (сборка, --check, --fragments, --release) и три кода, и переиспользования между режимами нет: код 1 существует ТОЛЬКО в сухом режиме и означает «расходится», а режим записи отвечает нулём и печатает свидетельство работы — какой файл собран и с какой версией контракта. Держит tests/test_gates_reject.py"
    },
    "187": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/ci_complete.py — пропущенная, отменённая и отсутствующая записи считаются отказом; tests/test_gates_complete.py прогоняет каждый из этих случаев. Признак нарушения, названный самим правилом, проверяется отдельно: scripts/ci_complete.py отвергает голову, на которой больше ОДНОЙ живой записи с именем обязательной проверки, — вердикт по такой голове неоднозначен, и молчаливый выбор между записями был бы выбором наугад",
      "why": "Ответ дополнен 17.09.2026. Назван был разбор ИСХОДА записи, а правило говорит о КОЛИЧЕСТВЕ записей на голове: «не должно оставаться больше одной живой записи с именем обязательной проверки». Эта половина в дереве стояла и ответом не называлась"
    },
    "188": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/agent_pr.py — kept_the_marks/say_lost_marks: тело изменения несёт строки «Разобрано: <отпечаток>», и это КОМАНДА другому механизму — уборка реестра находок читает их из тела и по ним уносит запись. Опубликованное тело сверяется с отправленным сразу же, и стоит это НОЛЬ запросов: площадка возвращает его в ответе на запись. Судятся отпечатки, а не тело побайтово: нормализация перевода строки законна, и краснеть на ней значило бы красить исправное (051). Потеря объявляется предупреждением, а не отказом: изменение уже открыто, и ронять шаг значило бы менять потерю записи на потерю изменения (084). Держит tests/test_agent_pr_consent.py — три проверки: потеря названа, целое тело молчит, нормализация пробела не находка. ТОТ ЖЕ ПРИЁМ УЖЕ СТОЯЛ У ЗАМЕРА ВЗВЕДЕНИЯ: scripts/arm.py::kept_the_body сверяет переданные commitHeadline и commitBody с тем, что отдала площадка (решение 011)",
      "why": "Ответ пересмотрен 17.09.2026 и сменён с «неприменимо». Прежняя причина — «текста, который обязан прочитать и на который обязан ответить кто-то ещё, автоматика проекта не публикует» — неверна: строка «Разобрано» в теле изменения адресована МЕХАНИЗМУ, а оклик в hail.yml — человеку, и оба обязаны быть прочитаны и отработаны. Прежний ответ смотрел только на бота, отвечающего человеку. ЧТО ДЕРЖИТСЯ НЕ ПОЛНОСТЬЮ, названо: оклик hail.yml перечитывается по своей отметке (hail-stamp) — то есть проверяется, что комментарий ЕСТЬ, а не что его текст доехал целым; сверки текста там нет, и это остаток (046)"
    },
    "189": {
      "status": "active",
      "mechanism": "gate",
      "where": ".github/workflows/agent-pr.yml и scripts/agent_pr.py — приставка ветки одна, `agent/**`; выданная платформой `claude/<имя-окна>` изменения больше не открывает, то есть ветка, названная ОКНОМ, в конвейер не входит вовсе. Гейт различает не смысл имени, а его приставку: «по задаче, а не по окну» дальше держится сводом (CLAUDE.md, раздел «Ветка в облачном окне») и связью изменения с задачей (scripts/check_pr_meta.py). Прежний ответ был «отвергнуто» — «имя ветки назначает платформа»; это перестало быть правдой, когда окно начало резать свою ветку, и подтверждено прогоном на изменении #35"
    },
    "190": {
      "status": "active",
      "mechanism": "pipeline",
      "where": ".rules/bindings.json и .rules/proposals.json собраны scripts/onboard_consumer.py каталога, .github/workflows/rules-inbox.yml подключён его действием — форма ответа спрошена у соседа, а не воспроизведена по памяти. ВИТРИНА ЧУЖИХ ЧИСЕЛ — scripts/family.py и scripts/build_facts.py: разрез по сводке каталога спрашивает источник, а не помнит его форму. Расхождение здесь НАХОДКА, А НЕ ОТКАЗ СБОРКИ: непрочитанная сводка даёт `read: false` с названной причиной, а не ноль (family.NotRun), разошедшаяся форма печатается рядом с числами и заводит запись дрейфа `family-schema` (scripts/drift.py::reader_is_behind). Право остановить наш конвейер правкой у себя сосед таким образом не получает",
      "why": "Ответ дополнен 17.09.2026. Назван был канал подключения — как форма ответа спрошена у соседа, — а вторая половина правила про то, ЧТО ДЕЛАТЬ ПРИ РАСХОЖДЕНИИ, не называлась вовсе. Она в дереве есть и в тот же день была достроена: расхождение формы сводки стало записью дрейфа"
    },
    "191": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/agent_pr.py::sync_description — вторая половина правила держится точно: механизм ПЕРЕЧИТЫВАЕТ тело изменения перед записью и по отметке MARK отличает своё от правленного человеком, чужое не затирая. Заготовок со стоп-краном первого рода — тех, что механизм создаёт вместо человека и держит красным до заполнения, — у проекта нет: описание изменения он заполняет сам из коммитов тем же заходом"
    },
    "192": {
      "status": "active",
      "mechanism": "gate",
      "where": "Ответ разделён надвое (182). МАШИНОЙ держится практическая граница правила «гейт, заведённый вместе с такой правкой, обязан ловить и НОЛЬ»: tests/test_attribution_history.py::test_the_boundary_leaves_a_subject_behind_it — после объявленной отметки `since` обязан остаться хотя бы один первопредок. Гейт каталога на пустом диапазоне печатает «новых коммитов нет — проверять нечего» и возвращает ноль; отметку двигали трижды (09.09, 10.09, 13.09) и каждый раз вперёд, и доехав до головы, она погасила бы прогон целиком — снаружи это неотличимо от проверенной истории (075). Замер 13.09.2026: после отметки 77a5c72 — 25 первопредков. СУЖДЕНИЕМ остаётся сам вопрос «обесценивает ли правка замер»: он о том, на чём замер снят, и задаётся до правки, а не выводится из дерева. Разбор — docs/decisions/001-attribution-of-merged-1.md: замер снят при одном исполнителе, а правка передавала действие другому, и личность записи потому узнаётся записью, а не чтением команды (135, CLAUDE.md)."
    },
    "193": {
      "status": "not-applicable",
      "why": "массовой автоматической починки данных в проекте нет: механизмы правят по одному предмету за изменение и проверяются отказом. Правило вступит вместе с первой такой починкой"
    },
    "194": {
      "status": "not-applicable",
      "why": "Ответ перечитан 16.09.2026. Остаётся НЕПРИМЕНИМЫМ, и это проверено, а не унаследовано: имени в чужом реестре проект по-прежнему не занимает — пакет `engineering-pipeline-transport` ставится из git по тегу выпуска, а не из реестра имён, и ни образа, ни аккаунта у проекта нет. Прежняя формулировка называла условием пронумерованный шаг дорожной карты — а номеров в ней нет: она ведётся этапами, и перечень отложенного переехал в #243, раздел 16. Рукописный номер в чужом документе сгнил (005), и условие названо заново СОБЫТИЕМ: правило вступит, когда проект ЗАЙМЁТ имя в чужом реестре — опубликует пакет, образ или заведёт аккаунт. Сегодня публикации нет ни в одном прогоне дерева"
    },
    "195": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_source_hygiene.py — починка соседей не оставляет: гейты идут по ВСЕМ модулям и всем прогонам сразу, а не по тому файлу, где нашли. Так и вышло: список путей без -z нашёлся в scripts/agent_pr.py, а закрыт вместе с шагом «тронутые файлы» в .github/workflows/ci.yml; кодировка у text=True — сразу в пяти вызовах. Сосед, который и прежде не работал, тоже красный: гейт не спрашивает, был ли он зелёным раньше"
    },
    "196": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_derived_refs.py — ссылка на своё производное, добавленная изменением, сверяется с площадкой: артефакт на своей ветке уже нарисован. Проверкой «файл есть в дереве» это не лечится — производного в дереве и не должно быть"
    },
    "197": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_gates_complete.py, tests/test_pipeline_checks.py — проект публикует наружу форму `.pipeline.yml` и классы проверок, и сам отвечает по ней же: его собственный ответ читается тем же `check_pipeline.py`, каким будет читаться ответ потребителя. Симметрия держится тем, что механизм один, а не двумя похожими."
    },
    "198": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/check_rule_links.py — ссылки вида `rules/ru/NNN-slug.md` сверяются с выгрузкой каталога, и номер с адресом разведены: «правила нет» и «имя не то» — разные находки. Гейт родился из замера 10.09.2026: 29 битых ссылок из 300, все с верным номером и выдуманным именем."
    },
    "199": {
      "status": "active",
      "mechanism": "gate",
      "where": "scripts/automerge.py::hand_over — заход очереди больше не ждёт зелёного внутри себя, и потому его смерть в очереди ничего не теряет: взведение у площадки ПЕРЕЖИВАЕТ прогон, который его выдал, и слияние случится без нового события. Прежде здесь стоял опрос внутри захода, заведённый от замера 10.09.2026 — 45 из 120 заходов умерли, не начав работу, — но он лечил следствие: держал исполнителя и упирался в собственный предел. Решение docs/decisions/011-merging-is-handed-to-the-platform.md отдало ожидание площадке. Держит это tests/test_automerge.py"
    },
    "200": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_facts.py — значок правил считает держащиеся МАШИНОЙ от действующих, а не `answered/total`: прежнее число было равно знаменателю по построению и не могло сдвинуться никогда. Гейт сверяет, что значок несёт то же число, что и факты."
    },
    "201": {
      "status": "not-applicable",
      "why": "предмет правила — ВЫГРУЗКА, которую издаёт каталог, а не ответ потребителя: требование обращено к тому, кто публикует корпус для чужого исполнения. Своего каталога требований наружу этот проект не издаёт — он издаёт механизмы и контракт на них, а их суть живёт в самих файлах, которые потребитель клонирует. Правило родилось здесь: нашли его мы, и предложением ушло в каталог (`.rules/proposals.json`, `an-export-without-the-rule-text-is-read-by-its-title`)."
    },
    "202": {
      "status": "active",
      "mechanism": "gate",
      "where": ".claude/hooks/push_guard.py — толчок мимо текущей головы отвергается ДО вызова git. Ветку, слитую и удалённую площадкой, воскрешает любой следующий пуш в неё; здесь это ловится тем же сторожем, что и промах именем ветки."
    },
    "203": {
      "status": "active",
      "mechanism": "pipeline",
      "where": "scripts/drift.py — источник `language_moved` сверяет матрицу версий языка с манифестом `actions/python-versions`, то есть с тем же живым источником, из которого версии берёт площадка. Предрелизная 3.15 в `test-next` перестанет быть предрелизной от чужого выпуска, и заметит это заход дрейфа, а не память."
    },
    "204": {
      "status": "active",
      "mechanism": "gate",
      "where": "tests/test_citation_applicability.py — ссылка на правило не опирается на то, чью применимость проект сам отверг в .rules/bindings.json. Упоминания чужой границы живут закрытым списком с причиной у каждого. Подходит ли раздел «Не работает» к нашему случаю, машина не решает — это вывод из смысла; решается половина, заданная данными: наш же ответ по правилу. Адреса ссылок сверяет scripts/check_rule_links.py"
    },
    "205": {
      "status": "active",
      "mechanism": "none",
      "why": "Правило родилось ЗДЕСЬ: инцидент отправлен этим проектом 17.09.2026 и принят каталогом в тот же день. Отвечать «неприменимо» по нему было бы прямым самоопровержением. ЗАМЕР ТОГО ЖЕ ДНЯ: из 14 живых ответов «неприменимо» предикат по дереву несут ПЯТЬ; девять — это ответы 033, 036, 066, 077, 081, 103, 120, 193 и 194 — называют только событие либо рассуждение. То есть правило действует, соблюдается меньше чем наполовину, и гейта на него нет — это долг (177), а не неприменимость (046). Замер, из которого правило выросло: 22 ответа за историю сменились с «неприменимо» на «действует» при 14 живых сегодня",
      "machine_half": "ПОЛОВИНА ДЕЛИТСЯ, И ГРАНИЦА У НЕЁ РЕЗКАЯ. Что предикат НАЗВАН, машина проверяет целиком: причина ответа «неприменимо» обязана нести адрес в дереве либо строку поиска — это тот же разбор, каким tests/test_answers_are_checkable.py уже проверяет адреса активных ответов. Что предикат ВЕРЕН — то есть что названное и правда не находится, — машина проверить может лишь для части форм: `grep` по строке исполним, «второго языка в проекте нет» не выражается поиском вовсе. Не построено потому, что первая половина без второй даёт гейт, требующий вписать ЛЮБУЮ строку поиска, — и он будет исполнен формально, ровно как 146 и запрещает"
    },
    "206": {
      "status": "active",
      "mechanism": "gate",
      "where": "Правило родилось здесь, и оба его требования держатся механизмами того же дня. ВСЕ ФОРМЫ: tests/test_citation_applicability.py читает цитату правила в ДВУХ формах — markdown-ссылкой и голым номером в скобках; до 17.09.2026 читалась одна, и замер показал, что невидимого больше видимого (1744 упоминания голым числом против 1007 ссылками, и 23 правила названы только голым числом). НАБОР ФОРМ БЕРЁТСЯ ЗАМЕРОМ: tests/test_steps_speak_outward.py::test_the_pattern_sees_every_annotation_in_the_tree сверяет разбор аннотаций с сырым поиском по дереву — 62 из 62, — и именно он нашёл две формы, которых образец не видел никогда. ДВУСМЫСЛЕННОЕ ОБЪЯВЛЕНО ВСЛУХ: у голой формы названа асимметрия — опечатка в номере уходит молча, потому что в скобках ходят и коды ответа площадки; граница записана рядом с образцом, а не умолчана",
      "why": "Инцидент отправлен этим проектом 17.09.2026 и принят каталогом в тот же день правилом 206. Повод был свой: гейт применимости цитат родился 11.09 на правиле 193, нашёл его нарушение в документе, где оно записано ССЫЛКОЙ, и не увидел БЛИЗНЕЦА того же утверждения, записанного голым числом в scripts/main_red.py, — тот прожил ещё шесть дней, говоря обратное. Чего механизм НЕ делает: он не проверяет формы, которых в дереве ещё нет. Набор форм берётся замером по дереву, и новая форма станет видна, когда её напишут"
    }
  }
}
