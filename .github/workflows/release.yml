# Выпуск: собрать журнал, поднять версию контракта, поставить тег.
#
# ТОЛЬКО КНОПКОЙ, И ЭТО НЕ ОСТОРОЖНОСТЬ, А УСТРОЙСТВО. Тег не переставляется:
# шаг, который нельзя отменить, получает собственную проверку ПЕРЕД собой (074),
# а решение «выпускать» принимает человек. Событие, запускающее выпуск само,
# сделало бы выпуском каждое слияние.
#
# ПРОВЕРКА ИДЁТ ДВАЖДЫ, И ВТОРОЙ РАЗ — НА ПЛОЩАДКЕ. Механизм отказывает по
# списку причин сразу, а не по первой попавшейся: выпуск делают редко, и чинить
# его по одной причине за заход дороже, чем увидеть все.
#
# ТОКЕН ВЛАДЕЛЬЦА, А НЕ ПРОГОНА. Выпуск несёт личность человека так же, как
# слияние: на токене прогона автором тега и коммита стало бы приложение, и
# переписать это потом нечем (131, 135).
name: release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "номер выпуска; пусто — следующий по правилу"
        required: false
        type: string
      acceptance:
        description: "номер задачи-приёмки: мажор поднимает её ЗАКРЫТИЕ, а не выпуск"
        required: false
        type: string
      apply:
        description: "сделать необратимое: собрать, пометить тегом и отправить"
        required: false
        default: false
        type: boolean
      # НЕСОВМЕСТИМОСТЬ ОБЪЯВЛЯЕТ ЧЕЛОВЕК: род фрагмента `contract` говорит
      # «поверхность тронута» и только, а гейт связи смотрит дифф ИЗМЕНЕНИЯ,
      # которого у выпуска уже нет — к этому моменту слито много изменений.
      breaking:
        description: "поверхность изменена НЕСОВМЕСТИМО: поднимает мажор версии контракта"
        required: false
        default: false
        type: boolean

permissions:
  contents: read
  # Состояние приёмки мажора читается у площадки: механизм не верит ключу на
  # слово. Право только на чтение — выпуск ничего в задачах не меняет.
  issues: read

concurrency:
  # Выпуск один на репозиторий, и вытеснять его нечем: два захода поставили бы
  # два тега на разные головы.
  group: release
  cancel-in-progress: false

jobs:
  release:
    name: release
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: токен владельца не задан
        id: token
        env:
          MERGE_QUEUE_TOKEN: ${{ secrets.MERGE_QUEUE_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${MERGE_QUEUE_TOKEN:-}" ]; then
            echo "present=no" >> "$GITHUB_OUTPUT"
            echo "::warning::MERGE_QUEUE_TOKEN не задан — выпуск возможен только проверкой: тег и коммит несут личность человека, и токеном прогона их ставить нельзя."
          else
            echo "present=yes" >> "$GITHUB_OUTPUT"
          fi

      # ВСЯ ИСТОРИЯ И ТЕГИ: выпуск проверяет, что такого тега ещё нет, и считает
      # версию проекта. Мелкий клон тегов не видит, и проверка стала бы ложной.
      - uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
          fetch-tags: true
          token: ${{ secrets.MERGE_QUEUE_TOKEN || github.token }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --quiet "pyyaml>=6,<7" ./packages/transport

      # Подпись — человека: автором выпуска в истории обязан быть он, а не
      # приложение. Проверить это потом нечем — уплотнения тега не бывает.
      - name: подпись выпуска
        run: |
          set -euo pipefail
          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com"

      - name: проверить условия выпуска
        env:
          GH_TOKEN: ${{ github.token }}
          # ПРАВО ОБХОДА СПРАШИВАЕТСЯ ТЕМ ТОКЕНОМ, КОТОРЫМ ТОЛКАЮТ. Площадка
          # отвечает про спросившего, и вопрос токеном прогона отвечает про
          # него же — «обход: никогда», и отвечает ВЕРНО: обход прогону не
          # положен (`.rules/protection.json`). 13.09.2026 выпуск из-за этого
          # отказывал всегда, а владелец видел, что выданный роли обход ничего
          # не меняет. Читать задачи по-прежнему может токен прогона — права
          # на это у шага есть, и токен владельца тут ничего не добавляет.
          MERGE_QUEUE_TOKEN: ${{ secrets.MERGE_QUEUE_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          # ВВОД ИДЁТ ОКРУЖЕНИЕМ, А НЕ ПОДСТАНОВКОЙ В ТЕКСТ КОМАНДЫ (085).
          # Кавычка или `$()` во входе кнопки исполнились бы как код.
          ASKED_VERSION: ${{ inputs.version }}
          ASKED_ACCEPTANCE: ${{ inputs.acceptance }}
          ASKED_BREAKING: ${{ inputs.breaking }}
        run: |
          set -euo pipefail
          args=()
          if [ -n "${ASKED_VERSION:-}" ]; then args+=(--version "$ASKED_VERSION"); fi
          if [ -n "${ASKED_ACCEPTANCE:-}" ]; then args+=(--acceptance "$ASKED_ACCEPTANCE"); fi
          if [ "${ASKED_BREAKING:-false}" = "true" ]; then args+=(--breaking); fi
          python scripts/release.py "${args[@]}"

      - name: выпустить
        if: inputs.apply && steps.token.outputs.present == 'yes'
        env:
          GH_TOKEN: ${{ secrets.MERGE_QUEUE_TOKEN }}
          # ПРАВО ОБХОДА СПРАШИВАЕТСЯ И ЗДЕСЬ — ОСОБЕННО ЗДЕСЬ. Сухой заход
          # получил этот токен, а необратимый остался без него: `push_token()`
          # читает ИМЕННО `MERGE_QUEUE_TOKEN`, и в шаге, который толкает,
          # вопрос о праве не задавался вовсе. Проверка, молчащая в заходе, где
          # она нужнее всего, — та же тишина, что и её отсутствие (045).
          # Нашёл внешний взгляд на #296.
          MERGE_QUEUE_TOKEN: ${{ secrets.MERGE_QUEUE_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          # То же окружение, что и у сухого захода: два разных способа читать
          # один вход разошлись бы молча (090).
          ASKED_VERSION: ${{ inputs.version }}
          ASKED_ACCEPTANCE: ${{ inputs.acceptance }}
          ASKED_BREAKING: ${{ inputs.breaking }}
        run: |
          set -euo pipefail
          args=(--apply)
          if [ -n "${ASKED_VERSION:-}" ]; then args+=(--version "$ASKED_VERSION"); fi
          if [ -n "${ASKED_ACCEPTANCE:-}" ]; then args+=(--acceptance "$ASKED_ACCEPTANCE"); fi
          if [ "${ASKED_BREAKING:-false}" = "true" ]; then args+=(--breaking); fi
          python scripts/release.py "${args[@]}"

          # ТОЛЧКА ДВА, И МЕЖДУ НИМИ ПОРЯДОК: метка ставится ПОСЛЕ того, как
          # помеченное доехало (074, 109). Один заход `--follow-tags` отправлял
          # два ref'а без порядка, и отказ ветки оставлял тег на коммите, до
          # общей ветки не доехавшем. Разбор — в журнале, фрагмент
          # `a-tag-follows-the-commit-that-landed`.
          tag="$(git tag --points-at HEAD)"
          if [ -z "${tag}" ]; then
            echo "::error::Выпуск собран, но тега на HEAD нет: метить в общей ветке нечего (075)."
            exit 1
          fi

          if ! git push origin HEAD; then
            echo "::error::Общая ветка не приняла коммит выпуска — тег НЕ отправлен, метить нечего. Коммит выпуска собирается машиной из уже слитых фрагментов, и проверки изменения у него нет: набору правил общей ветки нужен обход для того, кто выпускает (Settings → Rules → Bypass list)."
            exit 1
          fi

          # ОТКАЗ ТЕГА НАЗЫВАЕТСЯ ТОЖЕ, и это не симметрия ради симметрии:
          # коммит выпуска в этот момент УЖЕ в общей ветке, а метки на нём нет.
          # Состояние поправимое — тег ещё свободен, — но чинится оно руками, и
          # человек должен прочитать чем, а не код возврата (154).
          if ! git push origin "${tag}"; then
            echo "::error::Коммит выпуска в общей ветке, а тег ${tag} до неё не доехал. Выпуск НЕ повторять: журнал уже собран, а заход отвергнет уже стоящий тег. Доставить метку отдельно: git push origin ${tag}"
            exit 1
          fi

      # СТРАНИЦА ВЫПУСКА — ПЯТЫЙ ШАГ, И ОН ИДЁТ ПОСЛЕ ТЕГА. Раньше её заводил
      # человек, и площадка наполняла её своим списком изменений вместо нашего
      # журнала. Шаг отдельный: тег к этому моменту уже уехал, и отказ здесь
      # не отменяет выпуска — он говорит, что довести страницу надо кнопкой
      # `--page` (104).
      - name: завести страницу выпуска
        if: inputs.apply && steps.token.outputs.present == 'yes'
        env:
          # Страницу заводит токен ВЛАДЕЛЬЦА: её автор — человек, выпустивший
          # версию, а не приложение (131). `GH_TOKEN` назван тем же секретом:
          # транспорт читает его, и без него механизм пошёл бы к площадке без
          # ключа.
          GH_TOKEN: ${{ secrets.MERGE_QUEUE_TOKEN }}
          MERGE_QUEUE_TOKEN: ${{ secrets.MERGE_QUEUE_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          ASKED_VERSION: ${{ inputs.version }}
        run: |
          set -uo pipefail
          version="${ASKED_VERSION:-}"
          if [ -z "${version}" ]; then
            version="$(git tag --points-at HEAD | head -1 | sed 's/^v//')"
          fi
          if [ -z "${version}" ]; then
            echo "::error::Тега на HEAD нет — страницу заводить не для чего (075)."
            exit 1
          fi
          rc=0
          python scripts/release.py --page "${version}" --apply || rc=$?

          # Коды разбираются СПИСКОМ РАЗРЕШЁННОГО (068): зелёным считается
          # только объявленный ноль, всё прочее — отказ. «Красным считаем
          # перечисленное» прочитало бы необработанный сбой Python как успех.
          case "$rc" in
            0) ;;
            *)
              echo "::error::Страница выпуска v${version} не заведена (код $rc). Тег и журнал на месте, выпуск повторять НЕ надо: страница доводится отдельной кнопкой — python scripts/release.py --page ${version} --apply"
              exit 1
              ;;
          esac

      - name: выпуск не сделан — сказано, а не пропущено
        if: inputs.apply && steps.token.outputs.present != 'yes'
        run: |
          echo "::error::Выпуск запрошен, но MERGE_QUEUE_TOKEN не задан: тег несёт личность человека, и токеном прогона его ставить нельзя."
          exit 1
