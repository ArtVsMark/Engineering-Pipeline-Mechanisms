# Карта направлений: кто обязан задать вопрос

> **Читатель:** владелец и окно — какие вопросы о продукте здесь задают, а
> какие не задаёт никто.

Направление — это **вопрос**, а не должность
([082](https://github.com/ArtVsMark/Engineering-Incidents-Playbook/blob/main/rules/ru/082-roles-must-cover-every-layer.md)).
Исполнителей у проекта двое — владелец и его окно, — и это не делает правило
неприменимым: правило прямо говорит, что покрытие **не равно** отдельной роли и
направление может быть **профилем** существующей.

Карта — **замер**, а не намерение. По каждому направлению правила стоит один из
трёх его исходов:

* **профиль** — вопрос задаётся, артефакт в дереве есть, и его что-то держит.
  Чьим профилем, сказано рядом, если это не владелец с окном. Хвост после слова
  «профиль» отвечает ровно на «ЧЕЙ» и ни на что другое: предел профиля, его
  вход и его граница живут в обосновании справа. Одна строка отвечала здесь
  «профиль с названным пределом» — то есть вписывала в слот «чей» ответ на
  другой вопрос, и различить это машине было нечем (нашёл внешний взгляд
  на #403);
* **роли нет** — артефакт есть, а вопрос не задаёт никто. Это **дыра**;
* **артефакта нет** — направление названо, но предмета ещё не сделали;
* **пласта нет** — предмета нет и не предвидится; появится — вернуться.

**Исходов здесь четыре, а в правиле три, и это не вольность.** Второй исход
правила — «направление есть, но артефакта в проекте нет» — покрывает два разных
положения: направление, которое мы уже назвали и не построили (обратная связь
потребителей — задача #14), и направление, которого у продукта нет вовсе
(обучение моделей). Первое ждёт работы, второе ждёт другого продукта, и сводить
их в одну пометку значило бы прятать очередь внутри «неприменимо»
([046](https://github.com/ArtVsMark/Engineering-Incidents-Playbook/blob/main/rules/ru/046-name-the-gaps-do-not-level-them.md)).

Полнота карты против правила и форма записи держатся
`tests/test_roles_map.py`. Числа здесь не пишутся: они гниют
([005](https://github.com/ArtVsMark/Engineering-Incidents-Playbook/blob/main/rules/ru/005-hand-written-numbers-rot.md)).

## Делаем

| Направление | Исход | Чем держится или почему нет |
|---|---|---|
| Архитектор | профиль Архитектора | `docs/decisions/` — записи решений; `tests/test_decisions_format.py`, `scripts/check_decisions_edit.py` |
| Разработчик прикладной | профиль Механика | `scripts/`, `packages/transport`; вся сводная проверка дерева |
| Разработчик-предметник | роли нет | Предмет острый и уже кусался: `run:` идёт через `bash -e`, и `set -uo pipefail` этого не отменяет. Ловили постфактум, вопрос «верно ли это по правилам самой платформы» систематически не задаёт никто |
| Клиентская часть | пласта нет | Интерфейса у проекта нет: витрина — текст, её ведёт Редактор |
| Инженер платформы | профиль Инженера площадки | `docs/pipeline.md`, `.pipeline.yml`, `scripts/check_contract.py`, матрица версий |
| Инженер данных | роли нет | Данные проекта — `.rules/*.json` и живые задачи-реестры. «Воспроизводим ли пересчёт» не спрашивается: счёт осечек канала в #89 — накопитель, восстановить его нечем, и это объявлено, но не закрыто |
| Релиз и цепочка поставок | профиль Релиз-инженера | `.github/workflows/release.yml`, `CONTRACT_VERSION`, `scripts/check_version.py`; пакет ставят снаружи |
| ИИ в продукте | роли нет | Внешний взгляд — часть конвейера, и он отдаёт изменение чужому исполнителю. «Чем проверяется сгенерированное» закрыто (вердикт, находки, реестр #23); «что уходит в модель» не объявлено нигде |
| Обучение моделей | пласта нет | Моделей проект не обучает |

## Проверяем

| Направление | Исход | Чем держится или почему нет |
|---|---|---|
| Тестирование | профиль Тестировщика | `tests/`; `tests/test_gates_reject.py` требует прогона отказа у каждого гейта |
| Аудитор | профиль внешнего взгляда | ПРЕДЕЛ ЕГО НАЗВАН, И ЭТО ЧАСТЬ ОТВЕТА. Внешний взгляд прогоном (`review.yml`) и поздний взгляд по общей ветке. ВХОД У НЕГО ОДИН, и он не «от поверхностей продукта»: карту ему собирает `scripts/review_map.py` из ответов проекта каталогу — правило с документом отдаётся глазам, правило с гейтом машине, а объявленное неприменимым называется отдельной группой с просьбой сказать, если предмет попался. Числа берутся из прогона карты, а не отсюда. Пласты, не бывшие правилами каталога вовсе, в карту не попадают никак, и ниже они помечены «роли нет» |
| Ревизор | профиль Ревизора | `scripts/debt.py`, `scripts/drift.py`, `scripts/schedules_seen.py`, `tests/test_claims.py` |
| Верификатор | профиль Верификатора | Шаг 12f `verify` в `.github/workflows/review.yml` — вход от ОДНОЙ находки по её отпечатку из #23, установка «опровергай по умолчанию», ответ словом из закрытой шкалы (`scripts/review_findings.py` — `premise_of`, `verified`). Ответ снятием не является: находку снимает работа. Заведён 16.09.2026 по замеру того же дня — находка `1fd43a4` описывала дефект, починенный двумя днями раньше, и заметил это человек |
| Критик метода | профиль окна | `tests/test_gates_reject.py` прогоняет каждый гейт тем, что он обязан отвергнуть, `tests/test_outcomes_run.py` — каждый объявленный исход; приём «проверено откатом» обязателен в фрагменте. Предел назван: сами проверки набора отказом не прогоняются, и 16.09.2026 это дважды стоило зелёной впустую проверки |
| Безопасность | профиль Безопасности | `tests/test_review_safety.py`, разделение `pull_request` и `pull_request_target`, права токенов шагов |
| Данные и приватность | роли нет | Тот же предмет, что у «ИИ в продукте»: что именно уезжает внешнему исполнителю и на каких условиях, не записано |
| Производительность и ресурсы | профиль Эконома прогонов | `scripts/runs_series.py`, `.rules/series.json`; цена позднего взгляда ограничена `LOOK_AT_ONCE` |
| Совместимость | профиль Релиз-инженера | Матрица версий языка, `test-next` на следующей, контракт с потребителем |
| Правомерность | роли нет | Лицензии в дереве нет вовсе — ни своей, ни сверки чужих. Продукт при этом отдаётся наружу пакетом |

## Понимаем и ведём

| Направление | Исход | Чем держится или почему нет |
|---|---|---|
| Системный аналитик | профиль Архитектора | `docs/pipeline.md` — договор; приёмка пунктов задач держится `scripts/task_items.py` |
| Аналитик продукта | артефакта нет | Канал обращений заведён (#14 закрыт), а данных о пользовании нет: подключённых потребителей ноль, и собирать не с кого — `scripts/consumers.py` говорит это словом |
| Аналитик данных | профиль Ревизора | Предметная область проекта — сам конвейер, и срезы по нему даёт ряд прогонов |
| Моделирование и прогноз | пласта нет | Проект ничего не предсказывает |
| Владелец продукта | профиль владельца | `docs/roadmap.md`; отказы записываются решениями |
| Развитие | профиль владельца | `docs/roadmap.md` — этапы, а не шаги |
| Внешние практики | профиль Соседа по семье | `scripts/family.py` — сравнение с конвейерами семьи; задача #239 |
| Методология | профиль каталога | «Учит ли продукт верному» ведёт каталог правил, здесь на него ссылаются |
| Специалист предметной области | профиль каталога | Предметная область — инженерия конвейеров, её правила живут в каталоге |
| Экономика работы | роли нет | Расход прогонов не мерит никто. Замер 16.09.2026: ряд прогонов до починки не собрал ни одного дня, то есть числа не было вовсе |
| Исследователь | профиль окна | Приём «ЗАМЕР ДД.ММ.ГГГГ» обязателен в фрагментах и пояснениях; величина без замера находкой не считается |
| Научная работа | пласта нет | Ни датасета, ни публикации у проекта нет |

## Объясняем

| Направление | Исход | Чем держится или почему нет |
|---|---|---|
| Технический писатель | профиль Техписателя | `tests/test_docs_shape.py`, `tests/test_doc_links.py`, `tests/test_answers_are_checkable.py` |
| Редактор | профиль Редактора | README как вход; тексты отказов держит `tests/test_steps_speak_outward.py` |
| Локализация | пласта нет | Язык проекта один и выбран намеренно; появится второй — вернуться |
| Дизайн | пласта нет | Макетов и состояний интерфейса нет |
| Доступность | пласта нет | Интерфейса нет; метка `accessibility` в репозитории заведена рукой и механизмом не читается |
| Показ | профиль Редактора | `CHANGELOG.md` собирается из фрагментов; заметки к выпуску делает механизм |
| Привлечение и онбординг | артефакта нет | Метки площадки для новичка есть, но не объявлены файлом и не читаются ничем — внешних участников у проекта пока нет, и это записано в `.github/labels.yml` |
| Сообщество и поддержка | профиль Диспетчера | Три двери обращений — `.github/ISSUE_TEMPLATE/consumer-{jam,gap,mismatch}.yml`, метки `consumer/*` объявлены в `.github/labels.yml`, соответствие держит `tests/test_consumer_channel.py` (#14) |

## Про саму работу

| Направление | Исход | Чем держится или почему нет |
|---|---|---|
| Диспетчер исполнителей | профиль Диспетчера | `docs/behaviour.md` — порядок источников работы; `scripts/stuck.py`, `scripts/hail.py`, реестр #263 |
| Агентский процесс | профиль Методолога процесса | `CLAUDE.md`, `scripts/check_window_lifetime.py`, `scripts/check_rulebook_fresh.py`, навык `rulebook-reread`, решения 011, 013, 014, 025, 026 |

## Что эта карта НЕ делает

Она не заводит ролей и не назначает исполнителей: исполнителей по-прежнему
двое. Она называет **вопросы, которых никто не задаёт**, — и делает их видимыми
вместо того, чтобы оставлять слепой зоной. Такие места помечены «роли нет», а
названное и непостроенное — «артефакта нет»; каждое из них ждёт решения
владельца, а не механизма
([154](https://github.com/ArtVsMark/Engineering-Incidents-Playbook/blob/main/rules/ru/154-none-must-name-its-reason.md)).

**Числа дыр здесь НЕ пишутся**, и это не экономия. Рукописное число рядом с
таблицей расходится с ней первой же правкой — и расходится молча
([005](https://github.com/ArtVsMark/Engineering-Incidents-Playbook/blob/main/rules/ru/005-hand-written-numbers-rot.md)).
Первая редакция карты такое число несла — «восемь мест роли нет, три
артефакта нет». Через час 088 перестало быть дырой, и число соврало.

## Состав: кто задаёт эти вопросы

Роли здесь **выведены из карты, а не придуманы рядом**: каждая собрана из
направлений выше, и столбец «Исход» у каждого направления называет её имя.
Разойтись состав с картой не может — он из неё и читается
([049](https://github.com/ArtVsMark/Engineering-Incidents-Playbook/blob/main/rules/ru/049-derive-state-from-live-artifacts.md)).

**Так было не всегда, и это замер.** До 21.09.2026 из двадцати шести профилей
владелец был назван у **семи**; у девятнадцати стояло голое «профиль», что по
шапке значит «владелец с окном» — то есть кто-нибудь из двоих. Вопрос, который
задаёт кто-нибудь, не задаёт никто в тот день, когда оба заняты другим. Нашёл
это владелец, сравнив карту с соседом по семье, где роли расписаны поимённо.

**Различает роли ВХОД** — то, что ложится на стол в начале работы. Пока вход
разный, роли не повторяют друг друга; как только вход совпал, одна из них
лишняя.

| Роль | Вход | Возражает |
|---|---|---|
| 🏛 **Архитектор** | решение о границе и договоре | Механику: «работает, но заводит второй источник» (022) |
| 🔧 **Механик** | шаг или гейт | Архитектору: «договор объявлен, механизма под него нет» (002) |
| 🛰 **Инженер площадки** | как площадка исполняет договор | Механику: «шаг ходит внутренним путём, а потребитель пойдёт внешним» (139) |
| 🧪 **Тестировщик** | механизм | Механику: «зелёное не доказано откатом» (140) |
| 🔍 **Аудитор** | изменение целиком | окну: «вот чего вы о себе не спросили» |
| 🧾 **Ревизор** | список деклараций | всем: «объявлено одно, в дереве другое» |
| ⚖️ **Верификатор** | ОДНА находка | Аудитору: «а докажи — и какова настоящая тяжесть» |
| 🧭 **Критик метода** | след гейта | Тестировщику: «прогон был, а премиса не проверена» (146) |
| 🔐 **Безопасность** | граница прав | Инженеру площадки: «этот триггер отдаёт чужому коду наши секреты» |
| 💰 **Эконом прогонов** | ряд прогонов | Тестировщику: «этот прогон стоит минут из общей квоты» (058) |
| 📦 **Релиз-инженер** | выпуск и то, что уезжает наружу | Механику: «это не попало в выпуск» (замер: 11 отдаваемых файлов из 11 отсутствовали в `v1.1.0`) |
| 👥 **Сосед по семье** | чужое дерево | Архитектору: «у соседа решено иначе и дешевле» (решение [`002`](decisions/002-review-taken-from-two-neighbours.md)) |
| 📝 **Техписатель** | документ | Механику: «пояснение обещает то, чего код не делает» |
| 📰 **Редактор** | то, что механизм говорит наружу | Техписателю: «точно и нечитаемо» |
| 🎛 **Диспетчер** | состояние очереди и окна | всем: «работа не та по источнику» |
| 🤖 **Методолог процесса** | как ведётся само окно | Диспетчеру: «окно пережило срок, его вывод брать нельзя» (006) |

**Владелец ролью окна не является** и стоит отдельно: у него есть предмет,
которого механизм не знает, и его «профиль владельца» — это человек, а не
голос окна.

## Приёмку 062 прошли не все, и отказ записан

Пять направлений остались **профилем соседней роли**, а не стали своей. Это
результат приёмки, а не недосмотр: роль заводится, только если у неё есть
СВОЁ возражение конкретной существующей роли, а не дополнение к ней
([062](https://github.com/ArtVsMark/Engineering-Incidents-Playbook/blob/main/rules/ru/062-a-role-must-be-able-to-object.md)).

| Направление | Стало профилем | Почему не своя роль |
|---|---|---|
| Совместимость | Релиз-инженера | возражает не Релизу, а вместе с ним: вход тот же — то, что уезжает наружу |
| Системный аналитик | Архитектора | вопрос «как это устроено» — тот же договор, только пересказанный |
| Показ | Редактора | «дочитают ли» — дословно вопрос Редактора |
| Сообщество и поддержка | Диспетчера | вход тот же: входящая работа. Своё возражение нашлось бы у потребителей, а их ноль (замер `scripts/consumers.py`) |
| Владелец продукта | владельца | это человек, а не голос окна |

## Взаимодействие: стык важнее роли

Роль без стыка — половина работы. **Механизмы расходятся именно на стыке, и
каждый по отдельности остаётся зелёным**
([146](https://github.com/ArtVsMark/Engineering-Incidents-Playbook/blob/main/rules/ru/146-a-green-gate-does-not-verify-its-premise.md)).

```
изменение ──► 🔍 Аудитор ──► находка (место + отпечаток + авторская оценка)
                                │
                                ▼
                          ⚖️ Верификатор
                                │
                ┌───────────────┼───────────────┐
                ▼               ▼               ▼
          ПОДТВЕРЖДЕНА     ОПРОВЕРГНУТА      ГИПОТЕЗА
          в реестр #23     закрыть с         поверхность
                           причиной          не в этом окне

декларации ─► 🧾 Ревизор ──► расхождение (свой вход, идёт независимо)
след гейта ─► 🧭 Критик метода ──► находки О МЕТОДЕ, отдельным списком
```

**Что каждому ЗАПРЕЩЕНО — важнее того, что разрешено:** роль, которой можно
всё, поглощает соседнюю, и вход перестаёт их различать.

- **🔍 не выносит окончательного вердикта** — его оценка едет заявкой, снимает
  запись только `Разобрано:` в теле починившего изменения.
- **⚖️ не ищет нового.** Найдя дефект по дороге, отдаёт его отдельной находкой:
  иначе собственную находку некому опровергнуть.
- **🧾 не судит качество.** Его предмет — расхождение объявленного с фактом, а
  не дефект.
- **🧭 не находит дефектов продукта.** Его находки о МЕТОДЕ, и адресат у них
  другой.
- **🔧 не объявляет договор.** Меняет договор Архитектор, иначе механизм
  становится собственным обоснованием.
- **💰 не отменяет проверок.** Называет цену, а решает Тестировщик с
  Архитектором.

**Стык, который у нас уже ломался, — замером, а не опасением.** 21.09.2026 шаг
открытия писал причину задержки прозой, а реестр застрявших искал строку
`Ждёт:` — и звал ЗАБЫТОЙ метку с явно названной причиной. Два механизма, оба
зелёные, разошлись на стыке; нашёл это внешний взгляд, а не набор
([#587](../../issues/587)).
