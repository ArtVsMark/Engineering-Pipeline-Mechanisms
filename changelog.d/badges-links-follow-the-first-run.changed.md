### Значки README — по адресу семьи, копия в корне ветки снята

Второй шаг переезда фактов ([#759](../../issues/759)). Первый прогон публикации
после слияния положил значки в `.github/badges/` ветки `badges`: contents-API
отдаёт их там вместе с `facts.json` (`ref=badges`). Теперь README ссылается на
этот адрес, а переходная копия значков в корне ветки из `badges.yml` снята.
Ветка перезаписывается целиком, поэтому следующий прогон унесёт и сами копии.

#759
