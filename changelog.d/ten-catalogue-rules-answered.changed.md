Ответ каталогу правил перестал быть пустым больше чем наполовину: разобрана 71
запись, очередь без ответа — 129 → 58. У 54 правил назван механизм и его адрес в
дереве: квота и её порог, объявленное пустое состояние, отсутствие тихого
запасного пути, необязательный канал, недоверенный вход ревью, контракт
потребления, адресат находок, конфликт как штатный ход конвейера, разведённые
сбор и разбор, вторая половина описания сигнала, пара защит на намерение и
последствие, повтор только временных отказов, внешний взгляд отдельным прогоном,
вердикт после последнего случая, свёртка по имени вместо счёта записей, целый
маркер вместо начала строки, состояние по голове, отказ до подмены состояния,
исходник рядом с производным, производное вне маски входа и вне общей ветки.

У 17 ответ «не относится» — с названной причиной и границей, после которой
правило вступит: параллельных исполнителей, переводов, своего хранилища, чужого
кода, сравнений с послаблениями, сторожа побочных эффектов, уборки после сбоя и
получателя вне дерева у проекта пока нет.

Три правила признаны действующими и **не держатся ничем** — это записано, а не
умолчано: «тест краснеет до фикса» доказывается практикой, а не гейтом; след
падения недоступен второму исполнителю починки (#33); вход проекта описывает
механизмы копированием, пока нет вызова через `workflow_call` (шаг 12).

Заодно появились три механизма, которых ответу не хватало: комплексная задача
ведётся подзадачами площадки, вердикт сводного гейта проверен на том, что два
отказа названы оба, а код не встраивается в шаг прогона строкой, а зовётся
файлом.

#29
